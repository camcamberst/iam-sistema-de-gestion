commit 53c79c071b339489f485a16c8771d696a0309e6c
Author: camcamberst <camcamberst@gmail.com>
Date:   Sat Mar 28 02:38:45 2026 -0500

    perf(chat, ui): modernizaci├│n completa panel Modelo (Apple Style 2) y optimizaci├│n Extrema del ChatWidget con Page Visibility API y reducci├│n masiva de polling de BD

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index ab700c49..fbeecbd2 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -8,6 +8,8 @@ import { getColombiaDate } from '@/utils/calculator-dates';
 import ProgressMilestone from '@/components/ui/ProgressMilestone';
 import AIDashboard from '@/components/AIDashboard';
 import InfoCard, { InfoCardGrid } from '@/components/ui/InfoCard';
+import GlassCard from '@/components/ui/GlassCard';
+import PageHeader from '@/components/ui/PageHeader';
 import AnnouncementBoardWidget from '@/components/AnnouncementBoardWidget';
 import DynamicTimeIsland from '@/components/ui/DynamicTimeIsland';
 import VoiceCodeReader from '@/components/VoiceCodeReader';
@@ -111,7 +113,7 @@ export default function ModelDashboard() {
           behavior: 'smooth'
         });
         
-        console.log('­ƒöì [DASHBOARD] Scrollbar positioned at start:', targetPosition);
+
       }, 1000); // Delay para asegurar que el contenido est├® renderizado
     };
 
@@ -162,7 +164,7 @@ export default function ModelDashboard() {
   const loadProductivityData = async (userId: string) => {
     try {
       setProductivityLoading(true);
-      console.log('­ƒöì [DASHBOARD] Loading productivity data for user:', userId);
+
 
       // 1) Tasas activas
       const ratesRes = await fetch('/api/rates-v2?activeOnly=true', { cache: 'no-store' });
@@ -304,14 +306,6 @@ export default function ModelDashboard() {
       const copModelo = todayUsdModelo * rates.usd_cop;
       const goalProgress = goalUsd > 0 ? Math.min((todayUsdModelo / goalUsd) * 100, 100) : 0;
 
-      console.log('­ƒöì [DASHBOARD] Calculated productivity:', {
-        todayUsdBruto,
-        todayUsdModelo,
-        todayEarnings,
-        copModelo,
-        goalUsd,
-        goalProgress
-      });
 
       setProductivityData({
         usdBruto: todayUsdBruto, // Mantener para la barra de objetivo
@@ -331,7 +325,7 @@ export default function ModelDashboard() {
 
   if (loading) {
     return (
-      <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900 flex items-center justify-center">
+      <div className="aim-page-bg flex items-center justify-center">
         <div className="text-center">
           <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4"></div>
           <p className="text-gray-600 dark:text-gray-300">Cargando dashboard...</p>
@@ -345,36 +339,18 @@ export default function ModelDashboard() {
   }
 
   return (
-    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900">
+    <div className="aim-page-bg">
       <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pt-16">
-        {/* Header */}
-        <div className="mb-8 sm:mb-12">
-          <div className="relative">
-            <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl"></div>
-            <div className="relative bg-white/80 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl p-4 sm:p-6 border border-white/20 dark:border-gray-600/20 shadow-lg dark:shadow-lg dark:shadow-blue-900/15 dark:ring-0.5 dark:ring-blue-400/20">
-              <div className="flex flex-col md:flex-row md:justify-between md:items-center gap-4 md:gap-3">
-                <div className="flex items-center space-x-3 min-w-0 flex-1">
-                  <div className="w-10 h-10 sm:w-12 sm:h-12 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md flex-shrink-0">
-                    <svg className="w-5 h-5 sm:w-6 sm:h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
-                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
-                    </svg>
-                  </div>
-                  <div className="min-w-0 flex-1">
-                    <h1 className="text-base sm:text-lg md:text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 dark:from-gray-100 dark:to-gray-300 bg-clip-text text-transparent leading-tight whitespace-nowrap overflow-hidden text-ellipsis">
-                      Mi Dashboard
-                    </h1>
-                    {user && (
-                      <p className="mt-1 text-xs sm:text-sm text-gray-600 dark:text-gray-300 hidden sm:block">
-                        Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
-                        {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
-                      </p>
-                    )}
-                  </div>
-                </div>
-              </div>
-            </div>
-          </div>
-        </div>
+        <PageHeader
+          title="Mi Dashboard"
+          subtitle={user ? `Bienvenida, ${user.name} ┬À Rol: ${String(user.role).replace('_',' ')}${user.groups.length > 0 ? ` ┬À Grupos: ${user.groups.join(', ')}` : ''}` : undefined}
+          glow="model"
+          icon={
+            <svg className="w-5 h-5 sm:w-6 sm:h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
+            </svg>
+          }
+        />
 
         {/* Barra de Isla Din├ímica - Tiempos del Mundo y Cierre */}
         <DynamicTimeIsland
@@ -392,7 +368,7 @@ export default function ModelDashboard() {
           {/* Columna 2: Resumen de productividad + Lector de C├│digo Vx */}
           {user.role === 'modelo' && (
           <div className="flex flex-col gap-4 sm:gap-6 h-full">
-        <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-3 sm:p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
+        <GlassCard padding="sm" className="rounded-xl">
           <div className="flex items-center space-x-2 mb-2.5 sm:mb-4">
             <div className="w-5 h-5 sm:w-6 sm:h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center flex-shrink-0">
               <svg className="w-2.5 h-2.5 sm:w-3 sm:h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
@@ -576,7 +552,7 @@ export default function ModelDashboard() {
               </div>
             </>
           )}
-        </div>
+        </GlassCard>
             <div className="flex-1">
               <VoiceCodeReader className="h-full" />
             </div>

commit e913a1b41ae1b684a2f1c83652a76dd555993b3b
Author: camcamberst <camcamberst@gmail.com>
Date:   Mon Mar 9 13:00:29 2026 -0500

    Dashboard modelo: restaurar simbolo de pesos en COP Modelo
    
    Made-with: Cursor

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 68b0926b..ab700c49 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -425,7 +425,7 @@ export default function ModelDashboard() {
                 />
                 {/* En m├│vil, la tercera card ocupa 2 columnas para mantener balance */}
                 <InfoCard
-                  value={productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
+                  value={productivityData ? `$${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   label="COP Modelo"
                   color="purple"
                   size="sm"

commit 15cd3c8a904b1da46ee69b4eeec335624a46c2cc
Author: camcamberst <camcamberst@gmail.com>
Date:   Mon Mar 9 12:54:33 2026 -0500

    Dashboard modelo: compactar y estabilizar card de objetivo basico
    
    Made-with: Cursor

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index b1c8b440..68b0926b 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -480,7 +480,7 @@ export default function ModelDashboard() {
 
                   return (
                     <div
-                      className={`relative overflow-hidden rounded-2xl border transition-all duration-300`}
+                      className="relative overflow-hidden rounded-2xl border transition-all duration-300 min-h-[112px] sm:min-h-[124px]"
                       style={{
                         background: `linear-gradient(135deg, ${cardBgStart}, ${cardBgEnd})`,
                         borderColor: cardBorder,
@@ -498,19 +498,19 @@ export default function ModelDashboard() {
                           {/* Icono animado */}
                           <div className={`relative flex-shrink-0 ${estaPorDebajo ? 'animate-bounce' : 'animate-pulse'}`}>
                             <div
-                              className={`w-5 h-5 sm:w-8 sm:h-8 rounded-full flex items-center justify-center shadow-md`}
+                              className="w-4 h-4 sm:w-6 sm:h-6 rounded-full flex items-center justify-center shadow-md"
                               style={{
                                 background: `linear-gradient(135deg, ${iconStart}, ${iconEnd})`
                               }}
                             >
-                              <span className="text-white text-[10px] sm:text-sm">Ô£ô</span>
+                              <span className="text-white text-[9px] sm:text-xs">Ô£ô</span>
                             </div>
                           </div>
                           
                           {/* Contenido compacto */}
                           <div className="flex-1 min-w-0">
                             <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-0.5 sm:gap-0">
-                              <div className={`font-bold text-[11px] sm:text-sm leading-tight`} style={{ color: headingColor }}>
+                              <div className="font-bold text-[10px] sm:text-xs leading-tight" style={{ color: headingColor }}>
                                 {estaPorDebajo ? 'Objetivo B├ísico en Progreso' : 'Objetivo B├ísico Alcanzado'}
                               </div>
                               {(() => {
@@ -543,15 +543,17 @@ export default function ModelDashboard() {
                               })()}
                             </div>
                             
-                            {/* Mensaje de progreso por hito */}
-                            {(() => {
-                              const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
-                              return <ProgressMilestone progress={roundedProgress} />;
-                            })()}
+                            {/* Mensaje de progreso por hito (alto fijo para evitar saltos de layout) */}
+                            <div className="mt-1 sm:mt-1.5 min-h-[22px] sm:min-h-[26px] flex items-center">
+                              {(() => {
+                                const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
+                                return <ProgressMilestone progress={roundedProgress} />;
+                              })()}
+                            </div>
                             
                             {/* Barra de progreso compacta */}
                             <div className="mt-1.5 sm:mt-2">
-                              <div className="w-full bg-gray-200 rounded-full h-1.5 sm:h-2 overflow-hidden">
+                              <div className="w-full bg-gray-200 rounded-full h-1.5 sm:h-1.5 overflow-hidden">
                                 <div 
                                   className={`h-full transition-all duration-1000 ease-out`}
                                   style={{ 

commit 497cc6b3137aac2a0b67169bec13de56f9673ebc
Author: camcamberst <camcamberst@gmail.com>
Date:   Tue Feb 24 19:49:53 2026 -0500

    fix: stretch VoiceCodeReader to fill remaining column height symmetrically
    
    Co-authored-by: Cursor <cursoragent@cursor.com>

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index f6878a26..b1c8b440 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -384,14 +384,14 @@ export default function ModelDashboard() {
         />
 
         {/* Corcho Informativo + Resumen de Productividad ÔÇö 2 columnas en desktop */}
-        <div className="grid grid-cols-1 lg:grid-cols-2 gap-4 sm:gap-6 mb-4 sm:mb-6 items-start">
+        <div className="grid grid-cols-1 lg:grid-cols-2 gap-4 sm:gap-6 mb-4 sm:mb-6 lg:items-stretch">
 
           {/* Columna 1: Corcho Informativo */}
           <AnnouncementBoardWidget userId={user.id} userGroups={user.groups} />
 
           {/* Columna 2: Resumen de productividad + Lector de C├│digo Vx */}
           {user.role === 'modelo' && (
-          <div className="flex flex-col gap-4 sm:gap-6">
+          <div className="flex flex-col gap-4 sm:gap-6 h-full">
         <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-3 sm:p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
           <div className="flex items-center space-x-2 mb-2.5 sm:mb-4">
             <div className="w-5 h-5 sm:w-6 sm:h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center flex-shrink-0">
@@ -575,7 +575,9 @@ export default function ModelDashboard() {
             </>
           )}
         </div>
-            <VoiceCodeReader />
+            <div className="flex-1">
+              <VoiceCodeReader className="h-full" />
+            </div>
           </div>
           )}
 

commit 34a628df4b632e3d23c87b46539dc52ace52514b
Author: camcamberst <camcamberst@gmail.com>
Date:   Tue Feb 24 19:44:09 2026 -0500

    fix: move VoiceCodeReader into right column below productivity summary
    
    Co-authored-by: Cursor <cursoragent@cursor.com>

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index e788fe8c..f6878a26 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -389,8 +389,9 @@ export default function ModelDashboard() {
           {/* Columna 1: Corcho Informativo */}
           <AnnouncementBoardWidget userId={user.id} userGroups={user.groups} />
 
-          {/* Columna 2: Resumen de productividad */}
+          {/* Columna 2: Resumen de productividad + Lector de C├│digo Vx */}
           {user.role === 'modelo' && (
+          <div className="flex flex-col gap-4 sm:gap-6">
         <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-3 sm:p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
           <div className="flex items-center space-x-2 mb-2.5 sm:mb-4">
             <div className="w-5 h-5 sm:w-6 sm:h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center flex-shrink-0">
@@ -574,15 +575,12 @@ export default function ModelDashboard() {
             </>
           )}
         </div>
+            <VoiceCodeReader />
+          </div>
           )}
 
         </div>{/* fin grid 2 columnas */}
 
-        {/* Lector de C├│digo Vx */}
-        <div className="mt-6">
-          <VoiceCodeReader />
-        </div>
-
         {/* AI Dashboard */}
         <div className="mt-6">
           <AIDashboard userId={user.id} userRole={user.role} />

commit 5801646baf2749688c6283d27a53477f8b302708
Author: camcamberst <camcamberst@gmail.com>
Date:   Tue Feb 24 19:38:18 2026 -0500

    feat: display AnnouncementBoardWidget and productivity summary side by side in model dashboard
    
    Co-authored-by: Cursor <cursoragent@cursor.com>

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 62bb6516..e788fe8c 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -383,13 +383,14 @@ export default function ModelDashboard() {
           facturadoPeriodoUsd={user?.role === 'modelo' ? periodGoal?.periodBilledUsd : undefined}
         />
 
-        {/* Corcho Informativo - Widget */}
-        <div className="mb-4 sm:mb-6">
+        {/* Corcho Informativo + Resumen de Productividad ÔÇö 2 columnas en desktop */}
+        <div className="grid grid-cols-1 lg:grid-cols-2 gap-4 sm:gap-6 mb-4 sm:mb-6 items-start">
+
+          {/* Columna 1: Corcho Informativo */}
           <AnnouncementBoardWidget userId={user.id} userGroups={user.groups} />
-        </div>
 
-        {/* Resumen de productividad y progreso de meta (solo para modelos) */}
-        {user.role === 'modelo' && (
+          {/* Columna 2: Resumen de productividad */}
+          {user.role === 'modelo' && (
         <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-3 sm:p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
           <div className="flex items-center space-x-2 mb-2.5 sm:mb-4">
             <div className="w-5 h-5 sm:w-6 sm:h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center flex-shrink-0">
@@ -573,7 +574,9 @@ export default function ModelDashboard() {
             </>
           )}
         </div>
-        )}
+          )}
+
+        </div>{/* fin grid 2 columnas */}
 
         {/* Lector de C├│digo Vx */}
         <div className="mt-6">

commit a720124b2cee67a873a9f586f36eb4f3d698212c
Author: camcamberst <camcamberst@gmail.com>
Date:   Tue Feb 24 19:29:18 2026 -0500

    feat: add VoiceCodeReader tool for Vx platform codes in model dashboard
    
    Co-authored-by: Cursor <cursoragent@cursor.com>

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 277e6747..62bb6516 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -10,6 +10,7 @@ import AIDashboard from '@/components/AIDashboard';
 import InfoCard, { InfoCardGrid } from '@/components/ui/InfoCard';
 import AnnouncementBoardWidget from '@/components/AnnouncementBoardWidget';
 import DynamicTimeIsland from '@/components/ui/DynamicTimeIsland';
+import VoiceCodeReader from '@/components/VoiceCodeReader';
 
 interface User {
   id: string;
@@ -574,6 +575,11 @@ export default function ModelDashboard() {
         </div>
         )}
 
+        {/* Lector de C├│digo Vx */}
+        <div className="mt-6">
+          <VoiceCodeReader />
+        </div>
+
         {/* AI Dashboard */}
         <div className="mt-6">
           <AIDashboard userId={user.id} userRole={user.role} />

commit 2843c5a919aebb5f116878a70f6db4ad14ae1625
Author: camcamberst <camcamberst@gmail.com>
Date:   Tue Feb 10 17:06:52 2026 -0500

    Mensaje promedio: color #0d0d0d forzado para maximo contraste sobre fondo claro
    
    Co-authored-by: Cursor <cursoragent@cursor.com>

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 59aa6806..277e6747 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -525,10 +525,10 @@ export default function ModelDashboard() {
                                   <div
                                     className={`text-[9px] sm:text-xs leading-tight transition-all duration-300 ${
                                       showPromedio
-                                        ? 'font-semibold text-gray-900 dark:text-white pl-1.5 border-l-2 border-emerald-500 dark:border-emerald-400'
+                                        ? 'font-semibold pl-1.5 border-l-2 border-emerald-600'
                                         : 'font-medium'
                                     }`}
-                                    style={!showPromedio ? { color: subTextColor } : undefined}
+                                    style={showPromedio ? { color: '#0d0d0d' } : { color: subTextColor }}
                                   >
                                     {estaPorDebajo
                                       ? showPromedio

commit 4231ad94e5aa206bea1d4e99e9cb2959674f59dc
Author: camcamberst <camcamberst@gmail.com>
Date:   Tue Feb 10 17:01:07 2026 -0500

    Mensaje promedio: sin franja, texto oscuro/blanco legible y solo borde izquierdo
    
    Co-authored-by: Cursor <cursoragent@cursor.com>

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 8b511a7d..59aa6806 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -525,7 +525,7 @@ export default function ModelDashboard() {
                                   <div
                                     className={`text-[9px] sm:text-xs leading-tight transition-all duration-300 ${
                                       showPromedio
-                                        ? 'font-semibold px-2 py-0.5 rounded-md bg-emerald-500/20 dark:bg-emerald-400/25 text-emerald-800 dark:text-emerald-200 ring-1 ring-emerald-400/40 dark:ring-emerald-300/30'
+                                        ? 'font-semibold text-gray-900 dark:text-white pl-1.5 border-l-2 border-emerald-500 dark:border-emerald-400'
                                         : 'font-medium'
                                     }`}
                                     style={!showPromedio ? { color: subTextColor } : undefined}

commit 12de21336aa93d4dcb5b36dbf3c58976c459f5ca
Author: camcamberst <camcamberst@gmail.com>
Date:   Tue Feb 10 16:40:30 2026 -0500

    Destacar mensaje promedio diario en barra Objetivo (fondo y texto esmeralda)
    
    Co-authored-by: Cursor <cursoragent@cursor.com>

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 14632e1d..8b511a7d 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -522,7 +522,14 @@ export default function ModelDashboard() {
                                 const dailyAvg = daysLeft > 0 ? remaining / daysLeft : 0;
                                 const showPromedio = estaPorDebajo && dailyAvg > 0 && (objectiveBarFlip % 2 === 1);
                                 return (
-                                  <div className={`text-[9px] sm:text-xs font-medium leading-tight`} style={{ color: subTextColor }}>
+                                  <div
+                                    className={`text-[9px] sm:text-xs leading-tight transition-all duration-300 ${
+                                      showPromedio
+                                        ? 'font-semibold px-2 py-0.5 rounded-md bg-emerald-500/20 dark:bg-emerald-400/25 text-emerald-800 dark:text-emerald-200 ring-1 ring-emerald-400/40 dark:ring-emerald-300/30'
+                                        : 'font-medium'
+                                    }`}
+                                    style={!showPromedio ? { color: subTextColor } : undefined}
+                                  >
                                     {estaPorDebajo
                                       ? showPromedio
                                         ? `Para alcanzar tu objetivo necesitas facturar $${Math.round(dailyAvg).toLocaleString('es-CO')} en promedio`

commit f6f7079846f32c987723c23416c6cb60f579471f
Author: camcamberst <camcamberst@gmail.com>
Date:   Tue Feb 10 16:20:04 2026 -0500

    Barra Objetivo Basico: rotar mensaje con promedio diario cada 7s (dashboard y calculadora)
    
    Co-authored-by: Cursor <cursoragent@cursor.com>

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 280d4133..14632e1d 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -37,6 +37,7 @@ export default function ModelDashboard() {
   const [productivityData, setProductivityData] = useState<ProductivityData | null>(null);
   const [productivityLoading, setProductivityLoading] = useState(false);
   const [periodGoal, setPeriodGoal] = useState<{ goalUsd: number; periodBilledUsd: number } | null>(null);
+  const [objectiveBarFlip, setObjectiveBarFlip] = useState(0);
   const router = useRouter();
   const supabase = require('@/lib/supabase').supabase;
 
@@ -119,6 +120,12 @@ export default function ModelDashboard() {
     }
   }, [loading, productivityLoading, user]);
 
+  // Rotar mensaje de la barra "Objetivo B├ísico" cada 7 s (progreso Ôåö promedio diario)
+  useEffect(() => {
+    const t = setInterval(() => setObjectiveBarFlip((f) => f + 1), 7000);
+    return () => clearInterval(t);
+  }, []);
+
   const refreshPeriodGoal = async (userId: string) => {
     try {
       const res = await fetch(`/api/calculator/period-goal-summary?modelId=${userId}`, { cache: 'no-store' });
@@ -506,10 +513,20 @@ export default function ModelDashboard() {
                               {(() => {
                                 const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
                                 const remainingPct = Math.max(0, 100 - roundedProgress);
+                                const colDate = getColombiaDate();
+                                const [y, m, d] = colDate.split('-').map(Number);
+                                const lastDay = new Date(y, m, 0).getDate();
+                                const nextClosure = d <= 15 ? 15 : lastDay;
+                                const daysLeft = Math.max(0, nextClosure - d);
+                                const remaining = periodGoal ? Math.max(0, periodGoal.goalUsd - periodGoal.periodBilledUsd) : 0;
+                                const dailyAvg = daysLeft > 0 ? remaining / daysLeft : 0;
+                                const showPromedio = estaPorDebajo && dailyAvg > 0 && (objectiveBarFlip % 2 === 1);
                                 return (
                                   <div className={`text-[9px] sm:text-xs font-medium leading-tight`} style={{ color: subTextColor }}>
                                     {estaPorDebajo
-                                      ? `Faltan $${Math.ceil(cuotaMinima - totalUsdBruto)} USD (${remainingPct}% restante)`
+                                      ? showPromedio
+                                        ? `Para alcanzar tu objetivo necesitas facturar $${Math.round(dailyAvg).toLocaleString('es-CO')} en promedio`
+                                        : `Faltan $${Math.ceil(cuotaMinima - totalUsdBruto)} USD (${remainingPct}% restante)`
                                       : `Excelente +${Math.max(0, roundedProgress - 100)}%`}
                                   </div>
                                 );

commit e650f472f85b9b5b5a47261ca0695584b50e8dbe
Author: camcamberst <camcamberst@gmail.com>
Date:   Tue Feb 10 15:32:10 2026 -0500

    Barra horaria: objetivo/facturado + promedio diario rotativo; dise├â┬▒o mejorado; API period-goal-summary
    
    Co-authored-by: Cursor <cursoragent@cursor.com>

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 27a5feca..280d4133 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -36,6 +36,7 @@ export default function ModelDashboard() {
   const [loading, setLoading] = useState(true);
   const [productivityData, setProductivityData] = useState<ProductivityData | null>(null);
   const [productivityLoading, setProductivityLoading] = useState(false);
+  const [periodGoal, setPeriodGoal] = useState<{ goalUsd: number; periodBilledUsd: number } | null>(null);
   const router = useRouter();
   const supabase = require('@/lib/supabase').supabase;
 
@@ -76,9 +77,16 @@ export default function ModelDashboard() {
         };
         setUser(current);
         
-        // Cargar datos de productividad si es modelo
+        // Cargar datos de productividad y resumen periodo/objetivo si es modelo
         if (current.role === 'modelo') {
           await loadProductivityData(current.id);
+          try {
+            const res = await fetch(`/api/calculator/period-goal-summary?modelId=${current.id}`, { cache: 'no-store' });
+            const json = await res.json();
+            if (json?.success && typeof json.goalUsd === 'number') {
+              setPeriodGoal({ goalUsd: json.goalUsd, periodBilledUsd: json.periodBilledUsd ?? 0 });
+            }
+          } catch (_) {}
         }
       } finally {
         setLoading(false);
@@ -111,16 +119,28 @@ export default function ModelDashboard() {
     }
   }, [loading, productivityLoading, user]);
 
+  const refreshPeriodGoal = async (userId: string) => {
+    try {
+      const res = await fetch(`/api/calculator/period-goal-summary?modelId=${userId}`, { cache: 'no-store' });
+      const json = await res.json();
+      if (json?.success && typeof json.goalUsd === 'number') {
+        setPeriodGoal({ goalUsd: json.goalUsd, periodBilledUsd: json.periodBilledUsd ?? 0 });
+      }
+    } catch (_) {}
+  };
+
   // Refrescar datos al volver a la pesta├▒a/ventana o al volver desde otra ruta
   useEffect(() => {
     const handleFocus = () => {
       if (user?.role === 'modelo') {
         loadProductivityData(user.id);
+        refreshPeriodGoal(user.id);
       }
     };
     const handleVisibility = () => {
       if (document.visibilityState === 'visible' && user?.role === 'modelo') {
         loadProductivityData(user.id);
+        refreshPeriodGoal(user.id);
       }
     };
     window.addEventListener('focus', handleFocus);
@@ -349,7 +369,11 @@ export default function ModelDashboard() {
         </div>
 
         {/* Barra de Isla Din├ímica - Tiempos del Mundo y Cierre */}
-        <DynamicTimeIsland className="!max-w-none !px-0" />
+        <DynamicTimeIsland
+          className="!max-w-none !px-0"
+          objetivoUsd={user?.role === 'modelo' ? periodGoal?.goalUsd : undefined}
+          facturadoPeriodoUsd={user?.role === 'modelo' ? periodGoal?.periodBilledUsd : undefined}
+        />
 
         {/* Corcho Informativo - Widget */}
         <div className="mb-4 sm:mb-6">

commit edd08d94fc5b493baac65fd9bac18e855d80fcb9
Author: camcamberst <camcamberst@gmail.com>
Date:   Thu Jan 15 15:52:50 2026 -0500

    feat: implementar barra de isla din├â┬ímica con relojes mundiales y cuenta regresiva de cierre + correcciones de sincronizaci├â┬│n de totales

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 10663972..27a5feca 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -9,6 +9,7 @@ import ProgressMilestone from '@/components/ui/ProgressMilestone';
 import AIDashboard from '@/components/AIDashboard';
 import InfoCard, { InfoCardGrid } from '@/components/ui/InfoCard';
 import AnnouncementBoardWidget from '@/components/AnnouncementBoardWidget';
+import DynamicTimeIsland from '@/components/ui/DynamicTimeIsland';
 
 interface User {
   id: string;
@@ -347,6 +348,9 @@ export default function ModelDashboard() {
           </div>
         </div>
 
+        {/* Barra de Isla Din├ímica - Tiempos del Mundo y Cierre */}
+        <DynamicTimeIsland className="!max-w-none !px-0" />
+
         {/* Corcho Informativo - Widget */}
         <div className="mb-4 sm:mb-6">
           <AnnouncementBoardWidget userId={user.id} userGroups={user.groups} />

commit 4d27219eba809a29d453f484fbdcfe597ea986c6
Author: camcamberst <camcamberst@gmail.com>
Date:   Sat Dec 27 21:24:53 2025 -0500

    feat: Compactar Resumen de Productividad para mejor UX movil - Cards: grid-cols-2 en movil (2 columnas), tercera card ocupa 2 columnas - Padding reducido: p-3 en movil, p-2.5 en barra de progreso - InfoCard: padding p-2 en movil para size sm, texto text-base en movil - Barra de progreso: h-1.5 en movil, icono w-5 h-5, texto mas compacto - Espaciado reducido: mb-2.5, mt-2.5, gap-2 en movil - Labels mas cortos: 'USD Modelo' y 'COP Modelo' sin '(hoy)' - Mejor distribucion y legibilidad en pantallas moviles

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 33af7159..10663972 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -7,7 +7,7 @@ import { createClient } from "@supabase/supabase-js";
 import { getColombiaDate } from '@/utils/calculator-dates';
 import ProgressMilestone from '@/components/ui/ProgressMilestone';
 import AIDashboard from '@/components/AIDashboard';
-import { InfoCardGrid } from '@/components/ui/InfoCard';
+import InfoCard, { InfoCardGrid } from '@/components/ui/InfoCard';
 import AnnouncementBoardWidget from '@/components/AnnouncementBoardWidget';
 
 interface User {
@@ -354,8 +354,8 @@ export default function ModelDashboard() {
 
         {/* Resumen de productividad y progreso de meta (solo para modelos) */}
         {user.role === 'modelo' && (
-        <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-4 sm:p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
-          <div className="flex items-center space-x-2 mb-3 sm:mb-4">
+        <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-3 sm:p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
+          <div className="flex items-center space-x-2 mb-2.5 sm:mb-4">
             <div className="w-5 h-5 sm:w-6 sm:h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center flex-shrink-0">
               <svg className="w-2.5 h-2.5 sm:w-3 sm:h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                 <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
@@ -365,39 +365,38 @@ export default function ModelDashboard() {
           </div>
           
           {productivityLoading ? (
-            <div className="text-center py-4">
-              <div className="animate-spin rounded-full h-6 w-6 border-b-2 border-blue-600 mx-auto mb-2"></div>
-              <p className="text-sm text-gray-600 dark:text-gray-300">Cargando datos de productividad...</p>
+            <div className="text-center py-3 sm:py-4">
+              <div className="animate-spin rounded-full h-5 w-5 sm:h-6 sm:w-6 border-b-2 border-blue-600 mx-auto mb-2"></div>
+              <p className="text-xs sm:text-sm text-gray-600 dark:text-gray-300">Cargando datos de productividad...</p>
             </div>
           ) : (
             <>
-              <InfoCardGrid
-                cards={[
-                  {
-                    value: productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö',
-                    label: 'Ganancias Hoy',
-                    color: 'blue',
-                    size: 'sm'
-                  },
-                  {
-                    value: productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö',
-                    label: 'USD Modelo (hoy)',
-                    color: 'green',
-                    size: 'sm'
-                  },
-                  {
-                    value: productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö',
-                    label: 'COP Modelo (hoy)',
-                    color: 'purple',
-                    size: 'sm'
-                  }
-                ]}
-                columns={3}
-                className="mb-3 sm:mb-4"
-              />
+              {/* M├│vil: 2 columnas, Escritorio: 3 columnas */}
+              <div className="grid grid-cols-2 md:grid-cols-3 gap-2 sm:gap-4 mb-2.5 sm:mb-4">
+                <InfoCard
+                  value={productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
+                  label="Ganancias Hoy"
+                  color="blue"
+                  size="sm"
+                />
+                <InfoCard
+                  value={productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
+                  label="USD Modelo"
+                  color="green"
+                  size="sm"
+                />
+                {/* En m├│vil, la tercera card ocupa 2 columnas para mantener balance */}
+                <InfoCard
+                  value={productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
+                  label="COP Modelo"
+                  color="purple"
+                  size="sm"
+                  className="col-span-2 md:col-span-1"
+                />
+              </div>
 
               {/* Barra de alcance de meta - Copia exacta de Mi Calculadora */}
-              <div className="mt-3 sm:mt-4">
+              <div className="mt-2.5 sm:mt-4">
                 {(() => {
                   if (!productivityData) return null;
                   
@@ -456,31 +455,31 @@ export default function ModelDashboard() {
                         style={{ background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})` }}
                       ></div>
 
-                      <div className="relative p-3 sm:p-4">
+                      <div className="relative p-2.5 sm:p-4">
                         <div className="flex items-center space-x-2 sm:space-x-3">
                           {/* Icono animado */}
                           <div className={`relative flex-shrink-0 ${estaPorDebajo ? 'animate-bounce' : 'animate-pulse'}`}>
                             <div
-                              className={`w-6 h-6 sm:w-8 sm:h-8 rounded-full flex items-center justify-center shadow-md`}
+                              className={`w-5 h-5 sm:w-8 sm:h-8 rounded-full flex items-center justify-center shadow-md`}
                               style={{
                                 background: `linear-gradient(135deg, ${iconStart}, ${iconEnd})`
                               }}
                             >
-                              <span className="text-white text-xs sm:text-sm">Ô£ô</span>
+                              <span className="text-white text-[10px] sm:text-sm">Ô£ô</span>
                             </div>
                           </div>
                           
                           {/* Contenido compacto */}
                           <div className="flex-1 min-w-0">
-                            <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-1 sm:gap-0">
-                              <div className={`font-bold text-xs sm:text-sm leading-tight`} style={{ color: headingColor }}>
+                            <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-0.5 sm:gap-0">
+                              <div className={`font-bold text-[11px] sm:text-sm leading-tight`} style={{ color: headingColor }}>
                                 {estaPorDebajo ? 'Objetivo B├ísico en Progreso' : 'Objetivo B├ísico Alcanzado'}
                               </div>
                               {(() => {
                                 const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
                                 const remainingPct = Math.max(0, 100 - roundedProgress);
                                 return (
-                                  <div className={`text-[10px] sm:text-xs font-medium leading-tight`} style={{ color: subTextColor }}>
+                                  <div className={`text-[9px] sm:text-xs font-medium leading-tight`} style={{ color: subTextColor }}>
                                     {estaPorDebajo
                                       ? `Faltan $${Math.ceil(cuotaMinima - totalUsdBruto)} USD (${remainingPct}% restante)`
                                       : `Excelente +${Math.max(0, roundedProgress - 100)}%`}
@@ -496,8 +495,8 @@ export default function ModelDashboard() {
                             })()}
                             
                             {/* Barra de progreso compacta */}
-                            <div className="mt-2">
-                              <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
+                            <div className="mt-1.5 sm:mt-2">
+                              <div className="w-full bg-gray-200 rounded-full h-1.5 sm:h-2 overflow-hidden">
                                 <div 
                                   className={`h-full transition-all duration-1000 ease-out`}
                                   style={{ 
@@ -515,7 +514,7 @@ export default function ModelDashboard() {
                 })()}
               </div>
 
-              <div className="mt-3 sm:mt-4 text-[10px] sm:text-xs text-gray-500 dark:text-gray-300">
+              <div className="mt-2 sm:mt-4 text-[10px] sm:text-xs text-gray-500 dark:text-gray-300">
                 Para actualizar tus valores usa el men├║ <a href="/admin/model/calculator" className="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 underline font-medium">Mi Calculadora</a>.
               </div>
             </>

commit d868340043b073efa4211e9e470ed2a89b09b924
Author: camcamberst <camcamberst@gmail.com>
Date:   Sat Dec 27 16:23:30 2025 -0500

    feat: Mejorar experiencia visual movil en dashboard modelo - Corcho Informativo: padding responsivo (p-4 sm:p-6), iconos y textos adaptados - Resumen de Productividad: padding responsivo (p-4 sm:p-6), iconos y textos adaptados - InfoCardGrid con tamanos sm para movil - Barra de progreso con layout flex-col en movil - Todos los cambios usan clases responsivas (sm:, md:) sin afectar escritorio

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 584e692a..33af7159 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -348,20 +348,20 @@ export default function ModelDashboard() {
         </div>
 
         {/* Corcho Informativo - Widget */}
-        <div className="mb-6">
+        <div className="mb-4 sm:mb-6">
           <AnnouncementBoardWidget userId={user.id} userGroups={user.groups} />
         </div>
 
         {/* Resumen de productividad y progreso de meta (solo para modelos) */}
         {user.role === 'modelo' && (
-        <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
-          <div className="flex items-center space-x-2 mb-4">
-            <div className="w-6 h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center">
-              <svg className="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+        <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-4 sm:p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
+          <div className="flex items-center space-x-2 mb-3 sm:mb-4">
+            <div className="w-5 h-5 sm:w-6 sm:h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center flex-shrink-0">
+              <svg className="w-2.5 h-2.5 sm:w-3 sm:h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                 <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
               </svg>
             </div>
-            <h2 className="text-base font-semibold text-gray-900 dark:text-white">Resumen de Productividad</h2>
+            <h2 className="text-sm sm:text-base font-semibold text-gray-900 dark:text-white">Resumen de Productividad</h2>
           </div>
           
           {productivityLoading ? (
@@ -376,25 +376,28 @@ export default function ModelDashboard() {
                   {
                     value: productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö',
                     label: 'Ganancias Hoy',
-                    color: 'blue'
+                    color: 'blue',
+                    size: 'sm'
                   },
                   {
                     value: productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö',
                     label: 'USD Modelo (hoy)',
-                    color: 'green'
+                    color: 'green',
+                    size: 'sm'
                   },
                   {
                     value: productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö',
                     label: 'COP Modelo (hoy)',
-                    color: 'purple'
+                    color: 'purple',
+                    size: 'sm'
                   }
                 ]}
                 columns={3}
-                className="mb-4"
+                className="mb-3 sm:mb-4"
               />
 
               {/* Barra de alcance de meta - Copia exacta de Mi Calculadora */}
-              <div className="mt-4">
+              <div className="mt-3 sm:mt-4">
                 {(() => {
                   if (!productivityData) return null;
                   
@@ -453,31 +456,31 @@ export default function ModelDashboard() {
                         style={{ background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})` }}
                       ></div>
 
-                      <div className="relative p-4">
-                        <div className="flex items-center space-x-3">
+                      <div className="relative p-3 sm:p-4">
+                        <div className="flex items-center space-x-2 sm:space-x-3">
                           {/* Icono animado */}
                           <div className={`relative flex-shrink-0 ${estaPorDebajo ? 'animate-bounce' : 'animate-pulse'}`}>
                             <div
-                              className={`w-8 h-8 rounded-full flex items-center justify-center shadow-md`}
+                              className={`w-6 h-6 sm:w-8 sm:h-8 rounded-full flex items-center justify-center shadow-md`}
                               style={{
                                 background: `linear-gradient(135deg, ${iconStart}, ${iconEnd})`
                               }}
                             >
-                              <span className="text-white text-sm">Ô£ô</span>
+                              <span className="text-white text-xs sm:text-sm">Ô£ô</span>
                             </div>
                           </div>
                           
                           {/* Contenido compacto */}
-                          <div className="flex-1">
-                            <div className="flex items-center justify-between">
-                              <div className={`font-bold text-sm`} style={{ color: headingColor }}>
+                          <div className="flex-1 min-w-0">
+                            <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-1 sm:gap-0">
+                              <div className={`font-bold text-xs sm:text-sm leading-tight`} style={{ color: headingColor }}>
                                 {estaPorDebajo ? 'Objetivo B├ísico en Progreso' : 'Objetivo B├ísico Alcanzado'}
                               </div>
                               {(() => {
                                 const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
                                 const remainingPct = Math.max(0, 100 - roundedProgress);
                                 return (
-                                  <div className={`text-xs font-medium`} style={{ color: subTextColor }}>
+                                  <div className={`text-[10px] sm:text-xs font-medium leading-tight`} style={{ color: subTextColor }}>
                                     {estaPorDebajo
                                       ? `Faltan $${Math.ceil(cuotaMinima - totalUsdBruto)} USD (${remainingPct}% restante)`
                                       : `Excelente +${Math.max(0, roundedProgress - 100)}%`}
@@ -512,7 +515,7 @@ export default function ModelDashboard() {
                 })()}
               </div>
 
-              <div className="mt-4 text-xs text-gray-500 dark:text-gray-300">
+              <div className="mt-3 sm:mt-4 text-[10px] sm:text-xs text-gray-500 dark:text-gray-300">
                 Para actualizar tus valores usa el men├║ <a href="/admin/model/calculator" className="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 underline font-medium">Mi Calculadora</a>.
               </div>
             </>

commit 42845684379c0ffbccffe04fea33372ae91541ce
Author: camcamberst <camcamberst@gmail.com>
Date:   Sat Dec 27 16:08:56 2025 -0500

    feat: Aplicar estandar movil al header del Dashboard del panel modelo - Contenedor externo: mb-8 sm:mb-12 - Fondo: dark:bg-gray-700/70 - Padding responsivo: p-4 sm:p-6 - Layout vertical en movil: flex-col md:flex-row - Icono con tamanos responsivos: w-10 h-10 sm:w-12 sm:h-12 - Titulo: text-base sm:text-lg md:text-2xl con whitespace-nowrap - Subtitulo dinamico oculto en movil: hidden sm:block - Mismo estandar que 'Gestion de Usuarios'

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 680dffd1..584e692a 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -319,26 +319,28 @@ export default function ModelDashboard() {
     <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900">
       <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pt-16">
         {/* Header */}
-        <div className="mb-12">
+        <div className="mb-8 sm:mb-12">
           <div className="relative">
             <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl"></div>
-            <div className="relative bg-white/80 dark:bg-gray-700/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 dark:border-gray-600/20 shadow-lg dark:shadow-lg dark:shadow-blue-900/15 dark:ring-0.5 dark:ring-blue-400/20">
-              <div className="flex items-center space-x-3">
-                <div className="w-10 h-10 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md">
-                  <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
-                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
-                  </svg>
-                </div>
-                <div>
-                  <h1 className="text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 dark:from-gray-100 dark:to-gray-300 bg-clip-text text-transparent">
-                    Mi Dashboard
-                  </h1>
-                  {user && (
-                    <p className="mt-1 text-sm text-gray-600 dark:text-gray-300">
-                      Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
-                      {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
-                    </p>
-                  )}
+            <div className="relative bg-white/80 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl p-4 sm:p-6 border border-white/20 dark:border-gray-600/20 shadow-lg dark:shadow-lg dark:shadow-blue-900/15 dark:ring-0.5 dark:ring-blue-400/20">
+              <div className="flex flex-col md:flex-row md:justify-between md:items-center gap-4 md:gap-3">
+                <div className="flex items-center space-x-3 min-w-0 flex-1">
+                  <div className="w-10 h-10 sm:w-12 sm:h-12 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md flex-shrink-0">
+                    <svg className="w-5 h-5 sm:w-6 sm:h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                      <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
+                    </svg>
+                  </div>
+                  <div className="min-w-0 flex-1">
+                    <h1 className="text-base sm:text-lg md:text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 dark:from-gray-100 dark:to-gray-300 bg-clip-text text-transparent leading-tight whitespace-nowrap overflow-hidden text-ellipsis">
+                      Mi Dashboard
+                    </h1>
+                    {user && (
+                      <p className="mt-1 text-xs sm:text-sm text-gray-600 dark:text-gray-300 hidden sm:block">
+                        Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
+                        {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
+                      </p>
+                    )}
+                  </div>
                 </div>
               </div>
             </div>

commit b80f22d834379a376383bbcba6f3fd8baf68b097
Author: camcamberst <camcamberst@gmail.com>
Date:   Tue Nov 4 00:28:49 2025 -0500

    Feat: Reubicar widget corcho informativo y abrir publicaciones en nueva pesta├▒a - Mover widget del corcho informativo debajo del header del dashboard principal - Cambiar modal por apertura en nueva pesta├▒a/pop-up - Crear p├ígina dedicada para mostrar publicaciones completas - Eliminar componente modal que ya no se utiliza

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 88cfe59c..680dffd1 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -345,6 +345,11 @@ export default function ModelDashboard() {
           </div>
         </div>
 
+        {/* Corcho Informativo - Widget */}
+        <div className="mb-6">
+          <AnnouncementBoardWidget userId={user.id} userGroups={user.groups} />
+        </div>
+
         {/* Resumen de productividad y progreso de meta (solo para modelos) */}
         {user.role === 'modelo' && (
         <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
@@ -518,11 +523,6 @@ export default function ModelDashboard() {
           <AIDashboard userId={user.id} userRole={user.role} />
         </div>
 
-        {/* Corcho Informativo - Widget */}
-        <div className="mt-6">
-          <AnnouncementBoardWidget userId={user.id} userGroups={user.groups} />
-        </div>
-
       </div>
     </div>
   );

commit e9a7ce14527f5a33977f40fc3e5ba42535e943cf
Author: camcamberst <camcamberst@gmail.com>
Date:   Mon Nov 3 23:10:47 2025 -0500

    feat(corcho-informativo): implementar sistema de publicaciones tipo blog para modelos - Crear esquema de BD (announcements, categories, group_targets, views) - Implementar API routes (GET, POST, PUT, DELETE) - Crear widget de visualizaci├│n en dashboard de modelos - Integrar widget en dashboard principal de modelos - Configurar RLS y permisos por rol - Crear bucket de Storage para im├ígenes - Documentaci├│n completa en PROPUESTA_CORCHO_INFORMATIVO.md

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index e73cc99b..88cfe59c 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -8,6 +8,7 @@ import { getColombiaDate } from '@/utils/calculator-dates';
 import ProgressMilestone from '@/components/ui/ProgressMilestone';
 import AIDashboard from '@/components/AIDashboard';
 import { InfoCardGrid } from '@/components/ui/InfoCard';
+import AnnouncementBoardWidget from '@/components/AnnouncementBoardWidget';
 
 interface User {
   id: string;
@@ -517,6 +518,11 @@ export default function ModelDashboard() {
           <AIDashboard userId={user.id} userRole={user.role} />
         </div>
 
+        {/* Corcho Informativo - Widget */}
+        <div className="mt-6">
+          <AnnouncementBoardWidget userId={user.id} userGroups={user.groups} />
+        </div>
+
       </div>
     </div>
   );

commit 236739b63c6754da37c0b684fc28b2ced97a6622
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 31 00:49:50 2025 -0500

    chat: bot├│n AIM Assistant sigue la scrollbar

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index f4586b78..e73cc99b 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -1,6 +1,6 @@
 'use client';
 
-import { useState, useEffect, useRef, useCallback } from 'react';
+import { useState, useEffect } from 'react';
 import { useRouter } from 'next/navigation';
 import ModelCalculatorNew from '../../../../components/ModelCalculatorNew';
 import { createClient } from "@supabase/supabase-js";
@@ -36,7 +36,6 @@ export default function ModelDashboard() {
   const [productivityLoading, setProductivityLoading] = useState(false);
   const router = useRouter();
   const supabase = require('@/lib/supabase').supabase;
-  const isRefreshingRef = useRef(false);
 
   useEffect(() => {
     const load = async () => {
@@ -111,38 +110,24 @@ export default function ModelDashboard() {
   }, [loading, productivityLoading, user]);
 
   // Refrescar datos al volver a la pesta├▒a/ventana o al volver desde otra ruta
-  // Solo si es modelo, no est├í cargando, y tiene datos previos cargados
   useEffect(() => {
-    // No agregar listeners si no es modelo o si a├║n est├í cargando
-    if (!user || user.role !== 'modelo' || loading || productivityLoading) {
-      return;
-    }
-
     const handleFocus = () => {
-      if (!isRefreshingRef.current && user?.role === 'modelo' && !loading && !productivityLoading && productivityData) {
-        isRefreshingRef.current = true;
-        loadProductivityData(user.id).finally(() => {
-          isRefreshingRef.current = false;
-        });
+      if (user?.role === 'modelo') {
+        loadProductivityData(user.id);
       }
     };
-
     const handleVisibility = () => {
-      if (document.visibilityState === 'visible' && !isRefreshingRef.current && user?.role === 'modelo' && !loading && !productivityLoading && productivityData) {
-        isRefreshingRef.current = true;
-        loadProductivityData(user.id).finally(() => {
-          isRefreshingRef.current = false;
-        });
+      if (document.visibilityState === 'visible' && user?.role === 'modelo') {
+        loadProductivityData(user.id);
       }
     };
-
     window.addEventListener('focus', handleFocus);
     document.addEventListener('visibilitychange', handleVisibility);
     return () => {
       window.removeEventListener('focus', handleFocus);
       document.removeEventListener('visibilitychange', handleVisibility);
     };
-  }, [user, loading, productivityLoading, productivityData]);
+  }, [user]);
 
   const loadProductivityData = async (userId: string) => {
     try {

commit 78c90f6f17d0dad569593d65bb3046c1c6ae016e
Author: camcamberst <camcamberst@gmail.com>
Date:   Thu Oct 30 18:38:13 2025 -0500

    Fix dashboard modelo: prevenir recargas err├íticas al regresar (listeners solo si es modelo, no cargando y con datos previos; useRef para evitar m├║ltiples recargas simult├íneas)

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index e73cc99b..f4586b78 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -1,6 +1,6 @@
 'use client';
 
-import { useState, useEffect } from 'react';
+import { useState, useEffect, useRef, useCallback } from 'react';
 import { useRouter } from 'next/navigation';
 import ModelCalculatorNew from '../../../../components/ModelCalculatorNew';
 import { createClient } from "@supabase/supabase-js";
@@ -36,6 +36,7 @@ export default function ModelDashboard() {
   const [productivityLoading, setProductivityLoading] = useState(false);
   const router = useRouter();
   const supabase = require('@/lib/supabase').supabase;
+  const isRefreshingRef = useRef(false);
 
   useEffect(() => {
     const load = async () => {
@@ -110,24 +111,38 @@ export default function ModelDashboard() {
   }, [loading, productivityLoading, user]);
 
   // Refrescar datos al volver a la pesta├▒a/ventana o al volver desde otra ruta
+  // Solo si es modelo, no est├í cargando, y tiene datos previos cargados
   useEffect(() => {
+    // No agregar listeners si no es modelo o si a├║n est├í cargando
+    if (!user || user.role !== 'modelo' || loading || productivityLoading) {
+      return;
+    }
+
     const handleFocus = () => {
-      if (user?.role === 'modelo') {
-        loadProductivityData(user.id);
+      if (!isRefreshingRef.current && user?.role === 'modelo' && !loading && !productivityLoading && productivityData) {
+        isRefreshingRef.current = true;
+        loadProductivityData(user.id).finally(() => {
+          isRefreshingRef.current = false;
+        });
       }
     };
+
     const handleVisibility = () => {
-      if (document.visibilityState === 'visible' && user?.role === 'modelo') {
-        loadProductivityData(user.id);
+      if (document.visibilityState === 'visible' && !isRefreshingRef.current && user?.role === 'modelo' && !loading && !productivityLoading && productivityData) {
+        isRefreshingRef.current = true;
+        loadProductivityData(user.id).finally(() => {
+          isRefreshingRef.current = false;
+        });
       }
     };
+
     window.addEventListener('focus', handleFocus);
     document.addEventListener('visibilitychange', handleVisibility);
     return () => {
       window.removeEventListener('focus', handleFocus);
       document.removeEventListener('visibilitychange', handleVisibility);
     };
-  }, [user]);
+  }, [user, loading, productivityLoading, productivityData]);
 
   const loadProductivityData = async (userId: string) => {
     try {

commit 039611207a69382aa6ef82fcf4ceca62744198eb
Author: camcamberst <camcamberst@gmail.com>
Date:   Thu Oct 30 17:48:56 2025 -0500

    Model Dashboard: ocultar secci├│n 'Resumen de Productividad' para roles distintos a modelo (evita estados err├íticos al volver desde otras funciones)

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 1881f78b..e73cc99b 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -109,13 +109,33 @@ export default function ModelDashboard() {
     }
   }, [loading, productivityLoading, user]);
 
+  // Refrescar datos al volver a la pesta├▒a/ventana o al volver desde otra ruta
+  useEffect(() => {
+    const handleFocus = () => {
+      if (user?.role === 'modelo') {
+        loadProductivityData(user.id);
+      }
+    };
+    const handleVisibility = () => {
+      if (document.visibilityState === 'visible' && user?.role === 'modelo') {
+        loadProductivityData(user.id);
+      }
+    };
+    window.addEventListener('focus', handleFocus);
+    document.addEventListener('visibilitychange', handleVisibility);
+    return () => {
+      window.removeEventListener('focus', handleFocus);
+      document.removeEventListener('visibilitychange', handleVisibility);
+    };
+  }, [user]);
+
   const loadProductivityData = async (userId: string) => {
     try {
       setProductivityLoading(true);
       console.log('­ƒöì [DASHBOARD] Loading productivity data for user:', userId);
 
       // 1) Tasas activas
-      const ratesRes = await fetch('/api/rates-v2?activeOnly=true');
+      const ratesRes = await fetch('/api/rates-v2?activeOnly=true', { cache: 'no-store' });
       const ratesJson = await ratesRes.json();
       const rates = {
         usd_cop: ratesJson?.data?.find((r: any) => r.kind === 'USDÔåÆCOP')?.value || 3900,
@@ -124,7 +144,7 @@ export default function ModelDashboard() {
       };
 
       // 2) Configuraci├│n de plataformas habilitadas
-      const cfgRes = await fetch(`/api/calculator/config-v2?userId=${userId}`);
+      const cfgRes = await fetch(`/api/calculator/config-v2?userId=${userId}`, { cache: 'no-store' });
       const cfg = await cfgRes.json();
       const enabled = (cfg?.config?.platforms || []).filter((p: any) => p.enabled);
 
@@ -137,7 +157,7 @@ export default function ModelDashboard() {
       const yesterdayDate = new Date(new Date(todayDate).getTime() - 24 * 60 * 60 * 1000).toISOString().split('T')[0];
       
       // Obtener valores de hoy
-      const todayValuesRes = await fetch(`/api/calculator/model-values-v2?modelId=${userId}&periodDate=${todayDate}`);
+      const todayValuesRes = await fetch(`/api/calculator/model-values-v2?modelId=${userId}&periodDate=${todayDate}`, { cache: 'no-store' });
       const todayValuesJson = await todayValuesRes.json();
       const todayRows: Array<{ platform_id: string; value: number }> = todayValuesJson?.data || [];
       const todayIdToValue: Record<string, number> = {};
@@ -146,7 +166,7 @@ export default function ModelDashboard() {
       });
 
       // Obtener valores de ayer
-      const yesterdayValuesRes = await fetch(`/api/calculator/model-values-v2?modelId=${userId}&periodDate=${yesterdayDate}`);
+      const yesterdayValuesRes = await fetch(`/api/calculator/model-values-v2?modelId=${userId}&periodDate=${yesterdayDate}`, { cache: 'no-store' });
       const yesterdayValuesJson = await yesterdayValuesRes.json();
       const yesterdayRows: Array<{ platform_id: string; value: number }> = yesterdayValuesJson?.data || [];
       const yesterdayIdToValue: Record<string, number> = {};
@@ -324,7 +344,8 @@ export default function ModelDashboard() {
           </div>
         </div>
 
-        {/* Resumen de productividad y progreso de meta */}
+        {/* Resumen de productividad y progreso de meta (solo para modelos) */}
+        {user.role === 'modelo' && (
         <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
           <div className="flex items-center space-x-2 mb-4">
             <div className="w-6 h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center">
@@ -489,6 +510,7 @@ export default function ModelDashboard() {
             </>
           )}
         </div>
+        )}
 
         {/* AI Dashboard */}
         <div className="mt-6">

commit b114b1c6fb16446e3844083bb94060447eef6377
Author: camcamberst <camcamberst@gmail.com>
Date:   Thu Oct 30 02:06:12 2025 -0500

    fix(model-dashboard+calculator): evitar NaN con fallbacks de currency/percentage por plataforma y comparaciones case-insensitive de ids; USD/COP calculados de forma robusta

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 86964ffd..1881f78b 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -161,9 +161,13 @@ export default function ModelDashboard() {
         const value = todayIdToValue[p.id] || 0;
         if (value <= 0) continue;
 
+        // Normalizaciones y fallbacks
+        const currency = p.currency || 'USD';
+        const finalPct = (p.percentage_override ?? p.group_percentage ?? p.percentage ?? 80) as number;
+
         // USD base por plataforma (igual que Mi Calculadora)
         let usdFromPlatform = 0;
-        if (p.currency === 'EUR') {
+        if (currency === 'EUR') {
           if (p.id === 'big7') {
             usdFromPlatform = (value * rates.eur_usd) * 0.84; // 16% impuesto
           } else if (p.id === 'mondo') {
@@ -171,13 +175,13 @@ export default function ModelDashboard() {
           } else {
             usdFromPlatform = value * rates.eur_usd; // EUR directo
           }
-        } else if (p.currency === 'GBP') {
+        } else if (currency === 'GBP') {
           if (p.id === 'aw') {
             usdFromPlatform = (value * rates.gbp_usd) * 0.677; // 32.3% descuento
           } else {
             usdFromPlatform = value * rates.gbp_usd; // GBP directo
           }
-        } else if (p.currency === 'USD') {
+        } else if (currency === 'USD') {
           if (p.id === 'cmd' || p.id === 'camlust' || p.id === 'skypvt') {
             usdFromPlatform = value * 0.75; // 25% descuento
           } else if (p.id === 'chaturbate' || p.id === 'myfreecams' || p.id === 'stripchat') {
@@ -194,7 +198,7 @@ export default function ModelDashboard() {
         }
         todayUsdBruto += usdFromPlatform;
         // Participaci├│n para modelo (superfoon 100%)
-        const share = (p.id === 'superfoon') ? usdFromPlatform : (usdFromPlatform * (p.percentage / 100));
+        const share = (p.id === 'superfoon') ? usdFromPlatform : (usdFromPlatform * (finalPct / 100));
         todayUsdModelo += share;
       }
 
@@ -205,9 +209,13 @@ export default function ModelDashboard() {
         const value = yesterdayIdToValue[p.id] || 0;
         if (value <= 0) continue;
 
+        // Normalizaciones y fallbacks
+        const currency = p.currency || 'USD';
+        const finalPct = (p.percentage_override ?? p.group_percentage ?? p.percentage ?? 80) as number;
+
         // USD base por plataforma
         let usdFromPlatform = 0;
-        if (p.currency === 'EUR') {
+        if (currency === 'EUR') {
           if (p.id === 'big7') {
             usdFromPlatform = (value * rates.eur_usd) * 0.84; // 16% impuesto
           } else if (p.id === 'mondo') {
@@ -215,13 +223,13 @@ export default function ModelDashboard() {
           } else {
             usdFromPlatform = value * rates.eur_usd; // EUR directo
           }
-        } else if (p.currency === 'GBP') {
+        } else if (currency === 'GBP') {
           if (p.id === 'aw') {
             usdFromPlatform = (value * rates.gbp_usd) * 0.677; // 32.3% descuento
           } else {
             usdFromPlatform = value * rates.gbp_usd; // GBP directo
           }
-        } else if (p.currency === 'USD') {
+        } else if (currency === 'USD') {
           if (p.id === 'cmd' || p.id === 'camlust' || p.id === 'skypvt') {
             usdFromPlatform = value * 0.75; // 25% descuento
           } else if (p.id === 'chaturbate' || p.id === 'myfreecams' || p.id === 'stripchat') {
@@ -237,7 +245,7 @@ export default function ModelDashboard() {
           }
         }
         yesterdayUsdBruto += usdFromPlatform;
-        const share = (p.id === 'superfoon') ? usdFromPlatform : (usdFromPlatform * (p.percentage / 100));
+        const share = (p.id === 'superfoon') ? usdFromPlatform : (usdFromPlatform * (finalPct / 100));
         yesterdayUsdModelo += share;
       }
 

commit 4b1910d750c838495af0a68a21f4595ae2c9849e
Author: camcamberst <camcamberst@gmail.com>
Date:   Wed Oct 29 18:45:29 2025 -0500

    refactor(calculator): helper unificado por plataforma; Superfoon 100% y suma solo habilitadas en USD/COP Modelo y Dashboard de Anticipos

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index e8915698..86964ffd 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -154,14 +154,14 @@ export default function ModelDashboard() {
         yesterdayIdToValue[r.platform_id] = Number(r.value) || 0;
       });
 
-      // 4) C├ílculo por plataforma para hoy (mismo criterio que calculadora)
-      let todayUsdBruto = 0;
-      let todayUsdModelo = 0;
+      // 4) C├ílculo por plataforma para hoy (unificado con Mi Calculadora)
+      let todayUsdBruto = 0; // sin porcentaje, para barra objetivo
+      let todayUsdModelo = 0; // con porcentaje por plataforma (superfoon 100%)
       for (const p of enabled) {
         const value = todayIdToValue[p.id] || 0;
         if (value <= 0) continue;
 
-        // Calcular USD bruto con f├│rmulas espec├¡ficas por plataforma (igual que Mi Calculadora)
+        // USD base por plataforma (igual que Mi Calculadora)
         let usdFromPlatform = 0;
         if (p.currency === 'EUR') {
           if (p.id === 'big7') {
@@ -192,12 +192,12 @@ export default function ModelDashboard() {
             usdFromPlatform = value; // USD directo por defecto
           }
         }
-
         todayUsdBruto += usdFromPlatform;
+        // Participaci├│n para modelo (superfoon 100%)
+        const share = (p.id === 'superfoon') ? usdFromPlatform : (usdFromPlatform * (p.percentage / 100));
+        todayUsdModelo += share;
       }
 
-      todayUsdModelo = todayUsdBruto * (percentage / 100);
-
       // 5) C├ílculo por plataforma para ayer (para calcular ganancias del d├¡a)
       let yesterdayUsdBruto = 0;
       let yesterdayUsdModelo = 0;
@@ -205,7 +205,7 @@ export default function ModelDashboard() {
         const value = yesterdayIdToValue[p.id] || 0;
         if (value <= 0) continue;
 
-        // Calcular USD bruto con f├│rmulas espec├¡ficas por plataforma (igual que Mi Calculadora)
+        // USD base por plataforma
         let usdFromPlatform = 0;
         if (p.currency === 'EUR') {
           if (p.id === 'big7') {
@@ -236,12 +236,11 @@ export default function ModelDashboard() {
             usdFromPlatform = value; // USD directo por defecto
           }
         }
-
         yesterdayUsdBruto += usdFromPlatform;
+        const share = (p.id === 'superfoon') ? usdFromPlatform : (usdFromPlatform * (p.percentage / 100));
+        yesterdayUsdModelo += share;
       }
 
-      yesterdayUsdModelo = yesterdayUsdBruto * (percentage / 100);
-
       // 6) Calcular ganancias del d├¡a (diferencia entre hoy y ayer)
       const todayEarnings = todayUsdModelo - yesterdayUsdModelo;
       const copModelo = todayUsdModelo * rates.usd_cop;

commit ce5473804d38588470d15ae5c07cf5fdbbe5a215
Author: camcamberst <camcamberst@gmail.com>
Date:   Sat Oct 25 01:16:01 2025 -0500

    feat: Unificar estructura de p├íginas de modelo bajo layout de admin
    
    - Mover todas las p├íginas de /model/ a /admin/model/
    - Eliminar layout redundante de modelo
    - Actualizar rutas en men├║ de admin para modelos
    - Crear p├íginas simplificadas que compilan correctamente
    - Mantener funcionalidad completa del sistema
    - Resolver problema de barra de men├║ inconsistente
    - Build exitoso sin errores de compilaci├│n

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 9785e200..e8915698 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -477,7 +477,7 @@ export default function ModelDashboard() {
               </div>
 
               <div className="mt-4 text-xs text-gray-500 dark:text-gray-300">
-                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 underline font-medium">Mi Calculadora</a>.
+                Para actualizar tus valores usa el men├║ <a href="/admin/model/calculator" className="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 underline font-medium">Mi Calculadora</a>.
               </div>
             </>
           )}

commit 894f5f3c6be194a940087af39e977eb5de545bfc
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 24 20:52:11 2025 -0500

    feat: Aplicar est├índar de modo oscuro al dashboard del modelo
    
    - Aplicar modo oscuro completo al dashboard del modelo
    - Corregir pantalla de carga con gradientes oscuros
    - Aplicar efectos de luz y sombras a contenedores
    - Corregir colores de texto para cumplir est├índar (texto blanco)
    - Aplicar modo oscuro al componente AIDashboard
    - Corregir tabs, insights, tips y an├ílisis con modo oscuro
    - Aplicar gradientes oscuros a cards de m├®tricas
    - Corregir footer y elementos de navegaci├│n
    - Mantener consistencia con est├índar establecido

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index c218028a..9785e200 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -274,7 +274,7 @@ export default function ModelDashboard() {
 
   if (loading) {
     return (
-      <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 flex items-center justify-center">
+      <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900 flex items-center justify-center">
         <div className="text-center">
           <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4"></div>
           <p className="text-gray-600 dark:text-gray-300">Cargando dashboard...</p>
@@ -318,14 +318,14 @@ export default function ModelDashboard() {
         </div>
 
         {/* Resumen de productividad y progreso de meta */}
-        <div className="relative bg-white/70 dark:bg-gray-800/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-700/20 p-6">
+        <div className="relative bg-white/70 dark:bg-gray-700/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-600/20 p-6 dark:shadow-lg dark:shadow-blue-900/10 dark:ring-0.5 dark:ring-blue-500/15">
           <div className="flex items-center space-x-2 mb-4">
             <div className="w-6 h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center">
               <svg className="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                 <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
               </svg>
             </div>
-            <h2 className="text-base font-semibold text-gray-900 dark:text-gray-100">Resumen de Productividad</h2>
+            <h2 className="text-base font-semibold text-gray-900 dark:text-white">Resumen de Productividad</h2>
           </div>
           
           {productivityLoading ? (
@@ -476,8 +476,8 @@ export default function ModelDashboard() {
                 })()}
               </div>
 
-              <div className="mt-4 text-xs text-gray-500 dark:text-gray-400">
-                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 underline font-medium">Mi Calculadora</a>.
+              <div className="mt-4 text-xs text-gray-500 dark:text-gray-300">
+                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 dark:text-blue-400 dark:hover:text-blue-300 underline font-medium">Mi Calculadora</a>.
               </div>
             </>
           )}

commit afb0b5b6c2efec6cc68caae81a79c4762c0fea55
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 24 03:56:25 2025 -0500

    feat: Hacer fondos de elementos 10% menos oscuros para look m├â┬ís moderno y sofisticado empresarial - dark:bg-gray-800 a dark:bg-gray-700, etc.

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 86a141f8..c218028a 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -294,7 +294,7 @@ export default function ModelDashboard() {
         <div className="mb-12">
           <div className="relative">
             <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl"></div>
-            <div className="relative bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 dark:border-gray-700/20 shadow-lg dark:shadow-lg dark:shadow-blue-900/15 dark:ring-0.5 dark:ring-blue-400/20">
+            <div className="relative bg-white/80 dark:bg-gray-700/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 dark:border-gray-600/20 shadow-lg dark:shadow-lg dark:shadow-blue-900/15 dark:ring-0.5 dark:ring-blue-400/20">
               <div className="flex items-center space-x-3">
                 <div className="w-10 h-10 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md">
                   <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">

commit 124c674eaa5154bd43447eb526c9cb901b3b0bc9
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 24 03:42:39 2025 -0500

    refactor: Hacer efectos de luz m├â┬ís sutiles para look moderno y sofisticado empresarial - Reducir intensidad de sombras y anillos

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index e28806a3..86a141f8 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -294,7 +294,7 @@ export default function ModelDashboard() {
         <div className="mb-12">
           <div className="relative">
             <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl"></div>
-            <div className="relative bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 dark:border-gray-700/20 shadow-lg dark:shadow-2xl dark:shadow-blue-900/30 dark:ring-1 dark:ring-blue-400/30">
+            <div className="relative bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 dark:border-gray-700/20 shadow-lg dark:shadow-lg dark:shadow-blue-900/15 dark:ring-0.5 dark:ring-blue-400/20">
               <div className="flex items-center space-x-3">
                 <div className="w-10 h-10 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md">
                   <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">

commit d176b41a70b0862e4dca1ba8405fe7e4e4c52748
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 24 03:25:40 2025 -0500

    feat: Aplicar efectos de luz/glow detr├â┬ís de elementos principales - dark:shadow-2xl dark:ring-1 para diferenciaci├â┬│n del fondo oscuro

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 101f5c9e..e28806a3 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -294,7 +294,7 @@ export default function ModelDashboard() {
         <div className="mb-12">
           <div className="relative">
             <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl"></div>
-            <div className="relative bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 dark:border-gray-700/20 shadow-lg">
+            <div className="relative bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 dark:border-gray-700/20 shadow-lg dark:shadow-2xl dark:shadow-blue-900/30 dark:ring-1 dark:ring-blue-400/30">
               <div className="flex items-center space-x-3">
                 <div className="w-10 h-10 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md">
                   <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">

commit 6ab51675d4cdc6155e7e83b1be1ea7cec9fd31cf
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 24 03:21:17 2025 -0500

    feat: Aplicar fondo oscuro a todas las p├â┬íginas de dashboard - dark:from-gray-900 dark:via-gray-800 dark:to-gray-900

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index d08c74e9..101f5c9e 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -288,7 +288,7 @@ export default function ModelDashboard() {
   }
 
   return (
-    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50">
+    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900">
       <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pt-16">
         {/* Header */}
         <div className="mb-12">

commit d1c8115b46798e20945bfb63d286cdb9ba404f62
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 24 01:24:04 2025 -0500

    fix: Implementar dark mode completo en todos los dashboards - Corregir elementos claros que se ve├â┬¡an en modo oscuro en Admin, Super Admin, Model y Sedes dashboards

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 61254f64..d08c74e9 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -277,7 +277,7 @@ export default function ModelDashboard() {
       <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 flex items-center justify-center">
         <div className="text-center">
           <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4"></div>
-          <p className="text-gray-600">Cargando dashboard...</p>
+          <p className="text-gray-600 dark:text-gray-300">Cargando dashboard...</p>
         </div>
       </div>
     );
@@ -306,7 +306,7 @@ export default function ModelDashboard() {
                     Mi Dashboard
                   </h1>
                   {user && (
-                    <p className="mt-1 text-sm text-gray-600">
+                    <p className="mt-1 text-sm text-gray-600 dark:text-gray-300">
                       Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
                       {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
                     </p>
@@ -325,13 +325,13 @@ export default function ModelDashboard() {
                 <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
               </svg>
             </div>
-            <h2 className="text-base font-semibold text-gray-900">Resumen de Productividad</h2>
+            <h2 className="text-base font-semibold text-gray-900 dark:text-gray-100">Resumen de Productividad</h2>
           </div>
           
           {productivityLoading ? (
             <div className="text-center py-4">
               <div className="animate-spin rounded-full h-6 w-6 border-b-2 border-blue-600 mx-auto mb-2"></div>
-              <p className="text-sm text-gray-600">Cargando datos de productividad...</p>
+              <p className="text-sm text-gray-600 dark:text-gray-300">Cargando datos de productividad...</p>
             </div>
           ) : (
             <>
@@ -476,7 +476,7 @@ export default function ModelDashboard() {
                 })()}
               </div>
 
-              <div className="mt-4 text-xs text-gray-500">
+              <div className="mt-4 text-xs text-gray-500 dark:text-gray-400">
                 Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 underline font-medium">Mi Calculadora</a>.
               </div>
             </>

commit ebd34bad8fb066c6e26bd88b234c7d643d36daf1
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 24 01:02:43 2025 -0500

    feat: Implementar dark mode en cards con glassmorphism - Aplicar dark mode a InfoCard y cards de estad├â┬¡sticas manteniendo modo claro intacto

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 99f64111..61254f64 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -318,7 +318,7 @@ export default function ModelDashboard() {
         </div>
 
         {/* Resumen de productividad y progreso de meta */}
-        <div className="relative bg-white/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 p-6">
+        <div className="relative bg-white/70 dark:bg-gray-800/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 dark:border-gray-700/20 p-6">
           <div className="flex items-center space-x-2 mb-4">
             <div className="w-6 h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center">
               <svg className="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">

commit 482e9f9939efcb5172b62a052e0a73a2f3d404c8
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 24 00:50:45 2025 -0500

    fix: Aplicar dark mode a t├â┬¡tulos espec├â┬¡ficos faltantes - Dashboard, Gesti├â┬│n de Usuarios, Portafolio Modelos, Mi Historial de Calculadora

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 19e3c148..99f64111 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -302,7 +302,7 @@ export default function ModelDashboard() {
                   </svg>
                 </div>
                 <div>
-                  <h1 className="text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent">
+                  <h1 className="text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 dark:from-gray-100 dark:to-gray-300 bg-clip-text text-transparent">
                     Mi Dashboard
                   </h1>
                   {user && (

commit 1878c7b5bc5893e9b7fa2d83f22db44684a953d0
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 24 00:27:24 2025 -0500

    feat: Implementar dark mode en fondos de headers - Aplicar dark:bg-gray-800/80 y dark:border-gray-700/20 a todos los headers del proyecto

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index b53c4448..19e3c148 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -294,7 +294,7 @@ export default function ModelDashboard() {
         <div className="mb-12">
           <div className="relative">
             <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl"></div>
-            <div className="relative bg-white/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 shadow-lg">
+            <div className="relative bg-white/80 dark:bg-gray-800/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 dark:border-gray-700/20 shadow-lg">
               <div className="flex items-center space-x-3">
                 <div className="w-10 h-10 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md">
                   <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">

commit 13c406e6920ca31ef6b100c96e073d9370da0ad7
Author: camcamberst <camcamberst@gmail.com>
Date:   Mon Oct 20 01:08:56 2025 -0500

    REVERTIR: Enfoque incorrecto en dashboard
    
    - Revertir cambios al fondo del dashboard
    - El problema es SOLO la barra de men├║ principal
    - No tocar el contenido del dashboard

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 014467c9..b53c4448 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -274,10 +274,10 @@ export default function ModelDashboard() {
 
   if (loading) {
     return (
-      <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900 flex items-center justify-center">
+      <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 flex items-center justify-center">
         <div className="text-center">
           <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4"></div>
-          <p className="text-gray-600 dark:text-gray-300">Cargando dashboard...</p>
+          <p className="text-gray-600">Cargando dashboard...</p>
         </div>
       </div>
     );
@@ -288,7 +288,7 @@ export default function ModelDashboard() {
   }
 
   return (
-    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900">
+    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50">
       <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pt-16">
         {/* Header */}
         <div className="mb-12">

commit 98782fc86ef4212639711a7758ae18918c2ff6c1
Author: camcamberst <camcamberst@gmail.com>
Date:   Mon Oct 20 01:04:43 2025 -0500

    Corregir modo oscuro en dashboard del Panel Modelo
    
    - Dashboard: agregar dark: clases al fondo principal
    - Loading state: agregar dark: clases al fondo y texto
    - Ahora el dashboard del modelo aplica modo oscuro correctamente
    - La barra de men├║ se ve igual en dashboard y funciones
    - Consistencia visual en todo el panel modelo

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index b53c4448..014467c9 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -274,10 +274,10 @@ export default function ModelDashboard() {
 
   if (loading) {
     return (
-      <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 flex items-center justify-center">
+      <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900 flex items-center justify-center">
         <div className="text-center">
           <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4"></div>
-          <p className="text-gray-600">Cargando dashboard...</p>
+          <p className="text-gray-600 dark:text-gray-300">Cargando dashboard...</p>
         </div>
       </div>
     );
@@ -288,7 +288,7 @@ export default function ModelDashboard() {
   }
 
   return (
-    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50">
+    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900">
       <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pt-16">
         {/* Header */}
         <div className="mb-12">

commit 9e84f2e31b88b9a81038a2f35748ac86bb74f7ef
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 23:55:25 2025 -0500

     CORREGIDO: Elementos del modo oscuro en barra de men├║ principal
    
    PROBLEMA IDENTIFICADO:
    -  A├║n hab├¡a elementos del modo oscuro en la barra de men├║ principal
    -  Clases dark: en hover, backgrounds y textos de todos los paneles
    -  Causando inconsistencias visuales en el modo claro
    
    SOLUCI├ôN APLICADA:
    
     PANEL ADMIN (app/admin/layout.tsx):
    -  Eliminado: dark:hover:bg-gray-700/60 de elementos de men├║
    -  Eliminado: dark:bg-gray-700/50 de estados activos
    -  Eliminado: dark:bg-blue-900/30, dark:text-blue-100, dark:border-blue-700/30 de submen├║s
    -  Eliminado: dark:bg-gray-600 de grupos de usuario
    
     PANEL SUPERADMIN (app/superadmin/layout.tsx):
    -  Eliminado: dark:hover:bg-gray-700/60 de elementos de men├║
    -  Eliminado: dark:bg-gray-700/50 de estados activos
    -  Eliminado: dark:bg-blue-900/30, dark:text-blue-100, dark:border-blue-700/30 de submen├║s
    -  Eliminado: dark:bg-gray-600 de grupos de usuario
    
     PANEL ADMIN - P├üGINAS ADICIONALES:
    -  users/create/page.tsx: Eliminado dark: de mensajes de error/├®xito
    -  dashboard/page.tsx: Eliminado dark: de tarjetas de productividad
    -  model/dashboard/page.tsx: Eliminado dark: de barra de progreso y enlaces
    
    RESULTADO:
    -  Barra de men├║ principal completamente en modo claro
    -  Sin elementos del modo oscuro en ning├║n panel
    -  Consistencia visual total en modo claro
    -  Hover effects y estados activos funcionando correctamente

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 843b8729..b53c4448 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -458,7 +458,7 @@ export default function ModelDashboard() {
                             
                             {/* Barra de progreso compacta */}
                             <div className="mt-2">
-                              <div className="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2 overflow-hidden">
+                              <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
                                 <div 
                                   className={`h-full transition-all duration-1000 ease-out`}
                                   style={{ 
@@ -477,7 +477,7 @@ export default function ModelDashboard() {
               </div>
 
               <div className="mt-4 text-xs text-gray-500">
-                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 underline font-medium">Mi Calculadora</a>.
+                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 underline font-medium">Mi Calculadora</a>.
               </div>
             </>
           )}

commit ab3a32acd149e7bd17531e2fde91dd6705e84850
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 23:21:01 2025 -0500

     RESTAURADO: Panel modelo completamente al estado original
    
    SISTEMA DE TEMAS ELIMINADO:
    -  Sistema de temas causando m├ís problemas que soluciones
    -  Necesidad de volver al estado original completamente funcional
    
    CAMBIOS REVERTIDOS:
    
     CSS GLOBAL:
    -  Eliminado color-scheme y reglas de tema
    -  Solo Tailwind base, components, utilities
    -  Sin interferencias de CSS personalizado
    
     LAYOUT MODELO:
    -  Fondo: solo bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50
    -  Header: solo bg-white/80 backdrop-blur-md border border-white/20
    -  Textos: solo colores originales (text-gray-900, text-gray-600)
    -  Botones: solo colores originales sin dark:
    -  Panel usuario: solo bg-white/95 backdrop-blur-md
    
     DASHBOARD MODELO:
    -  Fondo: solo bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50
    -  Cards: solo bg-white/80, bg-white/70
    -  Textos: solo colores originales espec├¡ficos
    -  Sin clases dark: en ning├║n elemento
    
     MI CALCULADORA:
    -  Fondo: solo bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50
    -  Header: solo bg-white/80 backdrop-blur-sm
    -  Secciones: solo bg-white/70 backdrop-blur-sm
    -  Tabla: solo colores originales sin dark:
    -  Totales: solo bg-white rounded-2xl
    
     BOTONES DE MEN├Ü:
    -  CalculatorDropdown: solo colores originales
    -  AnticiposDropdown: solo colores originales
    -  PortfolioDropdown: solo colores originales
    
    RESULTADO:
    -  Panel modelo en estado original completamente funcional
    -  Sin sistema de temas
    -  Sin interferencias ni sobreposiciones
    -  Colores y estilos originales preservados

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index fa6ee6c0..843b8729 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -288,13 +288,13 @@ export default function ModelDashboard() {
   }
 
   return (
-    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900">
+    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50">
       <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pt-16">
         {/* Header */}
         <div className="mb-12">
           <div className="relative">
-            <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl dark:from-blue-400/20 dark:to-indigo-400/20"></div>
-            <div className="relative bg-white/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 shadow-lg dark:bg-gray-800/80 dark:border-gray-700/20 dark:shadow-gray-900/20">
+            <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl"></div>
+            <div className="relative bg-white/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 shadow-lg">
               <div className="flex items-center space-x-3">
                 <div className="w-10 h-10 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md">
                   <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
@@ -302,11 +302,11 @@ export default function ModelDashboard() {
                   </svg>
                 </div>
                 <div>
-                  <h1 className="text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent dark:from-white dark:to-gray-200">
+                  <h1 className="text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent">
                     Mi Dashboard
                   </h1>
                   {user && (
-                    <p className="mt-1 text-sm text-gray-600 dark:text-gray-300">
+                    <p className="mt-1 text-sm text-gray-600">
                       Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
                       {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
                     </p>
@@ -318,20 +318,20 @@ export default function ModelDashboard() {
         </div>
 
         {/* Resumen de productividad y progreso de meta */}
-        <div className="relative bg-white/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 p-6 dark:bg-gray-800/70 dark:border-gray-700/20 dark:shadow-gray-900/20">
+        <div className="relative bg-white/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 p-6">
           <div className="flex items-center space-x-2 mb-4">
             <div className="w-6 h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center">
               <svg className="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                 <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
               </svg>
             </div>
-            <h2 className="text-base font-semibold text-gray-900 dark:text-white">Resumen de Productividad</h2>
+            <h2 className="text-base font-semibold text-gray-900">Resumen de Productividad</h2>
           </div>
           
           {productivityLoading ? (
             <div className="text-center py-4">
               <div className="animate-spin rounded-full h-6 w-6 border-b-2 border-blue-600 mx-auto mb-2"></div>
-              <p className="text-sm text-gray-600 dark:text-gray-300">Cargando datos de productividad...</p>
+              <p className="text-sm text-gray-600">Cargando datos de productividad...</p>
             </div>
           ) : (
             <>

commit d11b547b1095aa141a0bc2fb292b5bae1f167160
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 23:03:40 2025 -0500

     CORREGIDO: Eliminado sistema CSS variables que causaba sobreposici├│n
    
    PROBLEMA IDENTIFICADO:
    -  Sistema de CSS variables en globals.css interfiriendo con clases dark:
    -  Variables CSS aplic├índose globalmente y afectando modo claro
    -  Clases theme-* causando conflictos con Tailwind dark:
    
    SOLUCI├ôN APLICADA:
    
     ELIMINADO COMPLETAMENTE:
    -  Variables CSS :root y .dark
    -  Clases utilitarias theme-bg-gradient, theme-text-primary, etc.
    -  Aplicaci├│n global de variables en body
    -  Sistema de temas basado en CSS variables
    
     REEMPLAZADO CON:
    -  Solo clases Tailwind nativas
    -  Clases dark: espec├¡ficas para modo oscuro
    -  Colores expl├¡citos para modo claro
    -  Sin interferencia entre sistemas
    
     ARCHIVOS CORREGIDOS:
    -  app/globals.css: Eliminadas todas las variables CSS
    -  app/admin/model/dashboard/page.tsx: Reemplazadas clases theme-*
    -  app/model/layout.tsx: Reemplazadas clases theme-*
    
    RESULTADO:
    -  Modo claro: colores originales preservados sin interferencia
    -  Modo oscuro: solo clases dark: aplicadas
    -  Sin sobreposici├│n entre modos
    -  Sistema de temas completamente funcional

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 5ec37008..fa6ee6c0 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -274,10 +274,10 @@ export default function ModelDashboard() {
 
   if (loading) {
     return (
-      <div className="min-h-screen theme-bg-gradient flex items-center justify-center">
+      <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 flex items-center justify-center">
         <div className="text-center">
           <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4"></div>
-          <p className="theme-text-secondary">Cargando dashboard...</p>
+          <p className="text-gray-600">Cargando dashboard...</p>
         </div>
       </div>
     );
@@ -476,7 +476,7 @@ export default function ModelDashboard() {
                 })()}
               </div>
 
-              <div className="mt-4 text-xs theme-text-secondary">
+              <div className="mt-4 text-xs text-gray-500">
                 Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 underline font-medium">Mi Calculadora</a>.
               </div>
             </>

commit 4bb7a6d0dbb57c2a47e29294e5111ecf64d9e34f
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 22:57:22 2025 -0500

     IMPLEMENTADO: Sistema de temas seguro con modo claro preservado
    
    ENFOQUE SEGURO:
    -  Solo clases dark: para modo oscuro
    -  Modo claro mantiene colores originales intactos
    -  Sin alteraci├│n de estilos base
    
    CAMBIOS APLICADOS:
    
     DASHBOARD MODELO:
    -  Fondo: dark:from-gray-900 dark:via-gray-800 dark:to-gray-900
    -  Cards: dark:bg-gray-800/80 dark:border-gray-700/20 dark:shadow-gray-900/20
    -  Textos: dark:from-white dark:to-gray-200, dark:text-gray-300
    -  T├¡tulos: dark:text-white
    
     MI CALCULADORA:
    -  Fondo: dark:from-gray-900 dark:via-gray-800 dark:to-gray-900
    -  Header: dark:bg-gray-800/80 dark:border-gray-700/20
    -  Secciones: dark:bg-gray-800/70 dark:border-gray-700/20
    -  Tabla: dark:border-gray-600/50 dark:bg-gray-800/50
    -  Textos tabla: dark:text-gray-200, dark:text-gray-100, dark:text-gray-300
    -  Totales: dark:bg-gray-800 dark:border-gray-700
    
     BOTONES DE MEN├Ü:
    -  CalculatorDropdown: dark:hover:bg-gray-700/60, dark:text-gray-100/300
    -  AnticiposDropdown: dark:hover:bg-gray-700/60, dark:text-gray-100/300
    -  PortfolioDropdown: dark:hover:bg-gray-700/60, dark:text-gray-100/300
    
    RESULTADO:
    -  Modo claro: colores originales preservados
    -  Modo oscuro: textos blancos/grises claros legibles
    -  Transici├│n suave entre temas
    -  Sin interferencia entre modos

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 6b9c0c3a..5ec37008 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -288,13 +288,13 @@ export default function ModelDashboard() {
   }
 
   return (
-    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50">
+    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50 dark:from-gray-900 dark:via-gray-800 dark:to-gray-900">
       <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pt-16">
         {/* Header */}
         <div className="mb-12">
           <div className="relative">
-            <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl"></div>
-            <div className="relative bg-white/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 shadow-lg">
+            <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl dark:from-blue-400/20 dark:to-indigo-400/20"></div>
+            <div className="relative bg-white/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 shadow-lg dark:bg-gray-800/80 dark:border-gray-700/20 dark:shadow-gray-900/20">
               <div className="flex items-center space-x-3">
                 <div className="w-10 h-10 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md">
                   <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
@@ -302,11 +302,11 @@ export default function ModelDashboard() {
                   </svg>
                 </div>
                 <div>
-                  <h1 className="text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent">
+                  <h1 className="text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent dark:from-white dark:to-gray-200">
                     Mi Dashboard
                   </h1>
                   {user && (
-                    <p className="mt-1 text-sm text-gray-600">
+                    <p className="mt-1 text-sm text-gray-600 dark:text-gray-300">
                       Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
                       {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
                     </p>
@@ -318,20 +318,20 @@ export default function ModelDashboard() {
         </div>
 
         {/* Resumen de productividad y progreso de meta */}
-        <div className="relative bg-white/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 p-6">
+        <div className="relative bg-white/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 p-6 dark:bg-gray-800/70 dark:border-gray-700/20 dark:shadow-gray-900/20">
           <div className="flex items-center space-x-2 mb-4">
             <div className="w-6 h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center">
               <svg className="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                 <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
               </svg>
             </div>
-            <h2 className="text-base font-semibold text-gray-900">Resumen de Productividad</h2>
+            <h2 className="text-base font-semibold text-gray-900 dark:text-white">Resumen de Productividad</h2>
           </div>
           
           {productivityLoading ? (
             <div className="text-center py-4">
               <div className="animate-spin rounded-full h-6 w-6 border-b-2 border-blue-600 mx-auto mb-2"></div>
-              <p className="text-sm text-gray-600">Cargando datos de productividad...</p>
+              <p className="text-sm text-gray-600 dark:text-gray-300">Cargando datos de productividad...</p>
             </div>
           ) : (
             <>

commit 5c0546d38b0be5cd7ec9c3aa87b7cadfec00476f
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 22:45:12 2025 -0500

     REVERTIDO: Sistema de temas en panel modelo
    
    PROBLEMA IDENTIFICADO:
    -  Sistema de temas causando m├ís problemas que soluciones
    -  Alterando colores y contrastes en modo claro
    -  Necesidad de volver al estado original
    
    CAMBIOS REVERTIDOS:
    
     DASHBOARD MODELO:
    -  Fondo: theme-bg-gradient  bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50
    -  Cards: theme-card  bg-white/80, bg-white/70
    -  Textos: theme-text-primary/secondary  colores originales espec├¡ficos
    -  Bordes: theme-border  border border-white/20, border-gray-100
    
     MI CALCULADORA:
    -  Fondo: theme-bg-gradient  bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50
    -  Header: theme-card  bg-white/80
    -  Secciones: theme-card  bg-white/70
    -  Tabla: dark: clases removidas, colores originales restaurados
    -  Textos: theme-text-*  colores espec├¡ficos originales
    
     BOTONES DE MEN├Ü:
    -  CalculatorDropdown: dark: clases removidas
    -  AnticiposDropdown: dark: clases removidas
    -  PortfolioDropdown: dark: clases removidas
    -  Colores: text-gray-900/600 originales restaurados
    
    RESULTADO:
    -  Panel modelo vuelve a su estado original
    -  Sin alteraciones de colores o contrastes
    -  Funcionalidad completa preservada

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 7401708e..6b9c0c3a 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -288,13 +288,13 @@ export default function ModelDashboard() {
   }
 
   return (
-    <div className="min-h-screen theme-bg-gradient">
+    <div className="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50">
       <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pt-16">
         {/* Header */}
         <div className="mb-12">
           <div className="relative">
-            <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl dark:from-blue-400/20 dark:to-indigo-400/20"></div>
-            <div className="relative theme-card backdrop-blur-sm rounded-xl p-6 theme-border theme-shadow">
+            <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl"></div>
+            <div className="relative bg-white/80 backdrop-blur-sm rounded-xl p-6 border border-white/20 shadow-lg">
               <div className="flex items-center space-x-3">
                 <div className="w-10 h-10 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md">
                   <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
@@ -302,11 +302,11 @@ export default function ModelDashboard() {
                   </svg>
                 </div>
                 <div>
-                  <h1 className="text-2xl font-bold theme-text-primary">
+                  <h1 className="text-2xl font-bold bg-gradient-to-r from-gray-900 to-gray-700 bg-clip-text text-transparent">
                     Mi Dashboard
                   </h1>
                   {user && (
-                    <p className="mt-1 text-sm theme-text-secondary">
+                    <p className="mt-1 text-sm text-gray-600">
                       Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
                       {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
                     </p>
@@ -318,20 +318,20 @@ export default function ModelDashboard() {
         </div>
 
         {/* Resumen de productividad y progreso de meta */}
-        <div className="relative theme-card backdrop-blur-sm rounded-xl theme-shadow theme-border p-6">
+        <div className="relative bg-white/70 backdrop-blur-sm rounded-xl shadow-md border border-white/20 p-6">
           <div className="flex items-center space-x-2 mb-4">
             <div className="w-6 h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center">
               <svg className="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                 <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
               </svg>
             </div>
-            <h2 className="text-base font-semibold theme-text-primary">Resumen de Productividad</h2>
+            <h2 className="text-base font-semibold text-gray-900">Resumen de Productividad</h2>
           </div>
           
           {productivityLoading ? (
             <div className="text-center py-4">
               <div className="animate-spin rounded-full h-6 w-6 border-b-2 border-blue-600 mx-auto mb-2"></div>
-              <p className="text-sm theme-text-secondary">Cargando datos de productividad...</p>
+              <p className="text-sm text-gray-600">Cargando datos de productividad...</p>
             </div>
           ) : (
             <>

commit 3f4953e88155174c282595e0e7b47a57c9b14a19
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 22:12:02 2025 -0500

     SOLUCI├ôN DEFINITIVA: Usar InfoCardGrid exacto de Mi Calculadora
    
    PROBLEMA IDENTIFICADO:
    -  Estilos manuales no coincid├¡an exactamente
    -  Diferencias sutiles en gradientes y bordes
    -  Volvimos al error inicial
    
    SOLUCI├ôN APLICADA:
    -  Importar InfoCardGrid de components/ui/InfoCard
    -  Usar exactamente el mismo componente que Mi Calculadora
    -  Mismas props: value, label, color, columns, className
    -  Eliminar estilos manuales que causaban diferencias
    
    COMPONENTE UNIFICADO:
    -  InfoCardGrid con cards array
    -  Colores: blue, green, purple
    -  3 columnas responsivas
    -  Estilos 100% id├®nticos a Mi Calculadora
    
    RESULTADO:
    -  Tarjetas pixel-perfect de Mi Calculadora
    -  Sin diferencias visuales
    -  Componente reutilizable y consistente

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 00c5c6fe..7401708e 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -7,6 +7,7 @@ import { createClient } from "@supabase/supabase-js";
 import { getColombiaDate } from '@/utils/calculator-dates';
 import ProgressMilestone from '@/components/ui/ProgressMilestone';
 import AIDashboard from '@/components/AIDashboard';
+import { InfoCardGrid } from '@/components/ui/InfoCard';
 
 interface User {
   id: string;
@@ -334,32 +335,27 @@ export default function ModelDashboard() {
             </div>
           ) : (
             <>
-              <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
-                <div className="text-center p-3 bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-900/40 dark:to-blue-800/60 rounded-lg border border-blue-200 dark:border-blue-500/50 shadow-sm hover:shadow-lg hover:shadow-blue-200 dark:hover:shadow-blue-500/20 transition-all duration-200 transform hover:scale-105">
-                  <div className="text-lg font-bold text-blue-700 dark:text-blue-200 mb-1">
-                    {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
-                  </div>
-                  <div className="text-xs font-medium text-blue-600 bg-blue-200 dark:bg-blue-700/50 dark:text-blue-100 px-2 py-1 rounded-full inline-block">
-                    Ganancias Hoy
-                  </div>
-                </div>
-                <div className="text-center p-3 bg-gradient-to-br from-green-50 to-green-100 dark:from-green-900/40 dark:to-green-800/60 rounded-lg border border-green-200 dark:border-green-500/50 shadow-sm hover:shadow-lg hover:shadow-green-200 dark:hover:shadow-green-500/20 transition-all duration-200 transform hover:scale-105">
-                  <div className="text-lg font-bold text-green-700 dark:text-green-200 mb-1">
-                    {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
-                  </div>
-                  <div className="text-xs font-medium text-green-600 bg-green-200 dark:bg-green-700/50 dark:text-green-100 px-2 py-1 rounded-full inline-block">
-                    USD Modelo (hoy)
-                  </div>
-                </div>
-                <div className="text-center p-3 bg-gradient-to-br from-purple-50 to-purple-100 dark:from-purple-900/40 dark:to-purple-800/60 rounded-lg border border-purple-200 dark:border-purple-500/50 shadow-sm hover:shadow-lg hover:shadow-purple-200 dark:hover:shadow-purple-500/20 transition-all duration-200 transform hover:scale-105">
-                  <div className="text-lg font-bold text-purple-700 dark:text-purple-200 mb-1">
-                    {productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
-                  </div>
-                  <div className="text-xs font-medium text-purple-600 bg-purple-200 dark:bg-purple-700/50 dark:text-purple-100 px-2 py-1 rounded-full inline-block">
-                    COP Modelo (hoy)
-                  </div>
-                </div>
-              </div>
+              <InfoCardGrid
+                cards={[
+                  {
+                    value: productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö',
+                    label: 'Ganancias Hoy',
+                    color: 'blue'
+                  },
+                  {
+                    value: productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö',
+                    label: 'USD Modelo (hoy)',
+                    color: 'green'
+                  },
+                  {
+                    value: productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö',
+                    label: 'COP Modelo (hoy)',
+                    color: 'purple'
+                  }
+                ]}
+                columns={3}
+                className="mb-4"
+              />
 
               {/* Barra de alcance de meta - Copia exacta de Mi Calculadora */}
               <div className="mt-4">

commit aab3f5cd1f66ab195e05527a54d16bcc8664898c
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 22:08:58 2025 -0500

     CORREGIDO: Capa de luminosidad en tarjetas del dashboard
    
    PROBLEMA IDENTIFICADO:
    -  Tarjetas usaban colores planos (bg-blue-50)
    -  Faltaban gradientes y bordes de Mi Calculadora
    -  Efecto de luminosidad alteraba los colores
    
    SOLUCI├ôN APLICADA:
    -  Gradientes exactos: bg-gradient-to-br from-blue-50 to-blue-100
    -  Bordes: border-blue-200 con sombras hover
    -  Badges redondeados: bg-blue-200 con texto blue-600
    -  Efectos hover: scale-105 y shadow-lg
    -  Colores de texto: blue-700 para valores, blue-600 para badges
    
    ESTILOS APLICADOS:
    -  Azul: Gradiente + borde + badge + hover effects
    -  Verde: Gradiente + borde + badge + hover effects
    -  P├║rpura: Gradiente + borde + badge + hover effects
    
    RESULTADO:
    -  Tarjetas id├®nticas a Mi Calculadora
    -  Sin capa de luminosidad alterando colores
    -  Efectos visuales consistentes

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index b59cff71..00c5c6fe 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -302,7 +302,7 @@ export default function ModelDashboard() {
                 </div>
                 <div>
                   <h1 className="text-2xl font-bold theme-text-primary">
-                    Mi Dashboard - Actualizado
+                    Mi Dashboard
                   </h1>
                   {user && (
                     <p className="mt-1 text-sm theme-text-secondary">
@@ -335,23 +335,29 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
-                <div className="p-3 rounded-lg bg-blue-50 dark:bg-blue-900/20 text-center">
-                  <div className="text-xs theme-text-secondary">Ganancias Hoy</div>
-                  <div className="text-lg font-bold text-blue-600 dark:text-blue-400">
+                <div className="text-center p-3 bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-900/40 dark:to-blue-800/60 rounded-lg border border-blue-200 dark:border-blue-500/50 shadow-sm hover:shadow-lg hover:shadow-blue-200 dark:hover:shadow-blue-500/20 transition-all duration-200 transform hover:scale-105">
+                  <div className="text-lg font-bold text-blue-700 dark:text-blue-200 mb-1">
                     {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
                   </div>
+                  <div className="text-xs font-medium text-blue-600 bg-blue-200 dark:bg-blue-700/50 dark:text-blue-100 px-2 py-1 rounded-full inline-block">
+                    Ganancias Hoy
+                  </div>
                 </div>
-                <div className="p-3 rounded-lg bg-green-50 dark:bg-green-900/20 text-center">
-                  <div className="text-xs theme-text-secondary">USD Modelo (hoy)</div>
-                  <div className="text-lg font-bold text-green-600 dark:text-green-400">
+                <div className="text-center p-3 bg-gradient-to-br from-green-50 to-green-100 dark:from-green-900/40 dark:to-green-800/60 rounded-lg border border-green-200 dark:border-green-500/50 shadow-sm hover:shadow-lg hover:shadow-green-200 dark:hover:shadow-green-500/20 transition-all duration-200 transform hover:scale-105">
+                  <div className="text-lg font-bold text-green-700 dark:text-green-200 mb-1">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
+                  <div className="text-xs font-medium text-green-600 bg-green-200 dark:bg-green-700/50 dark:text-green-100 px-2 py-1 rounded-full inline-block">
+                    USD Modelo (hoy)
+                  </div>
                 </div>
-                <div className="p-3 rounded-lg bg-purple-50 dark:bg-purple-900/20 text-center">
-                  <div className="text-xs theme-text-secondary">COP Modelo (hoy)</div>
-                  <div className="text-lg font-bold text-purple-600 dark:text-purple-400">
+                <div className="text-center p-3 bg-gradient-to-br from-purple-50 to-purple-100 dark:from-purple-900/40 dark:to-purple-800/60 rounded-lg border border-purple-200 dark:border-purple-500/50 shadow-sm hover:shadow-lg hover:shadow-purple-200 dark:hover:shadow-purple-500/20 transition-all duration-200 transform hover:scale-105">
+                  <div className="text-lg font-bold text-purple-700 dark:text-purple-200 mb-1">
                     {productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>
+                  <div className="text-xs font-medium text-purple-600 bg-purple-200 dark:bg-purple-700/50 dark:text-purple-100 px-2 py-1 rounded-full inline-block">
+                    COP Modelo (hoy)
+                  </div>
                 </div>
               </div>
 

commit 41595f88e8424dc59a44d0991366c0e953888340
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 22:02:56 2025 -0500

     Verificar: Archivo correcto del dashboard modelo
    
    PRUEBA DE VERIFICACI├ôN:
    -  Cambio temporal en t├¡tulo para confirmar archivo correcto
    -  T├¡tulo: 'Mi Dashboard - Actualizado'
    -  Verificar que estoy editando el archivo correcto
    
    ARCHIVO CONFIRMADO:
    -  app/admin/model/dashboard/page.tsx
    -  Este es el dashboard del panel modelo
    -  Cambios aplicados correctamente
    
    PR├ôXIMO PASO:
    - Restaurar t├¡tulo original despu├®s de confirmaci├│n
    - Aplicar cambios de colores correctamente

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 5e049f0b..b59cff71 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -302,7 +302,7 @@ export default function ModelDashboard() {
                 </div>
                 <div>
                   <h1 className="text-2xl font-bold theme-text-primary">
-                    Mi Dashboard
+                    Mi Dashboard - Actualizado
                   </h1>
                   {user && (
                     <p className="mt-1 text-sm theme-text-secondary">

commit 1430094a1e23184367d56aeab6a9c1531f3f24ac
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 21:58:38 2025 -0500

     Restaurar: Colores originales antes de cambios modo oscuro
    
    PROBLEMA RESUELTO:
    -  Colores complejos que no coinciden con dise├▒o original
    -  Textos con colores incorrectos
    -  Necesidad de volver a colores simples y efectivos
    
    RESTAURACI├ôN APLICADA:
    -  Fondos simples: bg-blue-50, bg-green-50, bg-purple-50
    -  Textos originales: theme-text-secondary para etiquetas
    -  Valores originales: text-blue-600, text-green-600, text-purple-600
    -  Modo oscuro: dark:bg-[color]-900/20, dark:text-[color]-400
    -  Estructura simple: p-3 rounded-lg text-center
    
    COLORES RESTAURADOS:
    -  Azul: bg-blue-50, text-blue-600, dark:bg-blue-900/20, dark:text-blue-400
    -  Verde: bg-green-50, text-green-600, dark:bg-green-900/20, dark:text-green-400
    -  P├║rpura: bg-purple-50, text-purple-600, dark:bg-purple-900/20, dark:text-purple-400
    -  Etiquetas: theme-text-secondary (consistente con sistema)
    
    RESULTADO:
    - Colores originales restaurados
    - Modo oscuro con contraste adecuado
    - Estructura simple y efectiva
    - Consistencia con sistema de temas

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index d90a3ed0..5e049f0b 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -335,29 +335,23 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
-                <div className="text-center p-3 bg-gradient-to-br from-blue-50/30 to-blue-100/50 dark:from-blue-900/40 dark:to-blue-800/60 rounded-lg border border-blue-200/30 dark:border-blue-500/50 shadow-sm hover:shadow-lg hover:shadow-blue-200/30 dark:hover:shadow-blue-500/20 transition-all duration-200 transform hover:scale-105">
-                  <div className="text-lg font-bold text-blue-600 dark:text-blue-200 mb-1">
+                <div className="p-3 rounded-lg bg-blue-50 dark:bg-blue-900/20 text-center">
+                  <div className="text-xs theme-text-secondary">Ganancias Hoy</div>
+                  <div className="text-lg font-bold text-blue-600 dark:text-blue-400">
                     {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-xs font-medium text-blue-500 bg-blue-100/60 dark:bg-blue-700/50 dark:text-blue-100 px-2 py-1 rounded-full inline-block">
-                    Ganancias Hoy
-                  </div>
                 </div>
-                <div className="text-center p-3 bg-gradient-to-br from-green-50/30 to-green-100/50 dark:from-green-900/40 dark:to-green-800/60 rounded-lg border border-green-200/30 dark:border-green-500/50 shadow-sm hover:shadow-lg hover:shadow-green-200/30 dark:hover:shadow-green-500/20 transition-all duration-200 transform hover:scale-105">
-                  <div className="text-lg font-bold text-green-600 dark:text-green-200 mb-1">
+                <div className="p-3 rounded-lg bg-green-50 dark:bg-green-900/20 text-center">
+                  <div className="text-xs theme-text-secondary">USD Modelo (hoy)</div>
+                  <div className="text-lg font-bold text-green-600 dark:text-green-400">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-xs font-medium text-green-500 bg-green-100/60 dark:bg-green-700/50 dark:text-green-100 px-2 py-1 rounded-full inline-block">
-                    USD Modelo (hoy)
-                  </div>
                 </div>
-                <div className="text-center p-3 bg-gradient-to-br from-purple-50/30 to-purple-100/50 dark:from-purple-900/40 dark:to-purple-800/60 rounded-lg border border-purple-200/30 dark:border-purple-500/50 shadow-sm hover:shadow-lg hover:shadow-purple-200/30 dark:hover:shadow-purple-500/20 transition-all duration-200 transform hover:scale-105">
-                  <div className="text-lg font-bold text-purple-600 dark:text-purple-200 mb-1">
+                <div className="p-3 rounded-lg bg-purple-50 dark:bg-purple-900/20 text-center">
+                  <div className="text-xs theme-text-secondary">COP Modelo (hoy)</div>
+                  <div className="text-lg font-bold text-purple-600 dark:text-purple-400">
                     {productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>
-                  <div className="text-xs font-medium text-purple-500 bg-purple-100/60 dark:bg-purple-700/50 dark:text-purple-100 px-2 py-1 rounded-full inline-block">
-                    COP Modelo (hoy)
-                  </div>
                 </div>
               </div>
 

commit e3e412981c2f299c363a4024c8a76ba96e630842
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 21:54:14 2025 -0500

     Ajustar: Colores suaves y transparentes como imagen 2
    
    PROBLEMA RESUELTO:
    -  Colores muy s├│lidos y opacos (imagen 1)
    -  Necesidad de colores suaves y transparentes (imagen 2)
    -  Aspecto m├ís elegante y sutil
    
    CAMBIOS APLICADOS:
    -  Fondos m├ís transparentes: from-[color]-50/30 to-[color]-100/50
    -  Bordes m├ís sutiles: border-[color]-200/30
    -  Sombras m├ís suaves: hover:shadow-[color]-200/30
    -  Textos m├ís suaves: text-[color]-600 (en lugar de text-[color]-700)
    -  Badges m├ís transparentes: bg-[color]-100/60
    
    ESTILOS SUAVES:
    -  Azul: from-blue-50/30 to-blue-100/50, text-blue-600, bg-blue-100/60
    -  Verde: from-green-50/30 to-green-100/50, text-green-600, bg-green-100/60
    -  P├║rpura: from-purple-50/30 to-purple-100/50, text-purple-600, bg-purple-100/60
    -  Transparencias: /30 y /50 para fondos, /60 para badges
    -  Bordes: /30 para mayor sutileza
    
    RESULTADO:
    - Tarjetas con colores suaves y transparentes
    - Aspecto elegante y sutil como imagen 2
    - Gradientes m├ís delicados
    - Experiencia visual refinada

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 00c5c6fe..d90a3ed0 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -335,27 +335,27 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
-                <div className="text-center p-3 bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-900/40 dark:to-blue-800/60 rounded-lg border border-blue-200 dark:border-blue-500/50 shadow-sm hover:shadow-lg hover:shadow-blue-200 dark:hover:shadow-blue-500/20 transition-all duration-200 transform hover:scale-105">
-                  <div className="text-lg font-bold text-blue-700 dark:text-blue-200 mb-1">
+                <div className="text-center p-3 bg-gradient-to-br from-blue-50/30 to-blue-100/50 dark:from-blue-900/40 dark:to-blue-800/60 rounded-lg border border-blue-200/30 dark:border-blue-500/50 shadow-sm hover:shadow-lg hover:shadow-blue-200/30 dark:hover:shadow-blue-500/20 transition-all duration-200 transform hover:scale-105">
+                  <div className="text-lg font-bold text-blue-600 dark:text-blue-200 mb-1">
                     {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-xs font-medium text-blue-600 bg-blue-200 dark:bg-blue-700/50 dark:text-blue-100 px-2 py-1 rounded-full inline-block">
+                  <div className="text-xs font-medium text-blue-500 bg-blue-100/60 dark:bg-blue-700/50 dark:text-blue-100 px-2 py-1 rounded-full inline-block">
                     Ganancias Hoy
                   </div>
                 </div>
-                <div className="text-center p-3 bg-gradient-to-br from-green-50 to-green-100 dark:from-green-900/40 dark:to-green-800/60 rounded-lg border border-green-200 dark:border-green-500/50 shadow-sm hover:shadow-lg hover:shadow-green-200 dark:hover:shadow-green-500/20 transition-all duration-200 transform hover:scale-105">
-                  <div className="text-lg font-bold text-green-700 dark:text-green-200 mb-1">
+                <div className="text-center p-3 bg-gradient-to-br from-green-50/30 to-green-100/50 dark:from-green-900/40 dark:to-green-800/60 rounded-lg border border-green-200/30 dark:border-green-500/50 shadow-sm hover:shadow-lg hover:shadow-green-200/30 dark:hover:shadow-green-500/20 transition-all duration-200 transform hover:scale-105">
+                  <div className="text-lg font-bold text-green-600 dark:text-green-200 mb-1">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-xs font-medium text-green-600 bg-green-200 dark:bg-green-700/50 dark:text-green-100 px-2 py-1 rounded-full inline-block">
+                  <div className="text-xs font-medium text-green-500 bg-green-100/60 dark:bg-green-700/50 dark:text-green-100 px-2 py-1 rounded-full inline-block">
                     USD Modelo (hoy)
                   </div>
                 </div>
-                <div className="text-center p-3 bg-gradient-to-br from-purple-50 to-purple-100 dark:from-purple-900/40 dark:to-purple-800/60 rounded-lg border border-purple-200 dark:border-purple-500/50 shadow-sm hover:shadow-lg hover:shadow-purple-200 dark:hover:shadow-purple-500/20 transition-all duration-200 transform hover:scale-105">
-                  <div className="text-lg font-bold text-purple-700 dark:text-purple-200 mb-1">
+                <div className="text-center p-3 bg-gradient-to-br from-purple-50/30 to-purple-100/50 dark:from-purple-900/40 dark:to-purple-800/60 rounded-lg border border-purple-200/30 dark:border-purple-500/50 shadow-sm hover:shadow-lg hover:shadow-purple-200/30 dark:hover:shadow-purple-500/20 transition-all duration-200 transform hover:scale-105">
+                  <div className="text-lg font-bold text-purple-600 dark:text-purple-200 mb-1">
                     {productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>
-                  <div className="text-xs font-medium text-purple-600 bg-purple-200 dark:bg-purple-700/50 dark:text-purple-100 px-2 py-1 rounded-full inline-block">
+                  <div className="text-xs font-medium text-purple-500 bg-purple-100/60 dark:bg-purple-700/50 dark:text-purple-100 px-2 py-1 rounded-full inline-block">
                     COP Modelo (hoy)
                   </div>
                 </div>

commit 80a88cef972029a78490577c0346d7a035d76e3f
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 21:50:35 2025 -0500

     Unificar: Estilos exactos de Mi Calculadora en dashboard
    
    PROBLEMA RESUELTO:
    -  Tarjetas del dashboard diferentes a Mi Calculadora
    -  Barra de objetivo b├ísico no id├®ntica
    -  Necesidad de consistencia visual total
    
    CAMBIOS APLICADOS:
    -  Tarjetas con gradientes exactos: bg-gradient-to-br from-[color]-50 to-[color]-100
    -  Bordes y sombras id├®nticos: border-[color]-200, hover:shadow-[color]-200
    -  Etiquetas con badges: text-[color]-600 bg-[color]-200 px-2 py-1 rounded-full
    -  Efectos hover: hover:scale-105, transition-all duration-200
    -  Barra de objetivo b├ísico: Copia exacta de Mi Calculadora
    
    ESTILOS UNIFICADOS:
    -  Azul: from-blue-50 to-blue-100, text-blue-700, bg-blue-200
    -  Verde: from-green-50 to-green-100, text-green-700, bg-green-200
    -  P├║rpura: from-purple-50 to-purple-100, text-purple-700, bg-purple-200
    -  Modo oscuro: dark:from-[color]-900/40 dark:to-[color]-800/60
    -  Barra de progreso: Colores din├ímicos, icono animado, ProgressMilestone
    
    RESULTADO:
    - Dashboard con estilos id├®nticos a Mi Calculadora
    - Consistencia visual total entre componentes
    - Experiencia de usuario unificada
    - Barra de objetivo b├ísico exactamente igual

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 27940f53..00c5c6fe 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -335,36 +335,149 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
-                <div className="p-3 rounded-lg bg-blue-50/50 dark:bg-blue-800/60 text-center border border-blue-200/20 dark:border-blue-500/50 shadow-sm dark:shadow-blue-500/10">
-                  <div className="text-xs text-gray-500 dark:text-blue-100">Ganancias Hoy</div>
-                  <div className="text-lg font-bold text-blue-600 dark:text-blue-200">
+                <div className="text-center p-3 bg-gradient-to-br from-blue-50 to-blue-100 dark:from-blue-900/40 dark:to-blue-800/60 rounded-lg border border-blue-200 dark:border-blue-500/50 shadow-sm hover:shadow-lg hover:shadow-blue-200 dark:hover:shadow-blue-500/20 transition-all duration-200 transform hover:scale-105">
+                  <div className="text-lg font-bold text-blue-700 dark:text-blue-200 mb-1">
                     {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
                   </div>
+                  <div className="text-xs font-medium text-blue-600 bg-blue-200 dark:bg-blue-700/50 dark:text-blue-100 px-2 py-1 rounded-full inline-block">
+                    Ganancias Hoy
+                  </div>
                 </div>
-                <div className="p-3 rounded-lg bg-green-50/50 dark:bg-green-800/60 text-center border border-green-200/20 dark:border-green-500/50 shadow-sm dark:shadow-green-500/10">
-                  <div className="text-xs text-gray-500 dark:text-green-100">USD Modelo (hoy)</div>
-                  <div className="text-lg font-bold text-green-600 dark:text-green-200">
+                <div className="text-center p-3 bg-gradient-to-br from-green-50 to-green-100 dark:from-green-900/40 dark:to-green-800/60 rounded-lg border border-green-200 dark:border-green-500/50 shadow-sm hover:shadow-lg hover:shadow-green-200 dark:hover:shadow-green-500/20 transition-all duration-200 transform hover:scale-105">
+                  <div className="text-lg font-bold text-green-700 dark:text-green-200 mb-1">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
+                  <div className="text-xs font-medium text-green-600 bg-green-200 dark:bg-green-700/50 dark:text-green-100 px-2 py-1 rounded-full inline-block">
+                    USD Modelo (hoy)
+                  </div>
                 </div>
-                <div className="p-3 rounded-lg bg-purple-50/50 dark:bg-purple-800/60 text-center border border-purple-200/20 dark:border-purple-500/50 shadow-sm dark:shadow-purple-500/10">
-                  <div className="text-xs text-gray-500 dark:text-purple-100">COP Modelo (hoy)</div>
-                  <div className="text-lg font-bold text-purple-600 dark:text-purple-200">
+                <div className="text-center p-3 bg-gradient-to-br from-purple-50 to-purple-100 dark:from-purple-900/40 dark:to-purple-800/60 rounded-lg border border-purple-200 dark:border-purple-500/50 shadow-sm hover:shadow-lg hover:shadow-purple-200 dark:hover:shadow-purple-500/20 transition-all duration-200 transform hover:scale-105">
+                  <div className="text-lg font-bold text-purple-700 dark:text-purple-200 mb-1">
                     {productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>
+                  <div className="text-xs font-medium text-purple-600 bg-purple-200 dark:bg-purple-700/50 dark:text-purple-100 px-2 py-1 rounded-full inline-block">
+                    COP Modelo (hoy)
+                  </div>
                 </div>
               </div>
 
-              {/* Barra de alcance de meta (compacta) */}
+              {/* Barra de alcance de meta - Copia exacta de Mi Calculadora */}
               <div className="mt-4">
-                <div className="flex items-center justify-between mb-2">
-                  <span className="text-xs font-medium theme-text-primary">Objetivo B├ísico</span>
-                  <span className="text-xs theme-text-secondary">${productivityData ? productivityData.usdBruto.toFixed(0) : 'ÔÇö'} / ${productivityData ? productivityData.goalUsd.toFixed(0) : 'ÔÇö'} USD</span>
-                </div>
-                <div className="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2 overflow-hidden">
-                  <div className="h-2 bg-gradient-to-r from-green-500 to-emerald-500 transition-all" style={{ width: `${productivityData ? Math.min(100, (productivityData.usdBruto / productivityData.goalUsd) * 100).toFixed(0) : 0}%` }}></div>
-                </div>
-                <div className="text-right text-xs theme-text-secondary mt-1">{productivityData ? Math.min(100, (productivityData.usdBruto / productivityData.goalUsd) * 100).toFixed(0) : 0}%</div>
+                {(() => {
+                  if (!productivityData) return null;
+                  
+                  const totalUsdBruto = productivityData.usdBruto;
+                  const cuotaMinima = productivityData.goalUsd;
+                  const porcentajeAlcanzado = (totalUsdBruto / cuotaMinima) * 100;
+                  const estaPorDebajo = totalUsdBruto < cuotaMinima;
+                  
+                  // Color din├ímico de progreso: 0% rojo (h=0) ÔåÆ 100% verde (h=120)
+                  const progressPct = Math.max(0, Math.min(100, porcentajeAlcanzado));
+                  
+                  // Paleta: Rojo -> P├║rpura -> Esmeralda (sin amarillos)
+                  const RED = { r: 229, g: 57, b: 53 };     // #E53935
+                  const PURPLE = { r: 142, g: 36, b: 170 }; // #8E24AA
+                  const EMERALD = { r: 46, g: 125, b: 50 };  // #2E7D32
+
+                  const mix = (a: any, b: any, t: number) => ({
+                    r: Math.round(a.r + (b.r - a.r) * t),
+                    g: Math.round(a.g + (b.g - a.g) * t),
+                    b: Math.round(a.b + (b.b - a.b) * t)
+                  });
+
+                  const tint = (c: any, t: number) => mix(c, { r: 255, g: 255, b: 255 }, t);
+                  const shade = (c: any, t: number) => mix(c, { r: 0, g: 0, b: 0 }, t);
+                  const rgbToHex = (color: any) => 
+                    `#${color.r.toString(16).padStart(2, '0')}${color.g.toString(16).padStart(2, '0')}${color.b.toString(16).padStart(2, '0')}`;
+
+                  const t = progressPct / 100;
+                  // 0ÔÇô60% rojoÔåÆp├║rpura, 60ÔÇô100% p├║rpuraÔåÆesmeralda
+                  const base = t <= 0.6
+                    ? mix(RED, PURPLE, t / 0.6)
+                    : mix(PURPLE, EMERALD, (t - 0.6) / 0.4);
+
+                  const progressStart = rgbToHex(shade(base, 0.05));
+                  const progressEnd = rgbToHex(shade(base, 0.15));
+                  const cardBgStart = rgbToHex(tint(base, 0.92));
+                  const cardBgEnd = rgbToHex(tint(base, 0.88));
+                  const cardBorder = rgbToHex(tint(base, 0.7));
+                  const iconStart = rgbToHex(shade(base, 0.0));
+                  const iconEnd = rgbToHex(shade(base, 0.2));
+                  const headingColor = rgbToHex(shade(base, 0.55));
+                  const subTextColor = rgbToHex(shade(base, 0.45));
+
+                  return (
+                    <div
+                      className={`relative overflow-hidden rounded-2xl border transition-all duration-300`}
+                      style={{
+                        background: `linear-gradient(135deg, ${cardBgStart}, ${cardBgEnd})`,
+                        borderColor: cardBorder,
+                        boxShadow: `0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)`
+                      }}
+                    >
+                      {/* Efecto de brillo animado */}
+                      <div
+                        className="absolute inset-0 opacity-10 animate-pulse"
+                        style={{ background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})` }}
+                      ></div>
+
+                      <div className="relative p-4">
+                        <div className="flex items-center space-x-3">
+                          {/* Icono animado */}
+                          <div className={`relative flex-shrink-0 ${estaPorDebajo ? 'animate-bounce' : 'animate-pulse'}`}>
+                            <div
+                              className={`w-8 h-8 rounded-full flex items-center justify-center shadow-md`}
+                              style={{
+                                background: `linear-gradient(135deg, ${iconStart}, ${iconEnd})`
+                              }}
+                            >
+                              <span className="text-white text-sm">Ô£ô</span>
+                            </div>
+                          </div>
+                          
+                          {/* Contenido compacto */}
+                          <div className="flex-1">
+                            <div className="flex items-center justify-between">
+                              <div className={`font-bold text-sm`} style={{ color: headingColor }}>
+                                {estaPorDebajo ? 'Objetivo B├ísico en Progreso' : 'Objetivo B├ísico Alcanzado'}
+                              </div>
+                              {(() => {
+                                const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
+                                const remainingPct = Math.max(0, 100 - roundedProgress);
+                                return (
+                                  <div className={`text-xs font-medium`} style={{ color: subTextColor }}>
+                                    {estaPorDebajo
+                                      ? `Faltan $${Math.ceil(cuotaMinima - totalUsdBruto)} USD (${remainingPct}% restante)`
+                                      : `Excelente +${Math.max(0, roundedProgress - 100)}%`}
+                                  </div>
+                                );
+                              })()}
+                            </div>
+                            
+                            {/* Mensaje de progreso por hito */}
+                            {(() => {
+                              const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
+                              return <ProgressMilestone progress={roundedProgress} />;
+                            })()}
+                            
+                            {/* Barra de progreso compacta */}
+                            <div className="mt-2">
+                              <div className="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2 overflow-hidden">
+                                <div 
+                                  className={`h-full transition-all duration-1000 ease-out`}
+                                  style={{ 
+                                    width: `${Math.min(porcentajeAlcanzado, 100)}%`,
+                                    background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})`
+                                  }}
+                                ></div>
+                              </div>
+                            </div>
+                          </div>
+                        </div>
+                      </div>
+                    </div>
+                  );
+                })()}
               </div>
 
               <div className="mt-4 text-xs theme-text-secondary">

commit 435bfd24d315b5edca7714b7014f4b4b81d9254b
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 21:44:16 2025 -0500

     Corregir: Colores originales en modo claro
    
    PROBLEMA RESUELTO:
    -  Modo claro con colores muy s├│lidos y opacos
    -  Tarjetas con apariencia no original
    -  Necesidad de restaurar transparencia
    
    CORRECCIONES APLICADAS:
    -  Fondos m├ís transparentes: bg-[color]-50  bg-[color]-50/50
    -  Bordes m├ís sutiles: border-[color]-200/30  border-[color]-200/20
    -  Textos m├ís suaves: text-gray-600  text-gray-500
    -  Mantener luminosidad en modo oscuro
    
    CAMBIOS ESPEC├ìFICOS:
    -  Modo Claro: bg-blue-50/50, border-blue-200/20, text-gray-500
    -  Modo Oscuro: dark:bg-blue-800/60, dark:border-blue-500/50, dark:text-blue-100
    -  Transparencia restaurada en modo claro
    -  Luminosidad mantenida en modo oscuro
    
    RESULTADO:
    - Modo claro con colores originales suaves y transparentes
    - Modo oscuro con alta luminosidad y contraste
    - Experiencia visual ├│ptima en ambos modos

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 164bd496..27940f53 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -335,20 +335,20 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
-                <div className="p-3 rounded-lg bg-blue-50 dark:bg-blue-800/60 text-center border border-blue-200/30 dark:border-blue-500/50 shadow-sm dark:shadow-blue-500/10">
-                  <div className="text-xs text-gray-600 dark:text-blue-100">Ganancias Hoy</div>
+                <div className="p-3 rounded-lg bg-blue-50/50 dark:bg-blue-800/60 text-center border border-blue-200/20 dark:border-blue-500/50 shadow-sm dark:shadow-blue-500/10">
+                  <div className="text-xs text-gray-500 dark:text-blue-100">Ganancias Hoy</div>
                   <div className="text-lg font-bold text-blue-600 dark:text-blue-200">
                     {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
                   </div>
                 </div>
-                <div className="p-3 rounded-lg bg-green-50 dark:bg-green-800/60 text-center border border-green-200/30 dark:border-green-500/50 shadow-sm dark:shadow-green-500/10">
-                  <div className="text-xs text-gray-600 dark:text-green-100">USD Modelo (hoy)</div>
+                <div className="p-3 rounded-lg bg-green-50/50 dark:bg-green-800/60 text-center border border-green-200/20 dark:border-green-500/50 shadow-sm dark:shadow-green-500/10">
+                  <div className="text-xs text-gray-500 dark:text-green-100">USD Modelo (hoy)</div>
                   <div className="text-lg font-bold text-green-600 dark:text-green-200">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
                 </div>
-                <div className="p-3 rounded-lg bg-purple-50 dark:bg-purple-800/60 text-center border border-purple-200/30 dark:border-purple-500/50 shadow-sm dark:shadow-purple-500/10">
-                  <div className="text-xs text-gray-600 dark:text-purple-100">COP Modelo (hoy)</div>
+                <div className="p-3 rounded-lg bg-purple-50/50 dark:bg-purple-800/60 text-center border border-purple-200/20 dark:border-purple-500/50 shadow-sm dark:shadow-purple-500/10">
+                  <div className="text-xs text-gray-500 dark:text-purple-100">COP Modelo (hoy)</div>
                   <div className="text-lg font-bold text-purple-600 dark:text-purple-200">
                     {productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>

commit 23bb9aa1e07b9eeffe06220b0e3756e5b27f1550
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 21:38:00 2025 -0500

     Aumentar: Luminosidad de tarjetas en modo oscuro
    
    PROBLEMA RESUELTO:
    -  Contraste a├║n bajo en modo oscuro
    -  Necesidad de mayor luminosidad
    -  Mejora de visibilidad general
    
    MEJORAS APLICADAS:
    -  Fondos m├ís brillantes: blue-900/40  blue-800/60
    -  Bordes m├ís luminosos: border-[color]-700/30  border-[color]-500/50
    -  Textos m├ís claros: dark:text-[color]-200  dark:text-[color]-100
    -  Valores m├ís brillantes: dark:text-[color]-300  dark:text-[color]-200
    -  Sombras tem├íticas: dark:shadow-[color]-500/10
    
    CAMBIOS ESPEC├ìFICOS:
    -  Azul: dark:bg-blue-800/60, dark:text-blue-100/200
    -  Verde: dark:bg-green-800/60, dark:text-green-100/200
    -  P├║rpura: dark:bg-purple-800/60, dark:text-purple-100/200
    -  Bordes: dark:border-[color]-500/50 (m├ís luminosos)
    -  Sombras: dark:shadow-[color]-500/10 (efecto sutil)
    
    RESULTADO:
    - Tarjetas con alta luminosidad en modo oscuro
    - Contraste excelente y legibilidad perfecta
    - Efectos visuales sutiles con sombras tem├íticas
    - Experiencia de usuario optimizada

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 9c79657c..164bd496 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -335,21 +335,21 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
-                <div className="p-3 rounded-lg bg-blue-50 dark:bg-blue-900/40 text-center border border-blue-200/30 dark:border-blue-700/30">
-                  <div className="text-xs text-gray-600 dark:text-blue-200">Ganancias Hoy</div>
-                  <div className="text-lg font-bold text-blue-600 dark:text-blue-300">
+                <div className="p-3 rounded-lg bg-blue-50 dark:bg-blue-800/60 text-center border border-blue-200/30 dark:border-blue-500/50 shadow-sm dark:shadow-blue-500/10">
+                  <div className="text-xs text-gray-600 dark:text-blue-100">Ganancias Hoy</div>
+                  <div className="text-lg font-bold text-blue-600 dark:text-blue-200">
                     {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
                   </div>
                 </div>
-                <div className="p-3 rounded-lg bg-green-50 dark:bg-green-900/40 text-center border border-green-200/30 dark:border-green-700/30">
-                  <div className="text-xs text-gray-600 dark:text-green-200">USD Modelo (hoy)</div>
-                  <div className="text-lg font-bold text-green-600 dark:text-green-300">
+                <div className="p-3 rounded-lg bg-green-50 dark:bg-green-800/60 text-center border border-green-200/30 dark:border-green-500/50 shadow-sm dark:shadow-green-500/10">
+                  <div className="text-xs text-gray-600 dark:text-green-100">USD Modelo (hoy)</div>
+                  <div className="text-lg font-bold text-green-600 dark:text-green-200">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
                 </div>
-                <div className="p-3 rounded-lg bg-purple-50 dark:bg-purple-900/40 text-center border border-purple-200/30 dark:border-purple-700/30">
-                  <div className="text-xs text-gray-600 dark:text-purple-200">COP Modelo (hoy)</div>
-                  <div className="text-lg font-bold text-purple-600 dark:text-purple-300">
+                <div className="p-3 rounded-lg bg-purple-50 dark:bg-purple-800/60 text-center border border-purple-200/30 dark:border-purple-500/50 shadow-sm dark:shadow-purple-500/10">
+                  <div className="text-xs text-gray-600 dark:text-purple-100">COP Modelo (hoy)</div>
+                  <div className="text-lg font-bold text-purple-600 dark:text-purple-200">
                     {productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>
                 </div>

commit 7398f15614ec909e8dd10836d7d1e307d1d5259a
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 21:35:05 2025 -0500

     Mejorar: Contrastes de tarjetas en modo oscuro
    
    PROBLEMA RESUELTO:
    -  Tarjetas de 'Resumen de Productividad' con bajo contraste en modo oscuro
    -  Textos poco legibles en fondos transparentes
    -  Falta de definici├│n visual en modo oscuro
    
    MEJORAS APLICADAS:
    -  Fondos m├ís opacos: /20  /40 (mejor visibilidad)
    -  Bordes sutiles: border con colores tem├íticos
    -  Textos de etiquetas: text-gray-600 dark:text-[color]-200
    -  Valores num├®ricos: dark:text-[color]-300 (m├ís brillantes)
    -  Contraste mejorado en todos los elementos
    
    CAMBIOS ESPEC├ìFICOS:
    -  Azul: dark:bg-blue-900/40, dark:text-blue-200/300
    -  Verde: dark:bg-green-900/40, dark:text-green-200/300
    -  P├║rpura: dark:bg-purple-900/40, dark:text-purple-200/300
    -  Bordes: border-[color]-200/30 dark:border-[color]-700/30
    
    RESULTADO:
    - Tarjetas con excelente contraste en modo oscuro
    - Textos completamente legibles
    - Definici├│n visual mejorada
    - Experiencia de usuario optimizada

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 312f82ed..9c79657c 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -335,21 +335,21 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
-                <div className="p-3 rounded-lg bg-blue-50 dark:bg-blue-900/20 text-center">
-                  <div className="text-xs theme-text-secondary">Ganancias Hoy</div>
-                  <div className="text-lg font-bold text-blue-600 dark:text-blue-400">
+                <div className="p-3 rounded-lg bg-blue-50 dark:bg-blue-900/40 text-center border border-blue-200/30 dark:border-blue-700/30">
+                  <div className="text-xs text-gray-600 dark:text-blue-200">Ganancias Hoy</div>
+                  <div className="text-lg font-bold text-blue-600 dark:text-blue-300">
                     {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
                   </div>
                 </div>
-                <div className="p-3 rounded-lg bg-green-50 dark:bg-green-900/20 text-center">
-                  <div className="text-xs theme-text-secondary">USD Modelo (hoy)</div>
-                  <div className="text-lg font-bold text-green-600 dark:text-green-400">
+                <div className="p-3 rounded-lg bg-green-50 dark:bg-green-900/40 text-center border border-green-200/30 dark:border-green-700/30">
+                  <div className="text-xs text-gray-600 dark:text-green-200">USD Modelo (hoy)</div>
+                  <div className="text-lg font-bold text-green-600 dark:text-green-300">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
                 </div>
-                <div className="p-3 rounded-lg bg-purple-50 dark:bg-purple-900/20 text-center">
-                  <div className="text-xs theme-text-secondary">COP Modelo (hoy)</div>
-                  <div className="text-lg font-bold text-purple-600 dark:text-purple-400">
+                <div className="p-3 rounded-lg bg-purple-50 dark:bg-purple-900/40 text-center border border-purple-200/30 dark:border-purple-700/30">
+                  <div className="text-xs text-gray-600 dark:text-purple-200">COP Modelo (hoy)</div>
+                  <div className="text-lg font-bold text-purple-600 dark:text-purple-300">
                     {productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>
                 </div>

commit 8170ec9afab5d8cc7de889fc9dbaed02eaa6bafa
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 21:30:48 2025 -0500

     Unificar: Estilo est├®tico del dashboard modelo con paneles admin
    
    PROBLEMA RESUELTO:
    -  Dashboard del panel modelo con estilo diferente a paneles admin
    -  Falta de consistencia visual entre dashboards
    
    CAMBIOS APLICADOS:
    -  Header con efectos visuales: gradiente de fondo, blur, sombras
    -  Icono profesional con gradiente azul-indigo
    -  Estructura de header id├®ntica a paneles admin
    -  Panel de productividad con icono y estructura compacta
    -  Tarjetas de m├®tricas con estilo compacto (gap-3, p-3)
    -  Barra de progreso simplificada y compacta
    -  Texto de enlace con estilo consistente
    
    ESTILO UNIFICADO:
    -  Header: Efectos de gradiente, blur, backdrop-blur-sm
    -  Iconos: w-6 h-6 con gradiente from-blue-500 to-indigo-600
    -  T├¡tulos: text-base font-semibold theme-text-primary
    -  M├®tricas: Estilo compacto con text-xs y text-lg
    -  Progreso: Barra simple con gradiente verde
    -  Enlaces: text-xs con font-medium
    
    RESULTADO:
    - Dashboard modelo con mismo aspecto profesional que admin/superadmin
    - Consistencia visual completa entre todos los paneles
    - Experiencia de usuario unificada
    - Estilo moderno y elegante

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 48baf22b..312f82ed 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -288,18 +288,44 @@ export default function ModelDashboard() {
 
   return (
     <div className="min-h-screen theme-bg-gradient">
-      <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
-        <h1 className="text-3xl font-semibold theme-text-primary mb-2 animate-fade-in">Mi Dashboard</h1>
-        {user && (
-          <p className="theme-text-secondary mb-6 text-sm animate-fade-in-delay">
-            Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
-            {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
-          </p>
-        )}
+      <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8 pt-16">
+        {/* Header */}
+        <div className="mb-12">
+          <div className="relative">
+            <div className="absolute inset-0 bg-gradient-to-r from-blue-600/10 to-indigo-600/10 rounded-xl blur-xl dark:from-blue-400/20 dark:to-indigo-400/20"></div>
+            <div className="relative theme-card backdrop-blur-sm rounded-xl p-6 theme-border theme-shadow">
+              <div className="flex items-center space-x-3">
+                <div className="w-10 h-10 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-lg flex items-center justify-center shadow-md">
+                  <svg className="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
+                  </svg>
+                </div>
+                <div>
+                  <h1 className="text-2xl font-bold theme-text-primary">
+                    Mi Dashboard
+                  </h1>
+                  {user && (
+                    <p className="mt-1 text-sm theme-text-secondary">
+                      Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
+                      {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
+                    </p>
+                  )}
+                </div>
+              </div>
+            </div>
+          </div>
+        </div>
 
         {/* Resumen de productividad y progreso de meta */}
-        <div className="theme-card rounded-xl theme-shadow theme-border p-6 transition-all duration-300 hover:shadow-md hover:scale-[1.01] animate-slide-up">
-          <h2 className="text-lg font-semibold theme-text-primary mb-4">Resumen de Productividad</h2>
+        <div className="relative theme-card backdrop-blur-sm rounded-xl theme-shadow theme-border p-6">
+          <div className="flex items-center space-x-2 mb-4">
+            <div className="w-6 h-6 bg-gradient-to-br from-blue-500 to-indigo-600 rounded-md flex items-center justify-center">
+              <svg className="w-3 h-3 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
+                <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
+              </svg>
+            </div>
+            <h2 className="text-base font-semibold theme-text-primary">Resumen de Productividad</h2>
+          </div>
           
           {productivityLoading ? (
             <div className="text-center py-4">
@@ -308,150 +334,41 @@ export default function ModelDashboard() {
             </div>
           ) : (
             <>
-              <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
-                <div className="p-4 rounded-lg bg-blue-50 dark:bg-blue-900/20 text-center transition-all duration-300 hover:bg-blue-100 dark:hover:bg-blue-900/30 hover:scale-105 hover:shadow-md animate-fade-in-delay-1">
-                  <div className="text-2xl font-bold text-blue-600 dark:text-blue-400 mb-1 transition-colors duration-300">
+              <div className="grid grid-cols-1 md:grid-cols-3 gap-3 mb-4">
+                <div className="p-3 rounded-lg bg-blue-50 dark:bg-blue-900/20 text-center">
+                  <div className="text-xs theme-text-secondary">Ganancias Hoy</div>
+                  <div className="text-lg font-bold text-blue-600 dark:text-blue-400">
                     {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-sm theme-text-secondary">Ganancias Hoy</div>
                 </div>
-                <div className="p-4 rounded-lg bg-green-50 dark:bg-green-900/20 text-center transition-all duration-300 hover:bg-green-100 dark:hover:bg-green-900/30 hover:scale-105 hover:shadow-md animate-fade-in-delay-2">
-                  <div className="text-2xl font-bold text-green-600 dark:text-green-400 mb-1 transition-colors duration-300">
+                <div className="p-3 rounded-lg bg-green-50 dark:bg-green-900/20 text-center">
+                  <div className="text-xs theme-text-secondary">USD Modelo (hoy)</div>
+                  <div className="text-lg font-bold text-green-600 dark:text-green-400">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-sm theme-text-secondary">USD Modelo (hoy)</div>
                 </div>
-                <div className="p-4 rounded-lg bg-purple-50 dark:bg-purple-900/20 text-center transition-all duration-300 hover:bg-purple-100 dark:hover:bg-purple-900/30 hover:scale-105 hover:shadow-md animate-fade-in-delay-3">
-                  <div className="text-2xl font-bold text-purple-600 dark:text-purple-400 mb-1 transition-colors duration-300">
-                    {productivityData ? `$${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
+                <div className="p-3 rounded-lg bg-purple-50 dark:bg-purple-900/20 text-center">
+                  <div className="text-xs theme-text-secondary">COP Modelo (hoy)</div>
+                  <div className="text-lg font-bold text-purple-600 dark:text-purple-400">
+                    {productivityData ? `${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>
-                  <div className="text-sm theme-text-secondary">COP Modelo (hoy)</div>
                 </div>
               </div>
 
-              {/* Barra de alcance de meta - Copia exacta de Mi Calculadora */}
-              <div className="mt-4 animate-fade-in-delay-4">
-                {(() => {
-                  if (!productivityData) return null;
-                  
-                  const totalUsdBruto = productivityData.usdBruto;
-                  const cuotaMinima = productivityData.goalUsd;
-                  const porcentajeAlcanzado = (totalUsdBruto / cuotaMinima) * 100;
-                  const estaPorDebajo = totalUsdBruto < cuotaMinima;
-                  
-                  // Color din├ímico de progreso: 0% rojo (h=0) ÔåÆ 100% verde (h=120)
-                  const progressPct = Math.max(0, Math.min(100, porcentajeAlcanzado));
-                  
-                  // Paleta: Rojo -> P├║rpura -> Esmeralda (sin amarillos)
-                  const RED = { r: 229, g: 57, b: 53 };     // #E53935
-                  const PURPLE = { r: 142, g: 36, b: 170 }; // #8E24AA
-                  const EMERALD = { r: 46, g: 125, b: 50 };  // #2E7D32
-
-                  const mix = (a: any, b: any, t: number) => ({
-                    r: Math.round(a.r + (b.r - a.r) * t),
-                    g: Math.round(a.g + (b.g - a.g) * t),
-                    b: Math.round(a.b + (b.b - a.b) * t)
-                  });
-
-                  const tint = (c: any, t: number) => mix(c, { r: 255, g: 255, b: 255 }, t);
-                  const shade = (c: any, t: number) => mix(c, { r: 0, g: 0, b: 0 }, t);
-                  const rgbToHex = (color: any) => 
-                    `#${color.r.toString(16).padStart(2, '0')}${color.g.toString(16).padStart(2, '0')}${color.b.toString(16).padStart(2, '0')}`;
-
-                  const t = progressPct / 100;
-                  // 0ÔÇô60% rojoÔåÆp├║rpura, 60ÔÇô100% p├║rpuraÔåÆesmeralda
-                  const base = t <= 0.6
-                    ? mix(RED, PURPLE, t / 0.6)
-                    : mix(PURPLE, EMERALD, (t - 0.6) / 0.4);
-
-                  const progressStart = rgbToHex(shade(base, 0.05));
-                  const progressEnd = rgbToHex(shade(base, 0.15));
-                  const cardBgStart = rgbToHex(tint(base, 0.92));
-                  const cardBgEnd = rgbToHex(tint(base, 0.88));
-                  const cardBorder = rgbToHex(tint(base, 0.7));
-                  const iconStart = rgbToHex(shade(base, 0.0));
-                  const iconEnd = rgbToHex(shade(base, 0.2));
-                  const headingColor = rgbToHex(shade(base, 0.55));
-                  const subTextColor = rgbToHex(shade(base, 0.45));
-
-                  const milestone = progressPct >= 100 ? 100 : progressPct >= 75 ? 75 : progressPct >= 50 ? 50 : progressPct >= 25 ? 25 : 0;
-
-                  return (
-                    <div
-                      className={`relative overflow-hidden rounded-2xl border transition-all duration-300`}
-                      style={{
-                        background: `linear-gradient(135deg, ${cardBgStart}, ${cardBgEnd})`,
-                        borderColor: cardBorder,
-                        boxShadow: `0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)`
-                      }}
-                    >
-                      {/* Efecto de brillo animado */}
-                      <div
-                        className="absolute inset-0 opacity-10 animate-pulse"
-                        style={{ background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})` }}
-                      ></div>
-
-                      <div className="relative p-4">
-                        <div className="flex items-center space-x-3">
-                          {/* Icono animado */}
-                          <div className={`relative flex-shrink-0 ${estaPorDebajo ? 'animate-bounce' : 'animate-pulse'}`}>
-                            <div
-                              className={`w-8 h-8 rounded-full flex items-center justify-center shadow-md`}
-                              style={{
-                                background: `linear-gradient(135deg, ${iconStart}, ${iconEnd})`
-                              }}
-                            >
-                              <span className="text-white text-sm">Ô£ô</span>
-                            </div>
-                          </div>
-                          
-                          {/* Contenido compacto */}
-                          <div className="flex-1">
-                            <div className="flex items-center justify-between">
-                              <div className={`font-bold text-sm`} style={{ color: headingColor }}>
-                                {estaPorDebajo ? 'Objetivo B├ísico en Progreso' : 'Objetivo B├ísico Alcanzado'}
-                              </div>
-                              {(() => {
-                                const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
-                                const remainingPct = Math.max(0, 100 - roundedProgress);
-                                return (
-                                  <div className={`text-xs font-medium`} style={{ color: subTextColor }}>
-                                    {estaPorDebajo
-                                      ? `Faltan $${Math.ceil(cuotaMinima - totalUsdBruto)} USD (${remainingPct}% restante)`
-                                      : `Excelente +${Math.max(0, roundedProgress - 100)}%`}
-                                  </div>
-                                );
-                              })()}
-                            </div>
-                            
-                            {/* Mensaje de progreso por hito */}
-                            {(() => {
-                              const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
-                              return <ProgressMilestone progress={roundedProgress} />;
-                            })()}
-                            
-                            {/* Barra de progreso compacta */}
-              <div className="mt-2">
-                <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
-                  <div 
-                                  className={`h-full transition-all duration-1000 ease-out`}
-                                  style={{ 
-                                    width: `${Math.min(porcentajeAlcanzado, 100)}%`,
-                                    background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})`
-                                  }}
-                  ></div>
+              {/* Barra de alcance de meta (compacta) */}
+              <div className="mt-4">
+                <div className="flex items-center justify-between mb-2">
+                  <span className="text-xs font-medium theme-text-primary">Objetivo B├ísico</span>
+                  <span className="text-xs theme-text-secondary">${productivityData ? productivityData.usdBruto.toFixed(0) : 'ÔÇö'} / ${productivityData ? productivityData.goalUsd.toFixed(0) : 'ÔÇö'} USD</span>
                 </div>
-                            </div>
-                          </div>
-                        </div>
+                <div className="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2 overflow-hidden">
+                  <div className="h-2 bg-gradient-to-r from-green-500 to-emerald-500 transition-all" style={{ width: `${productivityData ? Math.min(100, (productivityData.usdBruto / productivityData.goalUsd) * 100).toFixed(0) : 0}%` }}></div>
                 </div>
-                    </div>
-                  );
-                })()}
+                <div className="text-right text-xs theme-text-secondary mt-1">{productivityData ? Math.min(100, (productivityData.usdBruto / productivityData.goalUsd) * 100).toFixed(0) : 0}%</div>
               </div>
 
-              <div className="mt-4 text-sm theme-text-secondary animate-fade-in-delay-5">
-                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 underline transition-colors duration-300 hover:scale-105 inline-block">Mi Calculadora</a>.
+              <div className="mt-4 text-xs theme-text-secondary">
+                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 underline font-medium">Mi Calculadora</a>.
               </div>
             </>
           )}

commit 9da8a493fde0f6dfa65c5203ab4ac368161e7ed9
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 19 20:46:54 2025 -0500

     Implementar: Temas en dashboards SuperAdmin y Modelo
    
    IMPLEMENTACI├ôN COMPLETADA:
    -  Dashboard SuperAdmin: Temas aplicados
    -  Dashboard Modelo: Temas aplicados
    
    CAMBIOS EN SUPERADMIN:
    -  Fondo: theme-bg-gradient
    -  Header: theme-card, theme-border, theme-shadow
    -  T├¡tulos: theme-text-primary
    -  Subt├¡tulos: theme-text-secondary
    -  Gradientes: dark:from-blue-400/20 dark:to-indigo-400/20
    
    CAMBIOS EN MODELO:
    -  Fondo: theme-bg-gradient
    -  Loading: theme-text-secondary
    -  T├¡tulos: theme-text-primary
    -  Tarjetas: theme-card, theme-shadow, theme-border
    -  Productividad: dark:bg-blue-900/20, dark:text-blue-400
    -  Enlaces: dark:text-blue-400, dark:hover:text-blue-300
    
    RESULTADO:
    - Sistema de temas consistente en todos los dashboards
    - Modo oscuro tenue y elegante
    - Transiciones suaves entre modos
    - Experiencia visual unificada

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index ef84d344..48baf22b 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -273,10 +273,10 @@ export default function ModelDashboard() {
 
   if (loading) {
     return (
-      <div className="min-h-screen bg-white flex items-center justify-center">
+      <div className="min-h-screen theme-bg-gradient flex items-center justify-center">
         <div className="text-center">
           <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4"></div>
-          <p className="text-gray-600">Cargando dashboard...</p>
+          <p className="theme-text-secondary">Cargando dashboard...</p>
         </div>
       </div>
     );
@@ -287,45 +287,45 @@ export default function ModelDashboard() {
   }
 
   return (
-    <div className="min-h-screen bg-white">
+    <div className="min-h-screen theme-bg-gradient">
       <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
-        <h1 className="text-3xl font-semibold text-gray-900 mb-2 animate-fade-in">Mi Dashboard</h1>
+        <h1 className="text-3xl font-semibold theme-text-primary mb-2 animate-fade-in">Mi Dashboard</h1>
         {user && (
-          <p className="text-gray-500 mb-6 text-sm animate-fade-in-delay">
+          <p className="theme-text-secondary mb-6 text-sm animate-fade-in-delay">
             Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
             {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
           </p>
         )}
 
         {/* Resumen de productividad y progreso de meta */}
-        <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 transition-all duration-300 hover:shadow-md hover:scale-[1.01] animate-slide-up">
-          <h2 className="text-lg font-semibold text-gray-900 mb-4">Resumen de Productividad</h2>
+        <div className="theme-card rounded-xl theme-shadow theme-border p-6 transition-all duration-300 hover:shadow-md hover:scale-[1.01] animate-slide-up">
+          <h2 className="text-lg font-semibold theme-text-primary mb-4">Resumen de Productividad</h2>
           
           {productivityLoading ? (
             <div className="text-center py-4">
               <div className="animate-spin rounded-full h-6 w-6 border-b-2 border-blue-600 mx-auto mb-2"></div>
-              <p className="text-sm text-gray-600">Cargando datos de productividad...</p>
+              <p className="text-sm theme-text-secondary">Cargando datos de productividad...</p>
             </div>
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
-                <div className="p-4 rounded-lg bg-blue-50 text-center transition-all duration-300 hover:bg-blue-100 hover:scale-105 hover:shadow-md animate-fade-in-delay-1">
-                  <div className="text-2xl font-bold text-blue-600 mb-1 transition-colors duration-300">
+                <div className="p-4 rounded-lg bg-blue-50 dark:bg-blue-900/20 text-center transition-all duration-300 hover:bg-blue-100 dark:hover:bg-blue-900/30 hover:scale-105 hover:shadow-md animate-fade-in-delay-1">
+                  <div className="text-2xl font-bold text-blue-600 dark:text-blue-400 mb-1 transition-colors duration-300">
                     {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-sm text-gray-600">Ganancias Hoy</div>
+                  <div className="text-sm theme-text-secondary">Ganancias Hoy</div>
                 </div>
-                <div className="p-4 rounded-lg bg-green-50 text-center transition-all duration-300 hover:bg-green-100 hover:scale-105 hover:shadow-md animate-fade-in-delay-2">
-                  <div className="text-2xl font-bold text-green-600 mb-1 transition-colors duration-300">
+                <div className="p-4 rounded-lg bg-green-50 dark:bg-green-900/20 text-center transition-all duration-300 hover:bg-green-100 dark:hover:bg-green-900/30 hover:scale-105 hover:shadow-md animate-fade-in-delay-2">
+                  <div className="text-2xl font-bold text-green-600 dark:text-green-400 mb-1 transition-colors duration-300">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-sm text-gray-600">USD Modelo (hoy)</div>
+                  <div className="text-sm theme-text-secondary">USD Modelo (hoy)</div>
                 </div>
-                <div className="p-4 rounded-lg bg-purple-50 text-center transition-all duration-300 hover:bg-purple-100 hover:scale-105 hover:shadow-md animate-fade-in-delay-3">
-                  <div className="text-2xl font-bold text-purple-600 mb-1 transition-colors duration-300">
+                <div className="p-4 rounded-lg bg-purple-50 dark:bg-purple-900/20 text-center transition-all duration-300 hover:bg-purple-100 dark:hover:bg-purple-900/30 hover:scale-105 hover:shadow-md animate-fade-in-delay-3">
+                  <div className="text-2xl font-bold text-purple-600 dark:text-purple-400 mb-1 transition-colors duration-300">
                     {productivityData ? `$${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>
-                  <div className="text-sm text-gray-600">COP Modelo (hoy)</div>
+                  <div className="text-sm theme-text-secondary">COP Modelo (hoy)</div>
                 </div>
               </div>
 
@@ -450,8 +450,8 @@ export default function ModelDashboard() {
                 })()}
               </div>
 
-              <div className="mt-4 text-sm text-gray-500 animate-fade-in-delay-5">
-                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 underline transition-colors duration-300 hover:scale-105 inline-block">Mi Calculadora</a>.
+              <div className="mt-4 text-sm theme-text-secondary animate-fade-in-delay-5">
+                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 underline transition-colors duration-300 hover:scale-105 inline-block">Mi Calculadora</a>.
               </div>
             </>
           )}

commit d44d7ac3a8879f276fd370ce2025f23172628f44
Author: camcamberst <camcamberst@gmail.com>
Date:   Sat Oct 18 15:33:04 2025 -0500

    Complete chat system cleanup: remove all individual chat functionality
    
    - Remove ConversationTabs and ConversationTab components
    - Delete /api/chat/broadcast and /api/chat/models endpoints
    - Remove entire /api/chat directory structure
    - Clean all layout references to individual chat system
    - Remove ChatWidget references from individual pages
    - Keep only visual ChatWidget component for future implementation
    - System is now completely clean of old chat architecture

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index b7e43745..ef84d344 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -6,7 +6,6 @@ import ModelCalculatorNew from '../../../../components/ModelCalculatorNew';
 import { createClient } from "@supabase/supabase-js";
 import { getColombiaDate } from '@/utils/calculator-dates';
 import ProgressMilestone from '@/components/ui/ProgressMilestone';
-import ChatWidget from '@/components/ChatWidget';
 import AIDashboard from '@/components/AIDashboard';
 
 interface User {
@@ -463,8 +462,6 @@ export default function ModelDashboard() {
           <AIDashboard userId={user.id} userRole={user.role} />
         </div>
 
-        {/* Chat Widget */}
-        <ChatWidget userId={user.id} userRole={user.role} />
       </div>
     </div>
   );

commit d84f6f367663c43c198529305d87286bf04f8409
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 17 14:05:09 2025 -0500

    fix: Usar API endpoint para cargar modelos en ChatWidget
    
    - Crear /api/chat/models para obtener lista de modelos
    - Usar supabaseServer (sin RLS) en lugar de cliente frontend
    - ChatWidget ahora llama a API en lugar de consulta directa
    - Solucionar problema de RLS que imped├¡a leer usuarios
    - Bot├│n de recarga tambi├®n usa API endpoint
    - Logs mejorados para debug

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 646f0b5b..b7e43745 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -432,20 +432,20 @@ export default function ModelDashboard() {
                             })()}
                             
                             {/* Barra de progreso compacta */}
-                            <div className="mt-2">
-                              <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
-                                <div 
+              <div className="mt-2">
+                <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
+                  <div 
                                   className={`h-full transition-all duration-1000 ease-out`}
                                   style={{ 
                                     width: `${Math.min(porcentajeAlcanzado, 100)}%`,
                                     background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})`
                                   }}
-                                ></div>
-                              </div>
+                  ></div>
+                </div>
                             </div>
                           </div>
                         </div>
-                      </div>
+                </div>
                     </div>
                   );
                 })()}

commit 25900ef7c16eceb9c12cd3ff9c466fbd5f9124c9
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 17 00:56:40 2025 -0500

     CORRECCI├ôN CR├ìTICA: Eliminar m├║ltiples instancias GoTrueClient
    
    - Reemplazadas 12 instancias locales de createClient con cliente centralizado
    - Corregido warning: 'Multiple GoTrueClient instances detected'
    - Archivos corregidos:
      * app/model/portafolio/page.tsx
      * app/model/anticipos/page.tsx
      * app/admin/calculator/view-model/page.tsx
      * app/admin/anticipos/history/page.tsx
      * app/admin/anticipos/pending/page.tsx
      * app/admin/anticipos/page.tsx
      * app/admin/rates/page.tsx
      * app/admin/model/dashboard/page.tsx
      * app/model/anticipos/solicitar/page.tsx
      * app/admin/chat-tickets/page.tsx
      * app/model/anticipos/historial/page.tsx
      * app/model/anticipos/solicitudes/page.tsx
    
    - Ahora todos los componentes frontend usan el cliente centralizado de lib/supabase.ts
    - Eliminado comportamiento inesperado por m├║ltiples instancias concurrentes

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 3e3fd722..646f0b5b 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -35,10 +35,7 @@ export default function ModelDashboard() {
   const [productivityData, setProductivityData] = useState<ProductivityData | null>(null);
   const [productivityLoading, setProductivityLoading] = useState(false);
   const router = useRouter();
-  const supabase = createClient(
-    process.env.NEXT_PUBLIC_SUPABASE_URL as string,
-    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY as string
-  );
+  const supabase = require('@/lib/supabase').supabase;
 
   useEffect(() => {
     const load = async () => {

commit 031d3569ba4179b11e472238e3222ab5e3f21e0c
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 12 10:57:17 2025 -0500

    feat: Posicionar scrollbar al inicio en dashboard modelo
    
    - Agregar useEffect para posicionar scrollbar al 0% (inicio)
    - Scroll suave al inicio de la p├ígina
    - Delay de 1 segundo para asegurar renderizado completo
    - Solo se ejecuta cuando los datos est├ín cargados y no hay errores

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 6005d0e7..3e3fd722 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -88,6 +88,30 @@ export default function ModelDashboard() {
     load();
   }, []);
 
+  // ­ƒöº NUEVO: Posicionar scrollbar al inicio al cargar la p├ígina
+  useEffect(() => {
+    const positionScrollbar = () => {
+      // Esperar a que el contenido se haya renderizado completamente
+      setTimeout(() => {
+        // Posici├│n al inicio (0% desde arriba)
+        const targetPosition = 0;
+        
+        // Hacer scroll suave a esa posici├│n
+        window.scrollTo({
+          top: targetPosition,
+          behavior: 'smooth'
+        });
+        
+        console.log('­ƒöì [DASHBOARD] Scrollbar positioned at start:', targetPosition);
+      }, 1000); // Delay para asegurar que el contenido est├® renderizado
+    };
+
+    // Solo posicionar si no hay error y los datos est├ín cargados
+    if (!loading && !productivityLoading && user) {
+      positionScrollbar();
+    }
+  }, [loading, productivityLoading, user]);
+
   const loadProductivityData = async (userId: string) => {
     try {
       setProductivityLoading(true);

commit 6f9772d32d1aee0e124192b9a3ddc93e8d76c344
Author: camcamberst <camcamberst@gmail.com>
Date:   Sat Oct 11 14:55:51 2025 -0500

    feat: Mostrar 'Ganancias Hoy' en lugar de 'USD Bruto' en dashboard modelo
    
    - Calcular ganancias del d├¡a como diferencia entre hoy y ayer
    - Obtener valores de ambos d├¡as usando timezone de Colombia
    - Mantener USD Bruto para barra de Objetivo B├ísico
    - Actualizar interfaz TypeScript con campo todayEarnings
    - Cambiar primera tarjeta para mostrar ganancias diarias
    - M├ís ├║til para modelos ver progreso diario real

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 1803fe78..6005d0e7 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -23,6 +23,7 @@ interface User {
 interface ProductivityData {
   usdBruto: number;
   usdModelo: number;
+  todayEarnings: number; // Nuevo: ganancias del d├¡a
   copModelo: number;
   goalUsd: number;
   goalProgress: number;
@@ -110,21 +111,33 @@ export default function ModelDashboard() {
       const percentage = (enabled[0]?.percentage_override || enabled[0]?.group_percentage || 80) as number;
       const goalUsd = (enabled[0]?.min_quota_override || enabled[0]?.group_min_quota || 470) as number;
 
-      // 3) Valores del d├¡a usando timezone de Colombia
-      const periodDate = getColombiaDate();
-      const valuesRes = await fetch(`/api/calculator/model-values-v2?modelId=${userId}&periodDate=${periodDate}`);
-      const valuesJson = await valuesRes.json();
-      const rows: Array<{ platform_id: string; value: number }> = valuesJson?.data || [];
-      const idToValue: Record<string, number> = {};
-      rows.forEach((r) => {
-        idToValue[r.platform_id] = Number(r.value) || 0;
+      // 3) Valores de hoy y ayer usando timezone de Colombia
+      const todayDate = getColombiaDate();
+      const yesterdayDate = new Date(new Date(todayDate).getTime() - 24 * 60 * 60 * 1000).toISOString().split('T')[0];
+      
+      // Obtener valores de hoy
+      const todayValuesRes = await fetch(`/api/calculator/model-values-v2?modelId=${userId}&periodDate=${todayDate}`);
+      const todayValuesJson = await todayValuesRes.json();
+      const todayRows: Array<{ platform_id: string; value: number }> = todayValuesJson?.data || [];
+      const todayIdToValue: Record<string, number> = {};
+      todayRows.forEach((r) => {
+        todayIdToValue[r.platform_id] = Number(r.value) || 0;
       });
 
-      // 4) C├ílculo por plataforma (mismo criterio que calculadora)
-      let usdBruto = 0;
-      let usdModelo = 0;
+      // Obtener valores de ayer
+      const yesterdayValuesRes = await fetch(`/api/calculator/model-values-v2?modelId=${userId}&periodDate=${yesterdayDate}`);
+      const yesterdayValuesJson = await yesterdayValuesRes.json();
+      const yesterdayRows: Array<{ platform_id: string; value: number }> = yesterdayValuesJson?.data || [];
+      const yesterdayIdToValue: Record<string, number> = {};
+      yesterdayRows.forEach((r) => {
+        yesterdayIdToValue[r.platform_id] = Number(r.value) || 0;
+      });
+
+      // 4) C├ílculo por plataforma para hoy (mismo criterio que calculadora)
+      let todayUsdBruto = 0;
+      let todayUsdModelo = 0;
       for (const p of enabled) {
-        const value = idToValue[p.id] || 0;
+        const value = todayIdToValue[p.id] || 0;
         if (value <= 0) continue;
 
         // Calcular USD bruto con f├│rmulas espec├¡ficas por plataforma (igual que Mi Calculadora)
@@ -159,24 +172,73 @@ export default function ModelDashboard() {
           }
         }
 
-        usdBruto += usdFromPlatform;
+        todayUsdBruto += usdFromPlatform;
       }
 
-      usdModelo = usdBruto * (percentage / 100);
-      const copModelo = usdModelo * rates.usd_cop;
-      const goalProgress = goalUsd > 0 ? Math.min((usdModelo / goalUsd) * 100, 100) : 0;
+      todayUsdModelo = todayUsdBruto * (percentage / 100);
+
+      // 5) C├ílculo por plataforma para ayer (para calcular ganancias del d├¡a)
+      let yesterdayUsdBruto = 0;
+      let yesterdayUsdModelo = 0;
+      for (const p of enabled) {
+        const value = yesterdayIdToValue[p.id] || 0;
+        if (value <= 0) continue;
+
+        // Calcular USD bruto con f├│rmulas espec├¡ficas por plataforma (igual que Mi Calculadora)
+        let usdFromPlatform = 0;
+        if (p.currency === 'EUR') {
+          if (p.id === 'big7') {
+            usdFromPlatform = (value * rates.eur_usd) * 0.84; // 16% impuesto
+          } else if (p.id === 'mondo') {
+            usdFromPlatform = (value * rates.eur_usd) * 0.78; // 22% descuento
+          } else {
+            usdFromPlatform = value * rates.eur_usd; // EUR directo
+          }
+        } else if (p.currency === 'GBP') {
+          if (p.id === 'aw') {
+            usdFromPlatform = (value * rates.gbp_usd) * 0.677; // 32.3% descuento
+          } else {
+            usdFromPlatform = value * rates.gbp_usd; // GBP directo
+          }
+        } else if (p.currency === 'USD') {
+          if (p.id === 'cmd' || p.id === 'camlust' || p.id === 'skypvt') {
+            usdFromPlatform = value * 0.75; // 25% descuento
+          } else if (p.id === 'chaturbate' || p.id === 'myfreecams' || p.id === 'stripchat') {
+            usdFromPlatform = value * 0.05; // 100 tokens = 5 USD
+          } else if (p.id === 'dxlive') {
+            usdFromPlatform = value * 0.60; // 100 pts = 60 USD
+          } else if (p.id === 'secretfriends') {
+            usdFromPlatform = value * 0.5; // 50% descuento
+          } else if (p.id === 'superfoon') {
+            usdFromPlatform = value; // 100% directo
+          } else {
+            usdFromPlatform = value; // USD directo por defecto
+          }
+        }
+
+        yesterdayUsdBruto += usdFromPlatform;
+      }
+
+      yesterdayUsdModelo = yesterdayUsdBruto * (percentage / 100);
+
+      // 6) Calcular ganancias del d├¡a (diferencia entre hoy y ayer)
+      const todayEarnings = todayUsdModelo - yesterdayUsdModelo;
+      const copModelo = todayUsdModelo * rates.usd_cop;
+      const goalProgress = goalUsd > 0 ? Math.min((todayUsdModelo / goalUsd) * 100, 100) : 0;
 
       console.log('­ƒöì [DASHBOARD] Calculated productivity:', {
-        usdBruto,
-        usdModelo,
+        todayUsdBruto,
+        todayUsdModelo,
+        todayEarnings,
         copModelo,
         goalUsd,
         goalProgress
       });
 
       setProductivityData({
-        usdBruto,
-        usdModelo,
+        usdBruto: todayUsdBruto, // Mantener para la barra de objetivo
+        usdModelo: todayUsdModelo,
+        todayEarnings: todayEarnings, // Nuevo: ganancias del d├¡a
         copModelo,
         goalUsd,
         goalProgress
@@ -229,9 +291,9 @@ export default function ModelDashboard() {
               <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
                 <div className="p-4 rounded-lg bg-blue-50 text-center transition-all duration-300 hover:bg-blue-100 hover:scale-105 hover:shadow-md animate-fade-in-delay-1">
                   <div className="text-2xl font-bold text-blue-600 mb-1 transition-colors duration-300">
-                    {productivityData ? `$${productivityData.usdBruto.toFixed(2)}` : 'ÔÇö'}
+                    {productivityData ? `$${productivityData.todayEarnings.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-sm text-gray-600">USD Bruto (hoy)</div>
+                  <div className="text-sm text-gray-600">Ganancias Hoy</div>
                 </div>
                 <div className="p-4 rounded-lg bg-green-50 text-center transition-all duration-300 hover:bg-green-100 hover:scale-105 hover:shadow-md animate-fade-in-delay-2">
                   <div className="text-2xl font-bold text-green-600 mb-1 transition-colors duration-300">

commit 10aa827928823fda08dfee3b61cf5e13ca163a41
Author: camcamberst <camcamberst@gmail.com>
Date:   Sat Oct 11 14:21:38 2025 -0500

    feat: Integrar ventana AI en dashboard principal de modelos
    
    - Crear componente AIDashboard con dise├▒o Apple Style 2
    - Implementar API route /api/ai-dashboard con Google Gemini
    - Generar insights personalizados basados en datos reales
    - Incluir tabs: Insights, Tips, An├ílisis
    - Mostrar recomendaciones espec├¡ficas y accionables
    - Integrar con datos de productividad del d├¡a/semana
    - Aplicar principios est├®ticos establecidos (glassmorphism, gradientes)
    - Mantener funcionalidad aislada sin afectar otras funciones

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 0e02c3ea..1803fe78 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -7,6 +7,7 @@ import { createClient } from "@supabase/supabase-js";
 import { getColombiaDate } from '@/utils/calculator-dates';
 import ProgressMilestone from '@/components/ui/ProgressMilestone';
 import ChatWidget from '@/components/ChatWidget';
+import AIDashboard from '@/components/AIDashboard';
 
 interface User {
   id: string;
@@ -374,6 +375,11 @@ export default function ModelDashboard() {
           )}
         </div>
 
+        {/* AI Dashboard */}
+        <div className="mt-6">
+          <AIDashboard userId={user.id} userRole={user.role} />
+        </div>
+
         {/* Chat Widget */}
         <ChatWidget userId={user.id} userRole={user.role} />
       </div>

commit 50ba318d412a012adeaaa4fcb27196312fa9ba1c
Author: camcamberst <camcamberst@gmail.com>
Date:   Sat Oct 11 00:32:26 2025 -0500

    FEATURE: Implementar sistema completo de ChatBot con IA para modelos de webcam
    
    - ChatBot con Google Gemini (gratuito, 15 requests/minuto)
    - Widget flotante en dashboard y calculadora
    - Sistema de escalaci├│n autom├ítica a admins
    - Panel de administraci├│n para gesti├│n de tickets
    - Base de datos completa con RLS y triggers
    - L├¡mites estrictos: solo portafolio propio, no sugerir plataformas
    - Escalaci├│n por palabras clave, 3 intentos, o solicitud expl├¡cita
    - Notificaciones en tiempo real para admins
    - Historial persistente por 7 d├¡as
    - Interfaz moderna y responsive

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index cd07fb7a..0e02c3ea 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -6,6 +6,7 @@ import ModelCalculatorNew from '../../../../components/ModelCalculatorNew';
 import { createClient } from "@supabase/supabase-js";
 import { getColombiaDate } from '@/utils/calculator-dates';
 import ProgressMilestone from '@/components/ui/ProgressMilestone';
+import ChatWidget from '@/components/ChatWidget';
 
 interface User {
   id: string;
@@ -372,6 +373,9 @@ export default function ModelDashboard() {
             </>
           )}
         </div>
+
+        {/* Chat Widget */}
+        <ChatWidget userId={user.id} userRole={user.role} />
       </div>
     </div>
   );

commit b068b1f96c2583f46142314a49a9cdc552fe1f95
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 10 23:38:08 2025 -0500

    FIX: Corregir error de TypeScript - mover declaraci├│n de rgbToHex antes de su uso

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 5273a2e2..cd07fb7a 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -271,6 +271,8 @@ export default function ModelDashboard() {
 
                   const tint = (c: any, t: number) => mix(c, { r: 255, g: 255, b: 255 }, t);
                   const shade = (c: any, t: number) => mix(c, { r: 0, g: 0, b: 0 }, t);
+                  const rgbToHex = (color: any) => 
+                    `#${color.r.toString(16).padStart(2, '0')}${color.g.toString(16).padStart(2, '0')}${color.b.toString(16).padStart(2, '0')}`;
 
                   const t = progressPct / 100;
                   // 0ÔÇô60% rojoÔåÆp├║rpura, 60ÔÇô100% p├║rpuraÔåÆesmeralda
@@ -288,9 +290,6 @@ export default function ModelDashboard() {
                   const headingColor = rgbToHex(shade(base, 0.55));
                   const subTextColor = rgbToHex(shade(base, 0.45));
 
-                  const rgbToHex = (color: any) => 
-                    `#${color.r.toString(16).padStart(2, '0')}${color.g.toString(16).padStart(2, '0')}${color.b.toString(16).padStart(2, '0')}`;
-
                   const milestone = progressPct >= 100 ? 100 : progressPct >= 75 ? 75 : progressPct >= 50 ? 50 : progressPct >= 25 ? 25 : 0;
 
                   return (

commit 673e34ba507e974da60253e9293426890a2ecf3e
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 10 23:36:47 2025 -0500

    FEATURE: Copiar exactamente la barra Objetivo B├ísico de Mi Calculadora al dashboard - incluye icono animado, colores din├ímicos, ProgressMilestone y efectos visuales completos

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 7e1eddbc..5273a2e2 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -5,6 +5,7 @@ import { useRouter } from 'next/navigation';
 import ModelCalculatorNew from '../../../../components/ModelCalculatorNew';
 import { createClient } from "@supabase/supabase-js";
 import { getColombiaDate } from '@/utils/calculator-dates';
+import ProgressMilestone from '@/components/ui/ProgressMilestone';
 
 interface User {
   id: string;
@@ -244,12 +245,20 @@ export default function ModelDashboard() {
                 </div>
               </div>
 
-              {/* Barra de alcance de meta */}
+              {/* Barra de alcance de meta - Copia exacta de Mi Calculadora */}
               <div className="mt-4 animate-fade-in-delay-4">
                 {(() => {
-                  const progress = productivityData ? Math.min(100, productivityData.goalProgress) : 0;
+                  if (!productivityData) return null;
                   
-                  // Sistema de colores din├ímicos igual que Mi Calculadora
+                  const totalUsdBruto = productivityData.usdBruto;
+                  const cuotaMinima = productivityData.goalUsd;
+                  const porcentajeAlcanzado = (totalUsdBruto / cuotaMinima) * 100;
+                  const estaPorDebajo = totalUsdBruto < cuotaMinima;
+                  
+                  // Color din├ímico de progreso: 0% rojo (h=0) ÔåÆ 100% verde (h=120)
+                  const progressPct = Math.max(0, Math.min(100, porcentajeAlcanzado));
+                  
+                  // Paleta: Rojo -> P├║rpura -> Esmeralda (sin amarillos)
                   const RED = { r: 229, g: 57, b: 53 };     // #E53935
                   const PURPLE = { r: 142, g: 36, b: 170 }; // #8E24AA
                   const EMERALD = { r: 46, g: 125, b: 50 };  // #2E7D32
@@ -260,47 +269,100 @@ export default function ModelDashboard() {
                     b: Math.round(a.b + (b.b - a.b) * t)
                   });
 
-                  const shade = (color: any, amount: number) => ({
-                    r: Math.round(color.r * (1 - amount)),
-                    g: Math.round(color.g * (1 - amount)),
-                    b: Math.round(color.b * (1 - amount))
-                  });
+                  const tint = (c: any, t: number) => mix(c, { r: 255, g: 255, b: 255 }, t);
+                  const shade = (c: any, t: number) => mix(c, { r: 0, g: 0, b: 0 }, t);
 
-                  const rgbToHex = (color: any) => 
-                    `#${color.r.toString(16).padStart(2, '0')}${color.g.toString(16).padStart(2, '0')}${color.b.toString(16).padStart(2, '0')}`;
-
-                  const t = progress / 100;
+                  const t = progressPct / 100;
+                  // 0ÔÇô60% rojoÔåÆp├║rpura, 60ÔÇô100% p├║rpuraÔåÆesmeralda
                   const base = t <= 0.6
                     ? mix(RED, PURPLE, t / 0.6)
                     : mix(PURPLE, EMERALD, (t - 0.6) / 0.4);
 
                   const progressStart = rgbToHex(shade(base, 0.05));
                   const progressEnd = rgbToHex(shade(base, 0.15));
+                  const cardBgStart = rgbToHex(tint(base, 0.92));
+                  const cardBgEnd = rgbToHex(tint(base, 0.88));
+                  const cardBorder = rgbToHex(tint(base, 0.7));
+                  const iconStart = rgbToHex(shade(base, 0.0));
+                  const iconEnd = rgbToHex(shade(base, 0.2));
+                  const headingColor = rgbToHex(shade(base, 0.55));
+                  const subTextColor = rgbToHex(shade(base, 0.45));
+
+                  const rgbToHex = (color: any) => 
+                    `#${color.r.toString(16).padStart(2, '0')}${color.g.toString(16).padStart(2, '0')}${color.b.toString(16).padStart(2, '0')}`;
+
+                  const milestone = progressPct >= 100 ? 100 : progressPct >= 75 ? 75 : progressPct >= 50 ? 50 : progressPct >= 25 ? 25 : 0;
 
                   return (
-                    <>
-                      <div className="flex items-center justify-between mb-2">
-                        <span className="text-sm font-medium text-gray-700">Objetivo B├ísico</span>
-                        <span className="text-sm text-gray-600">
-                          {productivityData ? 
-                            `$${productivityData.usdModelo.toFixed(0)} / $${productivityData.goalUsd} USD` : 
-                            'ÔÇö / ÔÇö USD'
-                          }
-                        </span>
-                      </div>
-                      <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden transition-all duration-500">
-                        <div 
-                          className="h-full transition-all duration-1000 ease-out" 
-                          style={{ 
-                            width: `${progress}%`,
-                            background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})`
-                          }}
-                        ></div>
-                      </div>
-                      <div className="text-right text-xs text-gray-600 mt-1 transition-colors duration-300">
-                        {progress.toFixed(0)}%
+                    <div
+                      className={`relative overflow-hidden rounded-2xl border transition-all duration-300`}
+                      style={{
+                        background: `linear-gradient(135deg, ${cardBgStart}, ${cardBgEnd})`,
+                        borderColor: cardBorder,
+                        boxShadow: `0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06)`
+                      }}
+                    >
+                      {/* Efecto de brillo animado */}
+                      <div
+                        className="absolute inset-0 opacity-10 animate-pulse"
+                        style={{ background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})` }}
+                      ></div>
+
+                      <div className="relative p-4">
+                        <div className="flex items-center space-x-3">
+                          {/* Icono animado */}
+                          <div className={`relative flex-shrink-0 ${estaPorDebajo ? 'animate-bounce' : 'animate-pulse'}`}>
+                            <div
+                              className={`w-8 h-8 rounded-full flex items-center justify-center shadow-md`}
+                              style={{
+                                background: `linear-gradient(135deg, ${iconStart}, ${iconEnd})`
+                              }}
+                            >
+                              <span className="text-white text-sm">Ô£ô</span>
+                            </div>
+                          </div>
+                          
+                          {/* Contenido compacto */}
+                          <div className="flex-1">
+                            <div className="flex items-center justify-between">
+                              <div className={`font-bold text-sm`} style={{ color: headingColor }}>
+                                {estaPorDebajo ? 'Objetivo B├ísico en Progreso' : 'Objetivo B├ísico Alcanzado'}
+                              </div>
+                              {(() => {
+                                const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
+                                const remainingPct = Math.max(0, 100 - roundedProgress);
+                                return (
+                                  <div className={`text-xs font-medium`} style={{ color: subTextColor }}>
+                                    {estaPorDebajo
+                                      ? `Faltan $${Math.ceil(cuotaMinima - totalUsdBruto)} USD (${remainingPct}% restante)`
+                                      : `Excelente +${Math.max(0, roundedProgress - 100)}%`}
+                                  </div>
+                                );
+                              })()}
+                            </div>
+                            
+                            {/* Mensaje de progreso por hito */}
+                            {(() => {
+                              const roundedProgress = Math.max(0, Math.min(100, Math.round(porcentajeAlcanzado)));
+                              return <ProgressMilestone progress={roundedProgress} />;
+                            })()}
+                            
+                            {/* Barra de progreso compacta */}
+                            <div className="mt-2">
+                              <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
+                                <div 
+                                  className={`h-full transition-all duration-1000 ease-out`}
+                                  style={{ 
+                                    width: `${Math.min(porcentajeAlcanzado, 100)}%`,
+                                    background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})`
+                                  }}
+                                ></div>
+                              </div>
+                            </div>
+                          </div>
+                        </div>
                       </div>
-                    </>
+                    </div>
                   );
                 })()}
               </div>

commit 1f624728872af5eaf7b22ee1156cd1664f5eb129
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 10 23:32:17 2025 -0500

    STYLE: Aplicar sistema de colores din├ímicos de Mi Calculadora a la barra de Objetivo B├ísico del dashboard - gradientes que cambian de rojo a p├║rpura a esmeralda seg├║n progreso

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 22816d94..7e1eddbc 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -246,24 +246,63 @@ export default function ModelDashboard() {
 
               {/* Barra de alcance de meta */}
               <div className="mt-4 animate-fade-in-delay-4">
-                <div className="flex items-center justify-between mb-2">
-                  <span className="text-sm font-medium text-gray-700">Objetivo B├ísico</span>
-                  <span className="text-sm text-gray-600">
-                    {productivityData ? 
-                      `$${productivityData.usdModelo.toFixed(0)} / $${productivityData.goalUsd} USD` : 
-                      'ÔÇö / ÔÇö USD'
-                    }
-                  </span>
-                </div>
-                <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden transition-all duration-500">
-                  <div 
-                    className="h-2 bg-gradient-to-r from-green-500 to-emerald-500 transition-all duration-1000 ease-out" 
-                    style={{ width: `${productivityData ? Math.min(100, productivityData.goalProgress).toFixed(0) : 0}%` }}
-                  ></div>
-                </div>
-                <div className="text-right text-xs text-gray-600 mt-1 transition-colors duration-300">
-                  {productivityData ? `${Math.min(100, productivityData.goalProgress).toFixed(0)}%` : '0%'}
-                </div>
+                {(() => {
+                  const progress = productivityData ? Math.min(100, productivityData.goalProgress) : 0;
+                  
+                  // Sistema de colores din├ímicos igual que Mi Calculadora
+                  const RED = { r: 229, g: 57, b: 53 };     // #E53935
+                  const PURPLE = { r: 142, g: 36, b: 170 }; // #8E24AA
+                  const EMERALD = { r: 46, g: 125, b: 50 };  // #2E7D32
+
+                  const mix = (a: any, b: any, t: number) => ({
+                    r: Math.round(a.r + (b.r - a.r) * t),
+                    g: Math.round(a.g + (b.g - a.g) * t),
+                    b: Math.round(a.b + (b.b - a.b) * t)
+                  });
+
+                  const shade = (color: any, amount: number) => ({
+                    r: Math.round(color.r * (1 - amount)),
+                    g: Math.round(color.g * (1 - amount)),
+                    b: Math.round(color.b * (1 - amount))
+                  });
+
+                  const rgbToHex = (color: any) => 
+                    `#${color.r.toString(16).padStart(2, '0')}${color.g.toString(16).padStart(2, '0')}${color.b.toString(16).padStart(2, '0')}`;
+
+                  const t = progress / 100;
+                  const base = t <= 0.6
+                    ? mix(RED, PURPLE, t / 0.6)
+                    : mix(PURPLE, EMERALD, (t - 0.6) / 0.4);
+
+                  const progressStart = rgbToHex(shade(base, 0.05));
+                  const progressEnd = rgbToHex(shade(base, 0.15));
+
+                  return (
+                    <>
+                      <div className="flex items-center justify-between mb-2">
+                        <span className="text-sm font-medium text-gray-700">Objetivo B├ísico</span>
+                        <span className="text-sm text-gray-600">
+                          {productivityData ? 
+                            `$${productivityData.usdModelo.toFixed(0)} / $${productivityData.goalUsd} USD` : 
+                            'ÔÇö / ÔÇö USD'
+                          }
+                        </span>
+                      </div>
+                      <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden transition-all duration-500">
+                        <div 
+                          className="h-full transition-all duration-1000 ease-out" 
+                          style={{ 
+                            width: `${progress}%`,
+                            background: `linear-gradient(90deg, ${progressStart}, ${progressEnd})`
+                          }}
+                        ></div>
+                      </div>
+                      <div className="text-right text-xs text-gray-600 mt-1 transition-colors duration-300">
+                        {progress.toFixed(0)}%
+                      </div>
+                    </>
+                  );
+                })()}
               </div>
 
               <div className="mt-4 text-sm text-gray-500 animate-fade-in-delay-5">

commit c5637dc4c1e041f6799350b4d066e56aaff4b4f0
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 10 23:26:14 2025 -0500

    ANIMATIONS: Agregar efectos visuales de transici├│n al dashboard de modelo - animaciones de entrada, hover effects, transiciones suaves en tarjetas y barra de progreso

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index f892709f..22816d94 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -204,16 +204,16 @@ export default function ModelDashboard() {
   return (
     <div className="min-h-screen bg-white">
       <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
-        <h1 className="text-3xl font-semibold text-gray-900 mb-2">Mi Dashboard</h1>
+        <h1 className="text-3xl font-semibold text-gray-900 mb-2 animate-fade-in">Mi Dashboard</h1>
         {user && (
-          <p className="text-gray-500 mb-6 text-sm">
+          <p className="text-gray-500 mb-6 text-sm animate-fade-in-delay">
             Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
             {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
           </p>
         )}
 
         {/* Resumen de productividad y progreso de meta */}
-        <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
+        <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6 transition-all duration-300 hover:shadow-md hover:scale-[1.01] animate-slide-up">
           <h2 className="text-lg font-semibold text-gray-900 mb-4">Resumen de Productividad</h2>
           
           {productivityLoading ? (
@@ -224,20 +224,20 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
-                <div className="p-4 rounded-lg bg-blue-50 text-center">
-                  <div className="text-2xl font-bold text-blue-600 mb-1">
+                <div className="p-4 rounded-lg bg-blue-50 text-center transition-all duration-300 hover:bg-blue-100 hover:scale-105 hover:shadow-md animate-fade-in-delay-1">
+                  <div className="text-2xl font-bold text-blue-600 mb-1 transition-colors duration-300">
                     {productivityData ? `$${productivityData.usdBruto.toFixed(2)}` : 'ÔÇö'}
                   </div>
                   <div className="text-sm text-gray-600">USD Bruto (hoy)</div>
                 </div>
-                <div className="p-4 rounded-lg bg-green-50 text-center">
-                  <div className="text-2xl font-bold text-green-600 mb-1">
+                <div className="p-4 rounded-lg bg-green-50 text-center transition-all duration-300 hover:bg-green-100 hover:scale-105 hover:shadow-md animate-fade-in-delay-2">
+                  <div className="text-2xl font-bold text-green-600 mb-1 transition-colors duration-300">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
                   <div className="text-sm text-gray-600">USD Modelo (hoy)</div>
                 </div>
-                <div className="p-4 rounded-lg bg-purple-50 text-center">
-                  <div className="text-2xl font-bold text-purple-600 mb-1">
+                <div className="p-4 rounded-lg bg-purple-50 text-center transition-all duration-300 hover:bg-purple-100 hover:scale-105 hover:shadow-md animate-fade-in-delay-3">
+                  <div className="text-2xl font-bold text-purple-600 mb-1 transition-colors duration-300">
                     {productivityData ? `$${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>
                   <div className="text-sm text-gray-600">COP Modelo (hoy)</div>
@@ -245,7 +245,7 @@ export default function ModelDashboard() {
               </div>
 
               {/* Barra de alcance de meta */}
-              <div className="mt-4">
+              <div className="mt-4 animate-fade-in-delay-4">
                 <div className="flex items-center justify-between mb-2">
                   <span className="text-sm font-medium text-gray-700">Objetivo B├ísico</span>
                   <span className="text-sm text-gray-600">
@@ -255,19 +255,19 @@ export default function ModelDashboard() {
                     }
                   </span>
                 </div>
-                <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
+                <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden transition-all duration-500">
                   <div 
-                    className="h-2 bg-gradient-to-r from-green-500 to-emerald-500 transition-all" 
+                    className="h-2 bg-gradient-to-r from-green-500 to-emerald-500 transition-all duration-1000 ease-out" 
                     style={{ width: `${productivityData ? Math.min(100, productivityData.goalProgress).toFixed(0) : 0}%` }}
                   ></div>
                 </div>
-                <div className="text-right text-xs text-gray-600 mt-1">
+                <div className="text-right text-xs text-gray-600 mt-1 transition-colors duration-300">
                   {productivityData ? `${Math.min(100, productivityData.goalProgress).toFixed(0)}%` : '0%'}
                 </div>
               </div>
 
-              <div className="mt-4 text-sm text-gray-500">
-                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 underline">Mi Calculadora</a>.
+              <div className="mt-4 text-sm text-gray-500 animate-fade-in-delay-5">
+                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 underline transition-colors duration-300 hover:scale-105 inline-block">Mi Calculadora</a>.
               </div>
             </>
           )}

commit 9a66d33c3447fd3e882517d707ee39c9b408af1c
Author: camcamberst <camcamberst@gmail.com>
Date:   Fri Oct 10 23:22:44 2025 -0500

    STYLE: Aplicar estilo limpio y simple de dashboards admin al dashboard de modelo - fondo blanco, tarjetas simples, sin efectos excesivos

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 596af528..f892709f 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -202,8 +202,8 @@ export default function ModelDashboard() {
   }
 
   return (
-    <div className="min-h-screen bg-gray-50">
-      <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
+    <div className="min-h-screen bg-white">
+      <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
         <h1 className="text-3xl font-semibold text-gray-900 mb-2">Mi Dashboard</h1>
         {user && (
           <p className="text-gray-500 mb-6 text-sm">
@@ -212,14 +212,6 @@ export default function ModelDashboard() {
           </p>
         )}
 
-        {/* Panel de perfil */}
-        <div className="bg-white rounded-xl shadow-sm border border-gray-200 mb-6 p-6">
-          <h2 className="text-lg font-semibold text-gray-900 mb-1">Mi perfil</h2>
-          <p className="text-sm text-gray-500">Revisa tu informaci├│n</p>
-          <div className="mt-4 text-sm text-gray-700">Email: {user.email}</div>
-          <div className="text-sm text-gray-700">Grupo: {user.groups[0] || 'ÔÇö'}</div>
-        </div>
-
         {/* Resumen de productividad y progreso de meta */}
         <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
           <h2 className="text-lg font-semibold text-gray-900 mb-4">Resumen de Productividad</h2>
@@ -232,51 +224,45 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
-                <div className="p-4 rounded-md bg-blue-50 text-center">
+                <div className="p-4 rounded-lg bg-blue-50 text-center">
                   <div className="text-2xl font-bold text-blue-600 mb-1">
                     {productivityData ? `$${productivityData.usdBruto.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-xs font-medium text-blue-600 bg-blue-200 px-2 py-1 rounded-full inline-block">
-                    USD Bruto (hoy)
-                  </div>
+                  <div className="text-sm text-gray-600">USD Bruto (hoy)</div>
                 </div>
-                <div className="p-4 rounded-md bg-green-50 text-center">
+                <div className="p-4 rounded-lg bg-green-50 text-center">
                   <div className="text-2xl font-bold text-green-600 mb-1">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
-                  <div className="text-xs font-medium text-green-600 bg-green-200 px-2 py-1 rounded-full inline-block">
-                    USD Modelo (hoy)
-                  </div>
+                  <div className="text-sm text-gray-600">USD Modelo (hoy)</div>
                 </div>
-                <div className="p-4 rounded-md bg-purple-50 text-center">
+                <div className="p-4 rounded-lg bg-purple-50 text-center">
                   <div className="text-2xl font-bold text-purple-600 mb-1">
                     {productivityData ? `$${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
                   </div>
-                  <div className="text-xs font-medium text-purple-600 bg-purple-200 px-2 py-1 rounded-full inline-block">
-                    COP Modelo (hoy)
-                  </div>
+                  <div className="text-sm text-gray-600">COP Modelo (hoy)</div>
                 </div>
               </div>
 
-              {/* Barra de alcance de meta (compacta) */}
-              <div className="mt-2">
-                <div className="flex items-center justify-between mb-1">
-                  <span className="text-sm font-medium text-gray-700">Alcance de objetivo B├ísico</span>
+              {/* Barra de alcance de meta */}
+              <div className="mt-4">
+                <div className="flex items-center justify-between mb-2">
+                  <span className="text-sm font-medium text-gray-700">Objetivo B├ísico</span>
                   <span className="text-sm text-gray-600">
                     {productivityData ? 
-                      `$${productivityData.usdModelo.toFixed(2)} / $${productivityData.goalUsd} USD` : 
+                      `$${productivityData.usdModelo.toFixed(0)} / $${productivityData.goalUsd} USD` : 
                       'ÔÇö / ÔÇö USD'
                     }
                   </span>
                 </div>
                 <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
                   <div 
-                    className="h-2 bg-gradient-to-r from-green-500 to-emerald-500 transition-all duration-300" 
-                    style={{ width: `${productivityData?.goalProgress || 0}%` }}
+                    className="h-2 bg-gradient-to-r from-green-500 to-emerald-500 transition-all" 
+                    style={{ width: `${productivityData ? Math.min(100, productivityData.goalProgress).toFixed(0) : 0}%` }}
                   ></div>
                 </div>
                 <div className="text-right text-xs text-gray-600 mt-1">
-                  {productivityData ? `${productivityData.goalProgress.toFixed(1)}%` : '0%'}
+                  {productivityData ? `${Math.min(100, productivityData.goalProgress).toFixed(0)}%` : '0%'}
                 </div>
               </div>
 

commit cd4c284532b6f8638d8e2484a10731199a402b30
Author: camcamberst <camcamberst@gmail.com>
Date:   Mon Oct 6 11:02:20 2025 -0500

    style(dashboard): badges inferiores y COP Modelo sin decimales

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index c8159e0b..596af528 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -233,21 +233,27 @@ export default function ModelDashboard() {
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
                 <div className="p-4 rounded-md bg-blue-50 text-center">
-                  <div className="text-xs text-gray-600">USD Bruto (hoy)</div>
-                  <div className="text-2xl font-bold text-blue-600">
+                  <div className="text-2xl font-bold text-blue-600 mb-1">
                     {productivityData ? `$${productivityData.usdBruto.toFixed(2)}` : 'ÔÇö'}
                   </div>
+                  <div className="text-xs font-medium text-blue-600 bg-blue-200 px-2 py-1 rounded-full inline-block">
+                    USD Bruto (hoy)
+                  </div>
                 </div>
                 <div className="p-4 rounded-md bg-green-50 text-center">
-                  <div className="text-xs text-gray-600">USD Modelo (hoy)</div>
-                  <div className="text-2xl font-bold text-green-600">
+                  <div className="text-2xl font-bold text-green-600 mb-1">
                     {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
+                  <div className="text-xs font-medium text-green-600 bg-green-200 px-2 py-1 rounded-full inline-block">
+                    USD Modelo (hoy)
+                  </div>
                 </div>
                 <div className="p-4 rounded-md bg-purple-50 text-center">
-                  <div className="text-xs text-gray-600">COP Modelo (hoy)</div>
-                  <div className="text-2xl font-bold text-purple-600">
-                    {productivityData ? `$${productivityData.copModelo.toFixed(2)}` : 'ÔÇö'}
+                  <div className="text-2xl font-bold text-purple-600 mb-1">
+                    {productivityData ? `$${Math.round(productivityData.copModelo).toLocaleString('es-CO')}` : 'ÔÇö'}
+                  </div>
+                  <div className="text-xs font-medium text-purple-600 bg-purple-200 px-2 py-1 rounded-full inline-block">
+                    COP Modelo (hoy)
                   </div>
                 </div>
               </div>

commit 890c3483d057dc4c30e477c14aaa4ce2c29b999a
Author: camcamberst <camcamberst@gmail.com>
Date:   Mon Oct 6 10:57:33 2025 -0500

    revert(dashboard): restaurar tarjetas originales del Resumen de Productividad

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index a6f7abf5..c8159e0b 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -3,7 +3,6 @@
 import { useState, useEffect } from 'react';
 import { useRouter } from 'next/navigation';
 import ModelCalculatorNew from '../../../../components/ModelCalculatorNew';
-import { InfoCardGrid } from '@/components/ui/InfoCard';
 import { createClient } from "@supabase/supabase-js";
 import { getColombiaDate } from '@/utils/calculator-dates';
 
@@ -233,30 +232,24 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
-                {/* Usar InfoCardGrid con tama├▒o compacto para mantener alto/ancho actuales */}
-                <InfoCardGrid
-                  cards={[
-                    {
-                      value: productivityData ? `$${productivityData.usdBruto.toFixed(2)}` : 'ÔÇö',
-                      label: 'USD Bruto (hoy)',
-                      color: 'blue',
-                      size: 'sm'
-                    },
-                    {
-                      value: productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö',
-                      label: 'USD Modelo (hoy)',
-                      color: 'green',
-                      size: 'sm'
-                    },
-                    {
-                      value: productivityData ? `$${productivityData.copModelo.toFixed(2)}` : 'ÔÇö',
-                      label: 'COP Modelo (hoy)',
-                      color: 'purple',
-                      size: 'sm'
-                    }
-                  ]}
-                  columns={3}
-                />
+                <div className="p-4 rounded-md bg-blue-50 text-center">
+                  <div className="text-xs text-gray-600">USD Bruto (hoy)</div>
+                  <div className="text-2xl font-bold text-blue-600">
+                    {productivityData ? `$${productivityData.usdBruto.toFixed(2)}` : 'ÔÇö'}
+                  </div>
+                </div>
+                <div className="p-4 rounded-md bg-green-50 text-center">
+                  <div className="text-xs text-gray-600">USD Modelo (hoy)</div>
+                  <div className="text-2xl font-bold text-green-600">
+                    {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
+                  </div>
+                </div>
+                <div className="p-4 rounded-md bg-purple-50 text-center">
+                  <div className="text-xs text-gray-600">COP Modelo (hoy)</div>
+                  <div className="text-2xl font-bold text-purple-600">
+                    {productivityData ? `$${productivityData.copModelo.toFixed(2)}` : 'ÔÇö'}
+                  </div>
+                </div>
               </div>
 
               {/* Barra de alcance de meta (compacta) */}

commit 1e4dba2bd5c00ed57ab6f9e9111bd44f795b652a
Author: camcamberst <camcamberst@gmail.com>
Date:   Mon Oct 6 10:55:17 2025 -0500

    fix(dashboard): importar InfoCardGrid para resolver error de build

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 75f78225..a6f7abf5 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -3,6 +3,7 @@
 import { useState, useEffect } from 'react';
 import { useRouter } from 'next/navigation';
 import ModelCalculatorNew from '../../../../components/ModelCalculatorNew';
+import { InfoCardGrid } from '@/components/ui/InfoCard';
 import { createClient } from "@supabase/supabase-js";
 import { getColombiaDate } from '@/utils/calculator-dates';
 

commit 5ffaf2c8781b0a7718f215b5a1492541ad6b225c
Author: camcamberst <camcamberst@gmail.com>
Date:   Mon Oct 6 10:52:49 2025 -0500

    style(dashboard): unificar Resumen de Productividad con InfoCardGrid (tama├â┬▒o compacto)

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index c8159e0b..75f78225 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -232,24 +232,30 @@ export default function ModelDashboard() {
           ) : (
             <>
               <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
-                <div className="p-4 rounded-md bg-blue-50 text-center">
-                  <div className="text-xs text-gray-600">USD Bruto (hoy)</div>
-                  <div className="text-2xl font-bold text-blue-600">
-                    {productivityData ? `$${productivityData.usdBruto.toFixed(2)}` : 'ÔÇö'}
-                  </div>
-                </div>
-                <div className="p-4 rounded-md bg-green-50 text-center">
-                  <div className="text-xs text-gray-600">USD Modelo (hoy)</div>
-                  <div className="text-2xl font-bold text-green-600">
-                    {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
-                  </div>
-                </div>
-                <div className="p-4 rounded-md bg-purple-50 text-center">
-                  <div className="text-xs text-gray-600">COP Modelo (hoy)</div>
-                  <div className="text-2xl font-bold text-purple-600">
-                    {productivityData ? `$${productivityData.copModelo.toFixed(2)}` : 'ÔÇö'}
-                  </div>
-                </div>
+                {/* Usar InfoCardGrid con tama├▒o compacto para mantener alto/ancho actuales */}
+                <InfoCardGrid
+                  cards={[
+                    {
+                      value: productivityData ? `$${productivityData.usdBruto.toFixed(2)}` : 'ÔÇö',
+                      label: 'USD Bruto (hoy)',
+                      color: 'blue',
+                      size: 'sm'
+                    },
+                    {
+                      value: productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö',
+                      label: 'USD Modelo (hoy)',
+                      color: 'green',
+                      size: 'sm'
+                    },
+                    {
+                      value: productivityData ? `$${productivityData.copModelo.toFixed(2)}` : 'ÔÇö',
+                      label: 'COP Modelo (hoy)',
+                      color: 'purple',
+                      size: 'sm'
+                    }
+                  ]}
+                  columns={3}
+                />
               </div>
 
               {/* Barra de alcance de meta (compacta) */}

commit 5a502ba9f65a1e5ece84440c03c2f0d639e5c529
Author: camcamberst <camcamberst@gmail.com>
Date:   Sun Oct 5 23:00:41 2025 -0500

     SOLUCI├ôN INTEGRAL: Corregir inconsistencias sist├®micas en panel modelo
    
    PROBLEMAS RESUELTOS:
     Dashboard Modelo - timezone UTC  getColombiaDate()
     Dashboard Admin - timezone UTC  getColombiaDate()
     Dashboard Modelo - l├│gica gen├®rica  f├│rmulas espec├¡ficas por plataforma
     Historial Anticipos - correcci├│n manual fr├ígil  funci├│n robusta con timezone Colombia
     API Unificada - nueva /api/calculator/unified-productivity para consistencia
    
    F├ôRMULAS UNIFICADAS IMPLEMENTADAS:
    - Chaturbate/MFC/Stripchat: value * 0.05 (tokens a USD)
    - DXLive: value * 0.60 (puntos a USD)
    - AW: (value * gbp_usd) * 0.677 (GBP + descuento 32.3%)
    - Big7: (value * eur_usd) * 0.84 (EUR + impuesto 16%)
    - Mondo: (value * eur_usd) * 0.78 (EUR + descuento 22%)
    - CMD/Camlust/Skypvt: value * 0.75 (descuento 25%)
    - SecretFriends: value * 0.5 (descuento 50%)
    - Superfoon: value directo
    
    IMPACTO:
    - Elimina valores en ceros al navegar entre Dashboard  Mi Calculadora
    - Resumen de Productividad ahora muestra datos correctos
    - Filtros de per├¡odos en historial anticipos m├ís robustos
    - Consistencia total entre todas las vistas del panel modelo
    - Fin del ciclo problem├ítico de datos desapareciendo

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 3609e4b2..c8159e0b 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -4,6 +4,7 @@ import { useState, useEffect } from 'react';
 import { useRouter } from 'next/navigation';
 import ModelCalculatorNew from '../../../../components/ModelCalculatorNew';
 import { createClient } from "@supabase/supabase-js";
+import { getColombiaDate } from '@/utils/calculator-dates';
 
 interface User {
   id: string;
@@ -106,9 +107,9 @@ export default function ModelDashboard() {
       const percentage = (enabled[0]?.percentage_override || enabled[0]?.group_percentage || 80) as number;
       const goalUsd = (enabled[0]?.min_quota_override || enabled[0]?.group_min_quota || 470) as number;
 
-      // 3) Valores del d├¡a (v2)
-      const today = new Date().toISOString().split('T')[0];
-      const valuesRes = await fetch(`/api/calculator/model-values-v2?modelId=${userId}&periodDate=${today}`);
+      // 3) Valores del d├¡a usando timezone de Colombia
+      const periodDate = getColombiaDate();
+      const valuesRes = await fetch(`/api/calculator/model-values-v2?modelId=${userId}&periodDate=${periodDate}`);
       const valuesJson = await valuesRes.json();
       const rows: Array<{ platform_id: string; value: number }> = valuesJson?.data || [];
       const idToValue: Record<string, number> = {};
@@ -123,28 +124,35 @@ export default function ModelDashboard() {
         const value = idToValue[p.id] || 0;
         if (value <= 0) continue;
 
+        // Calcular USD bruto con f├│rmulas espec├¡ficas por plataforma (igual que Mi Calculadora)
         let usdFromPlatform = 0;
-        if (p.currency === 'USD') {
-          if (p.direct_payout) {
-            usdFromPlatform = value;
-          } else if (p.discount_factor) {
-            usdFromPlatform = value * p.discount_factor;
+        if (p.currency === 'EUR') {
+          if (p.id === 'big7') {
+            usdFromPlatform = (value * rates.eur_usd) * 0.84; // 16% impuesto
+          } else if (p.id === 'mondo') {
+            usdFromPlatform = (value * rates.eur_usd) * 0.78; // 22% descuento
           } else {
-            usdFromPlatform = value;
+            usdFromPlatform = value * rates.eur_usd; // EUR directo
           }
-        } else if (p.currency === 'EUR') {
-          const eurToUsd = value * rates.eur_usd;
-          if (p.tax_rate) {
-            usdFromPlatform = eurToUsd * (1 - p.tax_rate);
+        } else if (p.currency === 'GBP') {
+          if (p.id === 'aw') {
+            usdFromPlatform = (value * rates.gbp_usd) * 0.677; // 32.3% descuento
           } else {
-            usdFromPlatform = eurToUsd;
+            usdFromPlatform = value * rates.gbp_usd; // GBP directo
           }
-        } else if (p.currency === 'GBP') {
-          const gbpToUsd = value * rates.gbp_usd;
-          if (p.discount_factor) {
-            usdFromPlatform = gbpToUsd * p.discount_factor;
+        } else if (p.currency === 'USD') {
+          if (p.id === 'cmd' || p.id === 'camlust' || p.id === 'skypvt') {
+            usdFromPlatform = value * 0.75; // 25% descuento
+          } else if (p.id === 'chaturbate' || p.id === 'myfreecams' || p.id === 'stripchat') {
+            usdFromPlatform = value * 0.05; // 100 tokens = 5 USD
+          } else if (p.id === 'dxlive') {
+            usdFromPlatform = value * 0.60; // 100 pts = 60 USD
+          } else if (p.id === 'secretfriends') {
+            usdFromPlatform = value * 0.5; // 50% descuento
+          } else if (p.id === 'superfoon') {
+            usdFromPlatform = value; // 100% directo
           } else {
-            usdFromPlatform = gbpToUsd;
+            usdFromPlatform = value; // USD directo por defecto
           }
         }
 

commit 8d183a602959c65330285b383271ab89ad81f3c4
Author: camcamberst <camcamberst@gmail.com>
Date:   Sat Oct 4 13:07:35 2025 -0500

     FIX: Corregir decimales en Resumen de Productividad del dashboard
    
    - Cambiar formateo de COP Modelo de toLocaleString() a toFixed(2)
    - Ahora muestra consistentemente 2 decimales como USD Bruto y USD Modelo
    - Mejora la consistencia visual en el dashboard del modelo
    
    Antes: ,581.088 (3 decimales)
    Despu├®s: ,581.09 (2 decimales)

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 3922b0b1..3609e4b2 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -239,7 +239,7 @@ export default function ModelDashboard() {
                 <div className="p-4 rounded-md bg-purple-50 text-center">
                   <div className="text-xs text-gray-600">COP Modelo (hoy)</div>
                   <div className="text-2xl font-bold text-purple-600">
-                    {productivityData ? `$${productivityData.copModelo.toLocaleString()}` : 'ÔÇö'}
+                    {productivityData ? `$${productivityData.copModelo.toFixed(2)}` : 'ÔÇö'}
                   </div>
                 </div>
               </div>

commit 963c4f5464160b90f26781fc9ef8f14c23bbc2ff
Author: Tu Nombre <tu-email@ejemplo.com>
Date:   Thu Oct 2 00:29:37 2025 -0500

    FIX: Corregir capitalizaci├â┬│n de 'b├â┬ísico' a 'B├â┬ísico'
    
    - Cambiar 'Objetivo b├â┬ísico' a 'Objetivo B├â┬ísico' en calculadora
    - Cambiar 'Alcance de objetivo b├â┬ísico' a 'Alcance de objetivo B├â┬ísico' en dashboard admin
    - Cambiar 'Objetivo b├â┬ísico' a 'Objetivo B├â┬ísico' en dashboard principal
    - Mantener consistencia en capitalizaci├â┬│n en toda la aplicaci├â┬│n

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 69697099..3922b0b1 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -247,7 +247,7 @@ export default function ModelDashboard() {
               {/* Barra de alcance de meta (compacta) */}
               <div className="mt-2">
                 <div className="flex items-center justify-between mb-1">
-                  <span className="text-sm font-medium text-gray-700">Alcance de objetivo b├ísico</span>
+                  <span className="text-sm font-medium text-gray-700">Alcance de objetivo B├ísico</span>
                   <span className="text-sm text-gray-600">
                     {productivityData ? 
                       `$${productivityData.usdModelo.toFixed(2)} / $${productivityData.goalUsd} USD` : 

commit 20e850136dc6b702dbe720a1fe30fceb62b881c0
Author: Tu Nombre <tu-email@ejemplo.com>
Date:   Wed Oct 1 00:49:17 2025 -0500

    feat: aplicar principio visual consistente del panel admin al dashboard del modelo

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 756e8a77..69697099 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -194,8 +194,8 @@ export default function ModelDashboard() {
   }
 
   return (
-    <div className="min-h-screen bg-white">
-      <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
+    <div className="min-h-screen bg-gray-50">
+      <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-6">
         <h1 className="text-3xl font-semibold text-gray-900 mb-2">Mi Dashboard</h1>
         {user && (
           <p className="text-gray-500 mb-6 text-sm">
@@ -205,7 +205,7 @@ export default function ModelDashboard() {
         )}
 
         {/* Panel de perfil */}
-        <div className="apple-card mb-6">
+        <div className="bg-white rounded-xl shadow-sm border border-gray-200 mb-6 p-6">
           <h2 className="text-lg font-semibold text-gray-900 mb-1">Mi perfil</h2>
           <p className="text-sm text-gray-500">Revisa tu informaci├│n</p>
           <div className="mt-4 text-sm text-gray-700">Email: {user.email}</div>
@@ -213,7 +213,7 @@ export default function ModelDashboard() {
         </div>
 
         {/* Resumen de productividad y progreso de meta */}
-        <div className="apple-card">
+        <div className="bg-white rounded-xl shadow-sm border border-gray-200 p-6">
           <h2 className="text-lg font-semibold text-gray-900 mb-4">Resumen de Productividad</h2>
           
           {productivityLoading ? (

commit 130190fd7f87df928fde05dc9d04100503424630
Author: Tu Nombre <tu-email@ejemplo.com>
Date:   Tue Sep 30 22:28:19 2025 -0500

    feat(dashboard): conectar valores reales de calculadora con resumen de productividad - datos en tiempo real, barra de progreso din├â┬ímica

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 091992e8..756e8a77 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -16,9 +16,19 @@ interface User {
   last_login: string;
 }
 
+interface ProductivityData {
+  usdBruto: number;
+  usdModelo: number;
+  copModelo: number;
+  goalUsd: number;
+  goalProgress: number;
+}
+
 export default function ModelDashboard() {
   const [user, setUser] = useState<User | null>(null);
   const [loading, setLoading] = useState(true);
+  const [productivityData, setProductivityData] = useState<ProductivityData | null>(null);
+  const [productivityLoading, setProductivityLoading] = useState(false);
   const router = useRouter();
   const supabase = createClient(
     process.env.NEXT_PUBLIC_SUPABASE_URL as string,
@@ -61,6 +71,11 @@ export default function ModelDashboard() {
           last_login: new Date().toISOString(),
         };
         setUser(current);
+        
+        // Cargar datos de productividad si es modelo
+        if (current.role === 'modelo') {
+          await loadProductivityData(current.id);
+        }
       } finally {
         setLoading(false);
       }
@@ -68,6 +83,101 @@ export default function ModelDashboard() {
     load();
   }, []);
 
+  const loadProductivityData = async (userId: string) => {
+    try {
+      setProductivityLoading(true);
+      console.log('­ƒöì [DASHBOARD] Loading productivity data for user:', userId);
+
+      // 1) Tasas activas
+      const ratesRes = await fetch('/api/rates-v2?activeOnly=true');
+      const ratesJson = await ratesRes.json();
+      const rates = {
+        usd_cop: ratesJson?.data?.find((r: any) => r.kind === 'USDÔåÆCOP')?.value || 3900,
+        eur_usd: ratesJson?.data?.find((r: any) => r.kind === 'EURÔåÆUSD')?.value || 1.01,
+        gbp_usd: ratesJson?.data?.find((r: any) => r.kind === 'GBPÔåÆUSD')?.value || 1.2,
+      };
+
+      // 2) Configuraci├│n de plataformas habilitadas
+      const cfgRes = await fetch(`/api/calculator/config-v2?userId=${userId}`);
+      const cfg = await cfgRes.json();
+      const enabled = (cfg?.config?.platforms || []).filter((p: any) => p.enabled);
+
+      // Porcentaje y cuota m├¡nima (fallbacks sensatos)
+      const percentage = (enabled[0]?.percentage_override || enabled[0]?.group_percentage || 80) as number;
+      const goalUsd = (enabled[0]?.min_quota_override || enabled[0]?.group_min_quota || 470) as number;
+
+      // 3) Valores del d├¡a (v2)
+      const today = new Date().toISOString().split('T')[0];
+      const valuesRes = await fetch(`/api/calculator/model-values-v2?modelId=${userId}&periodDate=${today}`);
+      const valuesJson = await valuesRes.json();
+      const rows: Array<{ platform_id: string; value: number }> = valuesJson?.data || [];
+      const idToValue: Record<string, number> = {};
+      rows.forEach((r) => {
+        idToValue[r.platform_id] = Number(r.value) || 0;
+      });
+
+      // 4) C├ílculo por plataforma (mismo criterio que calculadora)
+      let usdBruto = 0;
+      let usdModelo = 0;
+      for (const p of enabled) {
+        const value = idToValue[p.id] || 0;
+        if (value <= 0) continue;
+
+        let usdFromPlatform = 0;
+        if (p.currency === 'USD') {
+          if (p.direct_payout) {
+            usdFromPlatform = value;
+          } else if (p.discount_factor) {
+            usdFromPlatform = value * p.discount_factor;
+          } else {
+            usdFromPlatform = value;
+          }
+        } else if (p.currency === 'EUR') {
+          const eurToUsd = value * rates.eur_usd;
+          if (p.tax_rate) {
+            usdFromPlatform = eurToUsd * (1 - p.tax_rate);
+          } else {
+            usdFromPlatform = eurToUsd;
+          }
+        } else if (p.currency === 'GBP') {
+          const gbpToUsd = value * rates.gbp_usd;
+          if (p.discount_factor) {
+            usdFromPlatform = gbpToUsd * p.discount_factor;
+          } else {
+            usdFromPlatform = gbpToUsd;
+          }
+        }
+
+        usdBruto += usdFromPlatform;
+      }
+
+      usdModelo = usdBruto * (percentage / 100);
+      const copModelo = usdModelo * rates.usd_cop;
+      const goalProgress = goalUsd > 0 ? Math.min((usdModelo / goalUsd) * 100, 100) : 0;
+
+      console.log('­ƒöì [DASHBOARD] Calculated productivity:', {
+        usdBruto,
+        usdModelo,
+        copModelo,
+        goalUsd,
+        goalProgress
+      });
+
+      setProductivityData({
+        usdBruto,
+        usdModelo,
+        copModelo,
+        goalUsd,
+        goalProgress
+      });
+
+    } catch (error) {
+      console.error('ÔØî [DASHBOARD] Error loading productivity data:', error);
+    } finally {
+      setProductivityLoading(false);
+    }
+  };
+
   if (loading) {
     return (
       <div className="min-h-screen bg-white flex items-center justify-center">
@@ -105,36 +215,62 @@ export default function ModelDashboard() {
         {/* Resumen de productividad y progreso de meta */}
         <div className="apple-card">
           <h2 className="text-lg font-semibold text-gray-900 mb-4">Resumen de Productividad</h2>
-          <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
-            <div className="p-4 rounded-md bg-blue-50 text-center">
-              <div className="text-xs text-gray-600">USD Bruto (hoy)</div>
-              <div className="text-2xl font-bold text-blue-600">ÔÇö</div>
-            </div>
-            <div className="p-4 rounded-md bg-green-50 text-center">
-              <div className="text-xs text-gray-600">USD Modelo (hoy)</div>
-              <div className="text-2xl font-bold text-green-600">ÔÇö</div>
-            </div>
-            <div className="p-4 rounded-md bg-purple-50 text-center">
-              <div className="text-xs text-gray-600">COP Modelo (hoy)</div>
-              <div className="text-2xl font-bold text-purple-600">ÔÇö</div>
+          
+          {productivityLoading ? (
+            <div className="text-center py-4">
+              <div className="animate-spin rounded-full h-6 w-6 border-b-2 border-blue-600 mx-auto mb-2"></div>
+              <p className="text-sm text-gray-600">Cargando datos de productividad...</p>
             </div>
-          </div>
+          ) : (
+            <>
+              <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
+                <div className="p-4 rounded-md bg-blue-50 text-center">
+                  <div className="text-xs text-gray-600">USD Bruto (hoy)</div>
+                  <div className="text-2xl font-bold text-blue-600">
+                    {productivityData ? `$${productivityData.usdBruto.toFixed(2)}` : 'ÔÇö'}
+                  </div>
+                </div>
+                <div className="p-4 rounded-md bg-green-50 text-center">
+                  <div className="text-xs text-gray-600">USD Modelo (hoy)</div>
+                  <div className="text-2xl font-bold text-green-600">
+                    {productivityData ? `$${productivityData.usdModelo.toFixed(2)}` : 'ÔÇö'}
+                  </div>
+                </div>
+                <div className="p-4 rounded-md bg-purple-50 text-center">
+                  <div className="text-xs text-gray-600">COP Modelo (hoy)</div>
+                  <div className="text-2xl font-bold text-purple-600">
+                    {productivityData ? `$${productivityData.copModelo.toLocaleString()}` : 'ÔÇö'}
+                  </div>
+                </div>
+              </div>
 
-          {/* Barra de alcance de meta (compacta) */}
-          <div className="mt-2">
-            <div className="flex items-center justify-between mb-1">
-              <span className="text-sm font-medium text-gray-700">Alcance de meta diaria</span>
-              <span className="text-sm text-gray-600">ÔÇö / ÔÇö USD</span>
-            </div>
-            <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
-              <div className="h-2 bg-gradient-to-r from-green-500 to-emerald-500" style={{ width: '0%' }}></div>
-            </div>
-            <div className="text-right text-xs text-gray-600 mt-1">0%</div>
-          </div>
+              {/* Barra de alcance de meta (compacta) */}
+              <div className="mt-2">
+                <div className="flex items-center justify-between mb-1">
+                  <span className="text-sm font-medium text-gray-700">Alcance de objetivo b├ísico</span>
+                  <span className="text-sm text-gray-600">
+                    {productivityData ? 
+                      `$${productivityData.usdModelo.toFixed(2)} / $${productivityData.goalUsd} USD` : 
+                      'ÔÇö / ÔÇö USD'
+                    }
+                  </span>
+                </div>
+                <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
+                  <div 
+                    className="h-2 bg-gradient-to-r from-green-500 to-emerald-500 transition-all duration-300" 
+                    style={{ width: `${productivityData?.goalProgress || 0}%` }}
+                  ></div>
+                </div>
+                <div className="text-right text-xs text-gray-600 mt-1">
+                  {productivityData ? `${productivityData.goalProgress.toFixed(1)}%` : '0%'}
+                </div>
+              </div>
 
-          <div className="mt-4 text-sm text-gray-500">
-            Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 underline">Mi Calculadora</a>.
-          </div>
+              <div className="mt-4 text-sm text-gray-500">
+                Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 underline">Mi Calculadora</a>.
+              </div>
+            </>
+          )}
         </div>
       </div>
     </div>

commit 88020c79692dcd7b4bb4d0c67243a2856208aef5
Author: Tu Nombre <tu-email@ejemplo.com>
Date:   Tue Sep 30 13:50:06 2025 -0500

    ux(model-dashboard): reemplazar tarjetas por resumen de productividad y barra de meta; mantener enlace a Mi Calculadora

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index feda69b1..091992e8 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -102,18 +102,39 @@ export default function ModelDashboard() {
           <div className="text-sm text-gray-700">Grupo: {user.groups[0] || 'ÔÇö'}</div>
         </div>
 
-        {/* Informaci├│n adicional del dashboard */}
+        {/* Resumen de productividad y progreso de meta */}
         <div className="apple-card">
-          <h2 className="text-lg font-semibold text-gray-900 mb-4">Acceso R├ípido</h2>
-          <p className="text-gray-600 mb-4">
-            Usa el men├║ "Mi Calculadora" para acceder a tu calculadora de ingresos.
-          </p>
-          <a
-            href="/model/calculator"
-            className="inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
-          >
-            Ir a Mi Calculadora
-          </a>
+          <h2 className="text-lg font-semibold text-gray-900 mb-4">Resumen de Productividad</h2>
+          <div className="grid grid-cols-1 md:grid-cols-3 gap-4 mb-4">
+            <div className="p-4 rounded-md bg-blue-50 text-center">
+              <div className="text-xs text-gray-600">USD Bruto (hoy)</div>
+              <div className="text-2xl font-bold text-blue-600">ÔÇö</div>
+            </div>
+            <div className="p-4 rounded-md bg-green-50 text-center">
+              <div className="text-xs text-gray-600">USD Modelo (hoy)</div>
+              <div className="text-2xl font-bold text-green-600">ÔÇö</div>
+            </div>
+            <div className="p-4 rounded-md bg-purple-50 text-center">
+              <div className="text-xs text-gray-600">COP Modelo (hoy)</div>
+              <div className="text-2xl font-bold text-purple-600">ÔÇö</div>
+            </div>
+          </div>
+
+          {/* Barra de alcance de meta (compacta) */}
+          <div className="mt-2">
+            <div className="flex items-center justify-between mb-1">
+              <span className="text-sm font-medium text-gray-700">Alcance de meta diaria</span>
+              <span className="text-sm text-gray-600">ÔÇö / ÔÇö USD</span>
+            </div>
+            <div className="w-full bg-gray-200 rounded-full h-2 overflow-hidden">
+              <div className="h-2 bg-gradient-to-r from-green-500 to-emerald-500" style={{ width: '0%' }}></div>
+            </div>
+            <div className="text-right text-xs text-gray-600 mt-1">0%</div>
+          </div>
+
+          <div className="mt-4 text-sm text-gray-500">
+            Para actualizar tus valores usa el men├║ <a href="/model/calculator" className="text-blue-600 hover:text-blue-800 underline">Mi Calculadora</a>.
+          </div>
         </div>
       </div>
     </div>

commit 1128ad5663a5a119e6bc1cde41b23a2d854ff139
Author: Tu Nombre <tu-email@ejemplo.com>
Date:   Mon Sep 29 22:06:43 2025 -0500

    Crear p├â┬ígina dedicada para calculadora de modelo
    
    - Crear /model/calculator como p├â┬ígina espec├â┬¡fica para calculadora
    - Dise├â┬▒o optimizado para modelo con secciones claras
    - Remover calculadora del dashboard general
    - Actualizar men├â┬║ para apuntar a nueva ruta
    - Agregar acceso r├â┬ípido desde dashboard
    - Separar responsabilidades: dashboard vs calculadora

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index e89068f2..feda69b1 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -102,8 +102,19 @@ export default function ModelDashboard() {
           <div className="text-sm text-gray-700">Grupo: {user.groups[0] || 'ÔÇö'}</div>
         </div>
 
-        {/* Calculadora para modelos */}
-        <ModelCalculatorNew />
+        {/* Informaci├│n adicional del dashboard */}
+        <div className="apple-card">
+          <h2 className="text-lg font-semibold text-gray-900 mb-4">Acceso R├ípido</h2>
+          <p className="text-gray-600 mb-4">
+            Usa el men├║ "Mi Calculadora" para acceder a tu calculadora de ingresos.
+          </p>
+          <a
+            href="/model/calculator"
+            className="inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700"
+          >
+            Ir a Mi Calculadora
+          </a>
+        </div>
       </div>
     </div>
   );

commit 4eded2648f46d7bee796c4b26f57596825051562
Author: Tu Nombre <tu-email@ejemplo.com>
Date:   Mon Sep 29 21:57:57 2025 -0500

    Replanteamiento completo de calculadora para modelo
    
    - Crear ModelCalculatorNew.tsx con implementaci├â┬│n simplificada
    - Agregar logs de debug extensivos para troubleshooting
    - Implementar fallback con plataformas de ejemplo para testing
    - Corregir endpoint /api/calculator/config para usar userId
    - Actualizar dashboard de modelo para usar nuevo componente
    - Eliminar dependencias problem├â┬íticas del componente anterior

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index 163c5a6f..e89068f2 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -2,7 +2,7 @@
 
 import { useState, useEffect } from 'react';
 import { useRouter } from 'next/navigation';
-import ModelCalculator from '../../../../components/ModelCalculator';
+import ModelCalculatorNew from '../../../../components/ModelCalculatorNew';
 import { createClient } from "@supabase/supabase-js";
 
 interface User {
@@ -103,7 +103,7 @@ export default function ModelDashboard() {
         </div>
 
         {/* Calculadora para modelos */}
-        <ModelCalculator />
+        <ModelCalculatorNew />
       </div>
     </div>
   );

commit 982c805237ee9c21a1316f3d01eb1cf40c8a5932
Author: Tu Nombre <tu-email@ejemplo.com>
Date:   Mon Sep 29 21:31:43 2025 -0500

    Corregir propiedades faltantes en objeto User para TypeScript

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index fabd6e34..163c5a6f 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -56,6 +56,9 @@ export default function ModelDashboard() {
           email: userRow?.email || auth.user?.email || '',
           role: (userRow?.role as any) || 'modelo',
           groups,
+          organization_id: '',
+          is_active: true,
+          last_login: new Date().toISOString(),
         };
         setUser(current);
       } finally {

commit dd6aa6e5fb5993eed25c2a7baaee9c0d07b09dc7
Author: Tu Nombre <tu-email@ejemplo.com>
Date:   Mon Sep 29 21:30:49 2025 -0500

    Corregir import path de ModelCalculator en dashboard de modelo

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
index a58c25ab..fabd6e34 100644
--- a/app/admin/model/dashboard/page.tsx
+++ b/app/admin/model/dashboard/page.tsx
@@ -2,7 +2,7 @@
 
 import { useState, useEffect } from 'react';
 import { useRouter } from 'next/navigation';
-import ModelCalculator from '../../../components/ModelCalculator';
+import ModelCalculator from '../../../../components/ModelCalculator';
 import { createClient } from "@supabase/supabase-js";
 
 interface User {

commit ae8baef0795f105855b8818ae3582347788d4cd9
Author: Tu Nombre <tu-email@ejemplo.com>
Date:   Mon Sep 29 21:30:04 2025 -0500

    SOLUCION RADICAL: Eliminar layout de modelo y usar admin layout

diff --git a/app/admin/model/dashboard/page.tsx b/app/admin/model/dashboard/page.tsx
new file mode 100644
index 00000000..a58c25ab
--- /dev/null
+++ b/app/admin/model/dashboard/page.tsx
@@ -0,0 +1,107 @@
+'use client';
+
+import { useState, useEffect } from 'react';
+import { useRouter } from 'next/navigation';
+import ModelCalculator from '../../../components/ModelCalculator';
+import { createClient } from "@supabase/supabase-js";
+
+interface User {
+  id: string;
+  email: string;
+  name: string;
+  role: 'super_admin' | 'admin' | 'modelo';
+  groups: string[];
+  organization_id: string;
+  is_active: boolean;
+  last_login: string;
+}
+
+export default function ModelDashboard() {
+  const [user, setUser] = useState<User | null>(null);
+  const [loading, setLoading] = useState(true);
+  const router = useRouter();
+  const supabase = createClient(
+    process.env.NEXT_PUBLIC_SUPABASE_URL as string,
+    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY as string
+  );
+
+  useEffect(() => {
+    const load = async () => {
+      try {
+        setLoading(true);
+        // Load current user
+        const { data: auth } = await supabase.auth.getUser();
+        const uid = auth?.user?.id;
+        if (!uid) {
+          setUser(null);
+          setLoading(false);
+          return;
+        }
+        const { data: userRow } = await supabase
+          .from('users')
+          .select('id,name,email,role')
+          .eq('id', uid)
+          .single();
+        let groups: string[] = [];
+        if (userRow && userRow.role !== 'super_admin') {
+          const { data: ug } = await supabase
+            .from('user_groups')
+            .select('groups(name)')
+            .eq('user_id', uid);
+          groups = (ug || []).map((r: any) => r.groups?.name).filter(Boolean);
+        }
+        const current = {
+          id: userRow?.id || uid,
+          name: userRow?.name || auth.user?.email?.split('@')[0] || 'Usuario',
+          email: userRow?.email || auth.user?.email || '',
+          role: (userRow?.role as any) || 'modelo',
+          groups,
+        };
+        setUser(current);
+      } finally {
+        setLoading(false);
+      }
+    };
+    load();
+  }, []);
+
+  if (loading) {
+    return (
+      <div className="min-h-screen bg-white flex items-center justify-center">
+        <div className="text-center">
+          <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto mb-4"></div>
+          <p className="text-gray-600">Cargando dashboard...</p>
+        </div>
+      </div>
+    );
+  }
+
+  if (!user) {
+    return null;
+  }
+
+  return (
+    <div className="min-h-screen bg-white">
+      <div className="max-w-screen-2xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
+        <h1 className="text-3xl font-semibold text-gray-900 mb-2">Mi Dashboard</h1>
+        {user && (
+          <p className="text-gray-500 mb-6 text-sm">
+            Bienvenida, {user.name} ┬À Rol: {String(user.role).replace('_',' ')}
+            {user.groups.length > 0 && ` ┬À Grupos: ${user.groups.join(', ')}`}
+          </p>
+        )}
+
+        {/* Panel de perfil */}
+        <div className="apple-card mb-6">
+          <h2 className="text-lg font-semibold text-gray-900 mb-1">Mi perfil</h2>
+          <p className="text-sm text-gray-500">Revisa tu informaci├│n</p>
+          <div className="mt-4 text-sm text-gray-700">Email: {user.email}</div>
+          <div className="text-sm text-gray-700">Grupo: {user.groups[0] || 'ÔÇö'}</div>
+        </div>
+
+        {/* Calculadora para modelos */}
+        <ModelCalculator />
+      </div>
+    </div>
+  );
+}
\ No newline at end of file
